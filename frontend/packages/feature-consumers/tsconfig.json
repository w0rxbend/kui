{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "../../.tsbuild/feature-consumers",
    "tsBuildInfoFile": "../../.tsbuild/feature-consumers.tsbuildinfo",
    "rootDir": "src",
    // `src/recorded/*.json`: documents captured from a running gateway, decoded by
    // `recorded.test.ts` so the field names are checked against what the server really sends.
    "resolveJsonModule": true
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.json"],
  "references": [{ "path": "../api" }, { "path": "../kernel" }]
}
