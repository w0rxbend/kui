/**
 * The consumers feature. A placeholder until lane D ports the screens; it exists so that the
 * workspace graph, the project references and the bundle-shape check (SOL-010) have all four
 * feature packages to work with.
 */
export default function Placeholder() {
  return <p>The consumers feature has not been ported yet.</p>;
}
