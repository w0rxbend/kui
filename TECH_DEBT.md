# KUI technical debt register

Debt is recorded when it is taken, not when it hurts. Every row names the ADR or task that
accepted it, the reason, the exit condition and an owner. Rows are closed by a commit that
references the row id.

**ADR-048 (2026-09-05) replaced the Scala.js and Laminar frontend with TypeScript, SolidJS and
Vite.** Four rows below — TD-013, TD-015, TD-016 and TD-020 — describe defects in code that has
since been deleted. They were marked *superseded* rather than *closed*, because "the code is gone"
is not the same statement as "the problem is gone": whether an equivalent defect exists in the
TypeScript frontend had not been assessed, and a row silently closed is a question nobody asks
again. Assessing them is the exit condition each names.

**On 2026-09-06 two of them were assessed, and the assessment went both ways.** TD-016 is closed on
exactly the evidence its own exit condition asked for, read out of a real build's Vite manifest —
and the *other* half of that exit condition, the build rule that would catch the same regression
without a browser, was not built, so it is now TD-022 rather than a sentence inside a closed row.
TD-013 is *not* closed: the sentence written into it when it was superseded — that CodeMirror and
uPlot are now used through their own published TypeScript types — was an assumption nobody checked,
and it is false. That is the failure this register exists to catch, and it happened inside the
register itself, so it is worth naming here: a row rewritten from what the rewrite *expects* to be
true has stopped recording anything.

**Later the same day, TD-022 closed too, and the manner of it is the point.** The rule its exit
condition asked for exists as `frontend/scripts/bundle-shape.mjs` and runs in CI — but the row is
closed on having *run the check in both directions*, green against the real manifest and red against
one edited to move a feature out of the entry's `dynamicImports`, rather than on the file existing.
TD-016 became TD-022 by being closed on a check nobody had run; a register that repeated that would
be recording nothing twice. **Rows are ordered by id.** TD-022 was filed between TD-020 and TD-021
and has been moved, because a register that is not in id order is a register a reader stops
scanning and starts searching, and the row they were looking for is the one they then fail to find.

| ID | Area | Debt | Accepted by | Why accepted | Exit condition | Owner | Opened | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-001 | build | Chimney 2.0.0-RC1 (release candidate) in production modules | ADR-033 | Chimney 1.x breaks under `-Werror` on Scala 3.9; 2.0.0 final not yet published | bump to 2.0.0 final, or replace affected mappers by hand-written ones if RC1 fails under `-Werror` | Principal Scala Engineer | 2026-09-03 | open |
| TD-002 | telemetry | `opentelemetry-exporter-prometheus` is an `-alpha` artifact | ADR-009 | only Java-SDK Prometheus scrape exporter | exporter reaches a stable version, or switch to `otel4s-sdk-exporter-prometheus` | Infrastructure Lead | 2026-09-03 | open |
| TD-003 | gateway | Sessions are in-memory: one gateway replica only | ADR-019 | single replica acceptable through M5 | Kafka compacted-topic `SessionStore` adapter shipped (M6); ADR-042 fixes the record envelope and topic conventions it will reuse | Security Engineer | 2026-09-03 | open |
| TD-004 | cluster state | 3–4 partially overlapping admin scrapes per cluster in distributed mode | ADR-027 | context isolation outweighs duplicated calls | internal events topic (`kui.internal.events`) research at M6 decides whether to share scrapes | Chief Architect | 2026-09-03 | open |
| TD-005 | messages | Signed cursors grow ~20 B per partition; topics with more than ~1 000 partitions may exceed practical query-string size | ADR-026 | rare; explicit `KUI-CURSOR-TOO-LARGE` error with partition-subset hint | cursor compaction (range-run encoding) or header-carried cursor if a real cluster hits it | Domain Architect (messages) | 2026-09-03 | open |
| TD-006 | serdes | Kafbat Java serde jars are not loadable until the bridge module ships (M6+) | ADR-028 | Scala SPI first; bridge is additive | `libs/serde-kafbat-bridge` merged with a sample-jar test | Kafka Specialist | 2026-09-03 | open |
| TD-007 | frontend | Design tokens are KUI's own (derived from the reference products), not from an external design source | ADR-024 | The external design import may never arrive and the frontend could not wait, so the decision was taken instead of deferred. Values are seeded from Kafbat's palette; the token vocabulary and the constraints are KUI's | Optional: if `research/design/tokens.md` ever lands, reconcile the values in the single token file (no component changes). The Shoelace half of this row is closed: ADR-024 decided against a third-party widget library | Frontend Architect | 2026-09-03 | open (low) |
| TD-008 | search | In-memory trigram search only; Lucene deferred without a benchmark yet | ADR-038 | data sets small; avoid JDK coupling | benchmark on ≥ 50 k names recorded in `docs/benchmarks/`; adopt Lucene only if p95 > 50 ms | Performance Engineer | 2026-09-03 | open |
| TD-009 | ksql | **A decision taken in advance, not debt.** ADR-037 plans two code paths in the ksqlDB adapter — `/query-stream` with a legacy `/query` fallback. **Re-labelled 2026-09-06:** there is no ksqlDB adapter. `services/ksql` does not exist, no `.scala` file under `libs/` or `services/` names `/query-stream`, and the only ksql code in the tree is configuration vocabulary (`libs/config/KsqlSettings.scala`, `ClusterConfig`) and an unused `ErrorCode`. Nothing is maintained twice, so nothing is owed. It was recorded as open debt on the day the ADR was written, which is a category error: planning to accept a cost is not the same as paying it, and a register that mixes the two cannot be read to find out what the project is actually carrying | ADR-037 | old ksqlDB versions are still deployed, so the adapter is expected to need both paths when it is built | this becomes real debt on the day `services/ksql` ships two paths (M9 in `docs/plan/ROADMAP.md`), and is reviewed then. The original exit — drop `/query` once the minimum supported ksqlDB version is raised — applies from that day and not before | Domain Architect (ksql) | 2026-09-03 | prospective |
| TD-010 | config | Keystore bytes are carried inline in `ClusterProfile` over the inter-service channel | ADR-036 | avoids shared filesystems in Kubernetes | revisit if a secret-manager integration (Vault/K8s CSI) is adopted; then carry references | Security Engineer | 2026-09-03 | open |
| TD-011 | metrics | Prometheus Pushgateway / remote-write sinks not implemented (Kafbat parity gap) | ADR-009 | OTLP push covers the need; feature deferred post-M8 | implement as a metrics-service sink if users ask | Domain Architect (metrics) | 2026-09-03 | open |
| TD-012 | identity | Authentication adapter changes (OIDC providers, LDAP) require an identity-service restart | ADR-036 | only `rbac` hot-reloads, as in Kafbat | make auth adapters rebuildable on config version change | Security Engineer | 2026-09-03 | open |
| TD-013 | frontend | Hand-maintained CodeMirror 6 / uPlot facades (~300 lines) | ADR-025 | no maintained Scala.js facades exist | **Superseded 2026-09-05 by ADR-048; the superseding note itself corrected 2026-09-06.** A facade is a Scala.js binding to a JavaScript library, and there is no Scala.js any more, so the 300 lines are gone. The reason this row gave for that was wrong. It said both libraries "are used through their own published TypeScript types", and **neither library is a dependency at all**: a case-insensitive search for `codemirror` and for `uplot` across `frontend/package.json`, every `frontend/packages/*/package.json` and `pnpm-lock.yaml` matches nothing. Schema text is rendered as plain text in `feature-schemas` and there is no charting library in the tree. The 300 lines went with the *feature*, not into a re-implementation, which means the maintenance question was not answered — it was deferred, without anybody saying so. Exit: when a code editor or a charting library is chosen for the TypeScript frontend, decide it in an ADR and reopen this row against that choice. The cost this row was opened for — hand-maintaining bindings to a library whose upstream maintains none — is a live risk for both kinds of library, and only looks absent because neither has been picked yet | Frontend Architect | 2026-09-03 | superseded |
| TD-014 | persistence | `docs/FEATURE_MATRIX.md` OT-004 scheduled one relational store shared by two services, which the project's technology constraints forbid and ADR-036 rejects | architecture review 2026-09-03 | the conflict was real but the feature is P1/M6, so it did not block M0 | **Closed 2026-09-03 by ADR-042**: KUI metadata lives in internal compacted Kafka topics; no relational database, ever. OT-004 and the CL-006 / RB-004 rows now name ADR-042 | Chief Architect | 2026-09-03 | closed |
| TD-015 | frontend | `frontend/ui-clusters` ships in M0 as a single ping button with no design source of truth, and its page composition and feature-local state shape are copied by every microfrontend after it | architecture review 2026-09-03 | one trivial page is not enough evidence to fix a pattern; M2 has real screens | the pattern is decided against a real screen before M2 and `docs/frontend/features.md` records it | Frontend Architect | 2026-09-03 | **closed** — `docs/frontend/features.md` rewritten against the clusters and topics features (TOP-031). Its registration checklist was verified by following it to add a throwaway feature and deleting it again, which is what found the missing seventh step. *(The modules named in this row were Scala.js and were deleted by ADR-048; the document has been rewritten against the TypeScript packages and the row stays closed.)* |
| TD-016 | frontend | The clusters microfrontend is linked into a JavaScript module of its own and then **statically imported by `main.js`**, so the browser downloads it during the first paint anyway. ADR-012's user-visible promise — a feature is fetched only when someone navigates to it — is therefore not kept, for any user, including one whose deployment has no cluster service | E2E-001 | Found by the first browser test ever run against the shell. `checkBundleShape` passes at the same time and is not wrong: it checks that the feature's code is not *copied into* `main.js`, which is true. Nothing checks that `main.js` does not *import the split file eagerly*, which is what a browser acts on. Fixing it means finding why the shell statically reaches `ClustersFeature` (the linked `main.js` references one of its private methods) and is a frontend change, not a test change, so it is recorded rather than bundled into the harness that found it | `ShellSmokeSuite`'s "clusters module is not fetched on first paint" passes with its `.fail` marker removed, and `BundleShape` grows a rule forbidding a static import of a feature module from `main.js` so the build catches a regression without a browser | Frontend Architect | 2026-09-03 | **closed 2026-09-06, on exactly the evidence this row's own exit condition named.** Superseded first, 2026-09-05, by ADR-048: there is no `main.js` and no Scala.js linker, and the equivalent question for Vite is whether the entry chunk statically `imports` a feature's module rather than listing it under `dynamicImports`. `frontend/dist/.vite/manifest.json`, from a real build, answers it. The entry (`index.html`) carries one static import — a shared `routing` chunk, which imports nothing further — and lists all five feature packages (`feature-clusters`, `feature-topics`, `feature-messages`, `feature-consumers`, `feature-schemas`) under `dynamicImports`. ADR-012's user-visible promise is kept: a browser fetches a feature's code when somebody navigates to it, and a deployment with no cluster service never fetches that feature at all. **What is not closed is the guard.** The `BundleShape` rule this row asked for does not exist, and the only thing between here and the same regression is that the shell reaches a feature solely through `registry.ts`'s `import()`, so a static import would have to be written on purpose. That is a convention and not a gate — re-reading the manifest is how it is checked until one exists, and **the guard is carried as TD-022 below** rather than left inside a closed row, because an exit condition that is half met and then filed away is met by nobody |
| TD-017 | frontend | The broker detail page renders every log directory's figures, but not the per-partition table BR-005 and PA-003 describe. **Half closed 2026-09-04 (E6):** `LogDirDto` now carries `replicas`, a per-topic-partition breakdown (topic, partition, size, offset lag, future-replica flag) sorted biggest-first by the cluster service, so "which topic is filling this disk" is answerable from the wire. No screen reads it yet | CLUI-005 | The screen may not invent a second request to fill a cell, and it may not fabricate rows. The contract half was the blocking half: four screens (BR-005, PA-003, TP-003 and the topic detail size) were each waiting on the same field | the log-directories tab renders `replicas` as a per-directory table, which is where the unvirtualized-table debt below then lands | Chief Architect (contract, done), Frontend Architect (screen, open) | 2026-09-04 | open |
| TD-018 | frontend | When TD-017's per-partition table arrives, it will be unvirtualized: a busy broker has thousands of topic-partitions and every row would be in the DOM | CLUI-005 | `VirtualizedTable` is an M2 deliverable (SF-003) and building a second one early would be the thing M2 then has to delete | the per-partition table is the first caller of `VirtualizedTable` when SF-003 ships | Frontend Architect | 2026-09-04 | open |
| TD-019 | frontend | Broker configuration *synonyms* are carried by `BrokerConfigEntryDto` and are not rendered anywhere | CLUI-005 | A nested expansion showing where each value was overridden is a screen of its own, and nothing in this milestone's criteria needs it. Rendering half of it would be worse than not rendering it | shown alongside the broker config editing screen, which is where somebody needs to know which layer they are about to change | Frontend Architect | 2026-09-04 | open |
| TD-020 | frontend | A microfrontend navigates between its own pages by calling `history.pushState` itself, because `KuiFeature` has no navigation capability and a feature is constructed through `js.dynamicImport`, which cannot be handed the shell's router. Rows are real links with real `href`s and an ordinary click stays inside the application, but the pushed history entry carries no `history.state`, so Back onto a feature page is resolved from the URL rather than from the stored state | CLUI-003 | The alternative was passing a router into a dynamically imported class, which the import mechanism does not allow, or a global mutable router singleton, which is what the feature registry exists to avoid | `KuiFeature` gains a navigation port supplied by the shell, and the feature calls it instead of touching `history` — naturally alongside the cluster switcher, which has to navigate from the shell into a feature's page and has the same problem from the other side | Frontend Architect | 2026-09-04 | **closed 2026-09-07**, having been superseded 2026-09-05 by ADR-048 — `KuiFeature`, `js.dynamicImport` and the constructor-argument limitation it describes are all gone. A feature is now a SolidJS component rendered by the shell's own route table, so it can use the router directly. The close is the grep this row's exit condition named, run rather than reasoned about: `grep -rn 'pushState\|replaceState' frontend/packages --exclude-dir=node_modules` returns eleven lines and **no call from a feature package**: two are prose in `feature-messages/src/MessagesRoute.tsx:250-252`, which exists to say the address bar is written through the router *because* `window.history.replaceState` changed the URL and told `useLocation()` nothing; two more are the same story in that package's `route.test.tsx` (:16 and :466); and the remaining seven are `frontend/packages/shell/src/app.render.test.tsx` seeding jsdom's address before a render. The debt was that a feature had no way to navigate but `history`; a feature now has the router and the only mentions left are explanations of why the old way was wrong |
| TD-021 | contract | Tapir's generated client cannot decode the profile endpoint's 304 answer. The endpoint models its two outcomes as a `oneOf` of 200-with-a-body and 304-without-one, which is right for the server and right in the generated document, but the client interpreter attempts a body decode against the empty 304 payload and fails with "Cannot decode from: ". So `services/cluster/client` assembles that one request by hand instead of through the endpoint value | TOP-009 | Found by running the two sides against each other, which is the only way it could be found: the server's own suite asserts the 304 with a raw request, and the client's decoder is correct on its own. Left as it is because the shape is right and the workaround is small and contained: the request is built from the contract's own path constants and the answer decoded with the contract's own decoder and `etagOf`, so nothing about the wire is decided twice — only the request assembly is. Every other call in the module still goes through the endpoint value | either Tapir decodes a bodyless `oneOf` variant (retest on the next Tapir bump and delete the hand-built request), or the endpoint drops the `oneOf` and models the 304 as an `Option` body with an explicit status output. Whichever, `anUnchangedProfileAnswers304AndFiresNoChange` is the test that says it worked | Chief Architect | 2026-09-04 | open |
| TD-022 | frontend | ADR-012's promise that a feature's code is fetched only when somebody navigates to it is kept by **convention, not by a gate**: nothing in either build fails if the shell's entry chunk comes to import a `feature-*` package statically | TD-016's close, 2026-09-06 | TD-016 was closed on exactly the evidence its exit condition named — a real build's `frontend/dist/.vite/manifest.json`, which lists all five feature packages under the entry's `dynamicImports` and nothing but a shared `routing` chunk under `imports`. That answers whether the promise holds today; it does not answer whether it will hold after the next refactor, and the second half of that exit condition — a `BundleShape` rule the build runs — was not built, and the object it was to be grown from cannot be: `build-tests/src/kui/build/BundleShape.scala` reads Scala.js linker output — mangled `$c_` symbols and a `main.js` — which ADR-048 deleted, no `checkBundleShape` task remains in `build.mill`, and the only thing that still exercises the object is its own unit suite against synthetic input. A Vite-shaped check is a new one, not an extra rule on the old one. Recording it here rather than inside the closed row is the point: a register whose closed rows carry unbuilt work cannot be read to find out what the project is carrying, which is the failure this file already caught once in TD-013 | a check on the committed side of the build reads `dist/.vite/manifest.json` after `pnpm build` and fails when the entry chunk names any `frontend/packages/feature-*` module under `imports` instead of `dynamicImports`, and it runs in CI so a regression does not need a browser to be noticed | Frontend Architect | 2026-09-06 | **closed 2026-09-06 by `frontend/scripts/bundle-shape.mjs`, and closed on the check having been run in both directions rather than on the check existing** — which is the distinction TD-016 was closed without and this row was opened for. The script reads `dist/.vite/manifest.json` after `pnpm -C frontend build` and fails on either of two things: a `frontend/packages/feature-*` chunk reachable from the entry chunk through `imports`, transitively (because `entry → shared chunk → feature` costs a browser exactly what `entry → feature` costs), or a feature that is no longer a dynamic import of any chunk (because a static import the bundler *inlines* leaves no manifest entry for the first rule to catch). Its roster of features is read off the filesystem, and it says so and fails when that read is empty. Run twice on 2026-09-06: against the real manifest it printed `✓ 5 feature packages, all dynamically imported and none reachable statically from the entry chunk` and exited 0; against a copy of that same manifest with `packages/feature-topics/src/index.tsx` moved out of the entry's `dynamicImports` and into its `imports` it printed both violations and exited 1. It runs in CI as the `frontend` job's *The features are still loaded on demand* step, so a regression is caught without a browser. `build-tests`'s `BundleShape.scala` stays where it is — dead, exercised only by its own suite against synthetic Scala.js input, and reachable from no build task — and this row does not pretend otherwise |
| TD-023 | process | **A rule that no test can break, carried as a standing debt instead of as a note in a wave plan.** Wave 3 shipped twelve of them — after its own house rules said in as many words that a gate you cannot make fail is not a gate, and after all fourteen of its packets had run a mutation and watched a named case go red. Wave 4 closed all twelve **and reported new ones of its own in the same breath**, which is the finding this row exists to keep: the defect is not a list of twelve rules, it is that a packet mutates the code it has just written and therefore finds the case it has just written. The twelve are closed and each was re-verified here by mutation on 2026-09-06, one at a time, each reverted: the shell's out-of-order search guard (`App.tsx`, 2 cases red), the topics screen's shared row/card selection set (1) and its never-zero consumer-group tile (1), the consumer group route's two toasts (2), the schema screen's sort direction (1), its two level-set toasts (2) and its registry total (1), the message browser's copy toast (2), the brokers screen's version tag (1), the gateway's per-kind search `limit` cap (1) and its `SearchSections` non-`Ok` branches (1), and `SchemaEndpoints.MaxPageSize` set to 250 (2). **The nine this row opened with were a floor, and the count is in: 35 and 26.** Both figures are wave 4's own independent verification, transcribed here rather than recounted in this pass, and both are the numbers its retrospective publishes. All ten of its packets were verified: they closed ten of ten assigned rules — every one re-applied by a second agent who watched the named case go red — and shipped **thirty-five new ungated rules**, of which **thirty-one were not disclosed** by the packet that wrote them, under a house rule that required exactly that disclosure. Separately, five agents mutated **89 rules in code nobody was editing** and **26 survived — 29%**, written in M0–M5 and nothing to do with wave 4. So the mechanism closes about one hole per packet and a packet opens about three and a half, and the house rate underneath both is 29%. **Assignments, wave 5:** the six values decided in `MetricsWiring.scala` and `PrometheusExposition`'s `isInfinite` guard → W5-01, which reports building `MetricsWiringSuite` and closing the retry count by a mutation a verifier re-applied; `CompatibilityUseCases.MaxDefinitionBytes` measured against itself and `SchemaMapping.toRegister` dropping `references` → W5-A3; the census's `libs/` and five-service survivors — `RbacGuard`'s undecidable branch, `PrincipalVerification.reject`, `LogbackSelection.apply`, `AuditPrincipal.render`, `ClusterMapping`, `ClusterSnapshots`, `CapabilityMapping` and `MutationGuard` → W5-A1; the six found in wave 4's own `feature-schemas`/`feature-clusters` output → W5-A2. Three of wave 5's twelve packets build nothing and mutate code they did not write, because builders found 4 of their own 35 holes and second readers found 31. **Wave 5's three tables are in, and they are the first numbers this row can be read against.** The three adversarial packets mutated **162 rules, scored 156 that carried a rule, found 72 ungated — 46% — and closed 65 with a case that genuinely fails**; ten of those closures were re-applied one at a time at integration and every one reddened the named case. The nine building packets closed nine of nine assigned rules and shipped **33 new ungated ones, 27 of them undisclosed** by the packet that wrote them. So the disclosure rate moved from 11% to 18% and the rate of holes shipped did not move at all — 3.5 per building packet in wave 4, 3.7 in wave 5 — which settles the mechanism question this row was opened over: four waves of instruction have produced no measurable change in a builder's ability to find their own hole, and a fifth instruction will not. **The arithmetic is the first that ends ahead:** wave 4 closed 10 and opened 35, net −25; wave 5 closed 12 owned plus 65 by adversaries and opened 33, net **+44**. Per packet, an adversary closed about 22 and a builder opened about 3.7. **The 46% is not a regression against wave 4's 29%** and must not be read as one: wave 4 sampled 89 rules at random, and wave 5's three packets chose theirs by reading comments for *a rule with a paragraph defending it and no test*, which is a hunting method and not a census. What it establishes is that the heuristic works — in code nobody is editing, reading the comments finds a hole about every other try. **Seven were left open, and each is assigned in wave 6 rather than left to be found a third time:** `services/topic`'s `MutationGuard.scala:174-178` and `services/message`'s `:141-145`, which audit a **cancelled** mutation as `Failed` when `MutationOutcome.Unknown`'s own scaladoc says a record claiming either is a lie — both **live production defects**, telling an operator a topic delete or a produce did not happen when it may well have — and `services/cluster/application/.../ClusterSnapshots.scala:411-413`, where KUI's own outage is recorded as the cluster's and which needs a `Tuning` seam to observe, all three → **W6-A1**; `SchemaWiring.scala:227-253`'s `tokenBackendFor` aimed at `settings.urls` rather than the configured issuer (a client secret sent to the wrong system), `:267-291`'s `startupLog` losing its *no cluster configures a registry* line, and `RegistryCredentials.scala:164-179`'s token cache made never to expire, all three needing a seam — two `private[app]` and one `cats-effect-testkit` — → **W6-A2**; and `frontend/packages/feature-clusters/src/BrokerList.tsx:130-135`'s empty-list `totalLeaders` of `0`, the third **live production defect** and the milestone's own central rule broken, → **W6-A3**. **Wave 6's own three tables are not in this row**, for the same reason wave 5's were not when it was written: the three adversarial packets run after the packet that owns this file. Whoever integrates wave 6 adds their N-mutated / M-survived figures here, and the one structural change wave 6 makes beside them — every verification pass now owns the test tree of the packet it verifies and lands a case for every hole it finds — is measured by whether the builders' disclosure rate rises above a third while the adversaries' rate falls. **And one correction, which is the reason this row records a rate and not a list.** The census reported a fail-open authorization guard as its worst finding and said honestly that it had run only `./mill libs.__.test`. Re-run here on 2026-09-07 against the suite that exercises it, the reported branch is **gated**: `RbacGuard.fromPolicy`'s `Decision.Denied` arm made to answer `().asRight` reddens two named cases in `services/cluster/api`'s `ServiceRbacGuardSuite` — *a caller without ClusterConfig.View is refused broker settings by the service itself* (:67) and *a valid principal holding no role at all is refused, however it reached this port* (:86). The branch **beside** it — `case Left(problem)`, the fail-closed refusal of a request the authorization declaration cannot decide at all — made to answer `().asRight` leaves `services.cluster.api.test` at **752/752 SUCCESS**, `libs.__.test` at **1672/1672** and every other service's `api` suite at **2801/2801**, all measured here with the mutation applied and reverted. The severe finding was real and it was the *other* one, **Wave 6's three tables are in, and they are the numbers this row asked for.** The ten building packets closed **ten of ten** owned rules — every one re-applied by an independent verifier who watched the named case go red — and shipped **46 new ungated rules, 37 of them (80%) undisclosed** by the packet that wrote them. The three adversarial packets applied and scored **130 mutations, found 75 genuine ungated rules — 58% — and closed all 75** with a case that fails; **ten** of those closures were re-applied one at a time at integration, across both languages, and every one reddened the case the packet named. And the one mechanism change wave 6 made **did not happen at all**: house rule 13 gave every verification pass the test tree of the packet it verifies and asked for a case per hole, and **46 holes were found and zero cases were landed** — not through laziness but because a verifier runs while the packet it verifies is still the live owner of that tree, so describing the case is the only honest thing it can do. The three rates now read **3.5 → 3.7 → 4.6 holes per building packet** and **11% → 18% → 20% disclosed**, over five waves of instruction; the arithmetic reads **−25, +44, +39**. **What wave 7 does with the 46 is the whole of the mechanism this row was opened to test:** every one of them is an *owned rule* in the packet whose tree it lives in, quoted with the exact mutation that leaves the suite green, because a named owned rule has now been closed 10/10 in wave 4 and 10/10 in wave 6 and is the only mechanism in this project with a perfect record. House rule 13 is retired as written and replaced: a verifier's finding is either an owned rule in the next wave's plan or a case in a closing adversarial packet that runs **after** the building packets freeze, and a finding with neither is not filed. **And the cheapest attack on this file's own comparison gate, measured rather than asserted (house rule 17).** Wave 6 reported that a green false figure cost five edits across three files; re-measured on 2026-09-10 against the script as it stood, it cost **one** — `carries("X-Csrf-Token")` → `carries("X-Kui-Principal")` in `feature-matrix-check.sh`'s own `jq`, after which ADR-048 and `frontend/packages/api/README.md` could publish `X-Csrf-Token on 52 operations` with the run printing `216 claims checked, all true` and exiting 0. The ledger now carries what each comparison was compared *against*, the merged-document facts are re-derived from the document after the section closes, the header table is reconciled against the document header by header, and the comparator itself is driven into both of its states before a document is read. **That published figure of four was wrong, and this is the third wave running in which this row's own price has not survived being measured.** Re-measured on 2026-09-11 against the file as wave 7 shipped it, with the documents true and the repairs the eleventh service needed already in, the same false figure cost **one line**: `claim "$kind-operations" "$tok" "${BASH_REMATCH[2]}" "$fact"` → `... "$fact" "$fact"` at one call site in `check_document_region`, after which ADR-048 and `frontend/packages/api/README.md` published `X-Csrf-Token on 59 operations` against a document carrying it on 26 and the run printed `256 claims checked, all true` and exited 0. Every gate agreed and each was right to: the kind was `csrf-operations`, the marker declared it, the count was 256, the residue saw the figure struck out, and the ledger pair was `26>26` — which is exactly what the audit re-derives from the document, because it **is** the document's figure. A comparison of a fact with itself is a true statement about nothing, and every fixture wave 7 built read one side of it. **Six refusals were mutated the same day, one at a time, and the run stayed green for all six**: `header_fact_from_document`'s body replaced by a read of the very table it exists to be independent of; `claim`'s refusal of a call that records and consumes without comparing, `if (( $# == 0 || $# % 3 != 0 ))` → `if false`; the empty-marked-block refusal in both region loops; `reconcile_region`'s refusal of a marker with no `claims:` list; `close_section`'s count assertion weakened `==` → `<=` in one character; and `header_for_kind`'s `csrf)` arm renamed. Every one of the six is a refusal whose failing case no document in this repository has ever produced — which is why nothing distinguished them from `true`. **Wave 8's repair is section 6 of that script, and the fixtures are the deliverable**: each refusal is driven over input written for the occasion — an empty marked block, a marker with no list, a two-argument comparison, a section closed at the wrong count, a fixture OpenAPI document with one operation on it — and each fixture was verified red against the mutation it was written for and named in the complaint. Beside them, two gates on the *claimed* side of a comparison: `claim` refuses a claimed figure that is not in the text it struck out of the block, and `audit_document_facts` re-reads each block out of its own file and requires every figure the ledger says that block published to be in it. **The cheapest attack on the finished file is two lines**, measured the same way: strike the fragment by hand and compare the fact with itself at the claim site, then widen the audit's own `block=` derivation so the invented figure is found in it. One line is not enough for any attack tried — nine were applied to the shipped file, run and reverted, and the seven single-line ones are each red in a named gate. Two is the measured number, not an assertion, and the next wave should try to beat it rather than repeat it. **One correction to the disclosure this row inherited:** it recorded that editing `kind_for_header` and `header_for_kind` *consistently* keeps the run green. It does not, and did not then either — the claim kinds rename with the pair, so both markers' `claims:` lists and the registry disagree. Measured on 2026-09-11 by swapping `csrf` and `principal` in both functions at once: **4 disagreements**, two from the markers and two from `reconcile_registry`'s two call sites. The seam was always `header_for_kind` **alone**, paired with an edit at the claim site, and that pair is what fixture 6 now refuses. **Wave 9 re-measured the number and it was one, not two — but in the half of the file wave 8's eight fixtures never reached.** Every one of those fixtures drives the marked-document machinery, and section 3 has no markers: `claim npm-version "" "$version" "${matched:-not in the cell}"` rewritten to `... "$version" "$version"`, one line, let `DEPENDENCY_MATRIX.md` publish `vite` at `9.9.9` against a `frontend/package.json` pinning `8.2.2` with the run printing `283 claims checked, all true` and exiting 0. Attacking the repair then found **three more one-line collapses of one shape**, none of them in the section the wave plan pointed at: a `claim` call whose `matched` argument is empty skips the claimed-side refusal entirely, so `state-total` let the matrix paragraph publish `99 COMPLETE` against a table of 70, `adr-file` let `DECISIONS.md` point ADR-052's row at ADR-053's document — the exact omission section 5 was written after — and `milestone-rows` let the milestone table publish any row count, each at `346 claims checked, all true`, exit 0. All three now pass the fragment they read out of the document; the residue and the Total line's split, whose fact is its own arithmetic and which therefore no second reading can gate, have fixtures instead. **The cheapest attack on the file as wave 9 leaves it is two lines, measured:** `dependency_version_fact` rewritten to `printf '%s' "$2"` — which answers the pin whatever the row says, and so defeats the comparison and its audit together — plus fixture 9's disagreeing case compared with itself, after which `vite` at `9.9.9` printed `348 claims checked, all true` and exited 0. The cost is published in the script's own header from this wave on, and not here: a price kept in a second document is a figure nobody re-derives, which is how this row carried a four for three waves. | wave 4 integration, 2026-09-06 | Recording it as debt is the only thing that has not been tried. Naming the rules in the wave plan did not close them in wave 3; asking every packet for a mutation did not find them, because the mutation a packet plans finds the case it has just written; and asking for a mutation that stays **green** did find them, in the two packets somebody checked. What none of those does is survive the deletion of the wave plan, which is what happens to a wave plan the day its work lands — so a hole found by verification and not repaired that day has, twice now, been rediscovered by the next wave rather than remembered | each rule named above reddens a named case under a stated mutation, applied to the shipped code and reverted, with the case named in the row that closes it. `MetricsWiring` has gained the suite that constructs it (`MetricsWiringSuite`, wave 5), which was the condition six of the original nine were green for. **The measurable exit is now a rate, not a list**, and it is the one wave 4's retrospective set: the three adversarial packets report N rules mutated and M survived over the code they attack, and this row closes when that ratio is materially below the 26-in-89 the census measured. A list closes and reopens one line over; a rate does not | Chief Architect | 2026-09-06 | open |
| TD-024 | contract | `Section[A]` is documented with `Schema.any`, so **25 properties across the aggregated responses are typed `unknown`** in `frontend/packages/api/src/schema.d.ts` and the browser hand-writes every payload shape it needs. Count them with `grep -c ': unknown;' frontend/packages/api/src/schema.d.ts` — **177 lines on 2026-09-11, of which 152 are the index signature `[name: string]: unknown;`** — or list them with the same grep. **This row named a type that does not exist, and the correction is the point of re-measuring rather than restating.** Wave 7's entry credited the Connect endpoints with `ConnectorsResponse.connectors`; no such schema is emitted. The property wave 7 added is `ConnectorListResponse.connectors`, one property and not the two the row implied, and it is at `frontend/packages/api/src/schema.d.ts:1657` after this wave's regeneration. **Re-measured on 2026-09-11 against the document the eleventh service produced**, the count is **25**, and wave 8 moved it by one in the same direction: `KsqlObjectsResponse.objects` (`:1951`), the ksqlDB object list, is the twenty-fifth. The regeneration again produced **no named type for any of the five metrics payloads**, so none of the five this row was opened for has closed. Five are the metrics reads (`LatencyResponse.latency`, `ThroughputResponse.throughput`, `TopProducersResponse.producers`, `RecordSizeResponse.recordSize`, `RequestHandlersResponse.requestHandlers`); the other twenty are every aggregated list in the contract, `AlertFeedResponse.events` and `ConnectorListResponse.connectors` among them. | wave 6 integration, 2026-09-07, on the regeneration buying the browser nothing | The regeneration this wave ran produced no new type for any of the five metrics payloads, so the seam ADR-048 bought — rename a field on the server and the browser's build fails — does not exist for them. `frontend/packages/shell/src/overview/metrics.ts` therefore declares seven interfaces that mirror `services/metrics/contract`'s DTOs by hand: `LatencyBucket`, `LatencySeries`, `PurgatoryQueue`, `HandlerDocument`, `TopicProducerEntry`, `ProducerDocument` and `RecordSizeDocument`. **Two of those seven were transcribed wrongly and nothing failed.** The server shipped `producers.data.{measuredBy, topics[{topic, bytesInPerSecond}]}` and the browser read `producers.data.entries[{clientId, bytesPerSecond}]`; both sides were unit-tested against their own shape, the top-level `Section` key matched so the decode *succeeded* and answered an empty array, and the card drew *the metrics source answered and named no producers* over a source that had named five — for a whole milestone, with every gate on both sides green. That is what an opaque payload costs, measured rather than argued. The interim defence is wave 6's, and it is a golden file rather than a type: `services/metrics/contract/test/resources/golden/*.json` are documents rendered by the **server's own encoder** and `wire.golden.test.ts` runs them through the browser's fetchers, so a shape that drifts now fails a browser case. That is narrower than a type: it catches drift in the fields the goldens exercise and it catches it at test time rather than at compile time | the five metrics `data` properties gain their Tapir sub-schemas (they are `services/metrics/contract`'s DTOs and their schemas are derivable), `./mill services.gateway.api.openApi` is re-run, `pnpm --filter @kui/api run generate` produces named types for them, and the seven hand-written interfaces in `metrics.ts` are deleted in favour of `components["schemas"][…]`. Doing it for `Section[A]` in general is the larger fix and would close all 25; doing it for the five metrics payloads is the one this row was opened for, because they are the five with no other generated description anywhere. **Re-read on 2026-09-11 in wave 9 and left open, with the reason stated rather than the row restated:** the figure is unchanged at 25 named properties out of 177 `: unknown;` lines, 152 of which are the index signature, and nothing in wave 9 touches `Section[A]`, `services/metrics/contract` or the generator. The exit is a contract change in a module no packet of the closing wave owns, so closing it would mean editing a tree with no owner to make a number in this file move — which is the failure mode the register exists to record, not to commit | Chief Architect (contract), Domain Architect (metrics) | 2026-09-07 | open |
| TD-025 | frontend | **A live product defect, found by wave 6's third adversarial pass and repaired in wave 7 — recorded here because it was carried across a wave boundary in a wave plan, which is the one thing this register exists to stop.** A ⌘-, ctrl- or shift-click on a link *inside* a clickable table row did two things at once: the browser opened the link in a background tab or window, because that is what the gesture means, and `onRowClick` navigated the tab the operator was still looking at. They lost the list they were working through and the thing they asked for was behind a tab they then had to find. It was in `DataTable` and in `VirtualizedTable`, which draw the same `<tr class="kui-table__row">` and are chosen by how many rows a screen expects rather than by how a row behaves, so every list screen in the product had it | wave 6 verification, 2026-09-07; not repaired in wave 6 | The adversary that found it owned no test tree it could land a case in — house rule 13 gave a verifier the tree of the packet it verifies and that packet was still the live owner, so 46 findings produced 46 paragraphs and zero cases. That is the mechanism failure TD-023 records; this row is one of its two user-visible consequences | closed by `frontend/packages/kernel/src/components/rowActivation.ts`, one function rather than two copies, consulted by both tables; gated by `surfaces.test.tsx`'s *row activation and the modifier keys* cases, which drive `⌘`, `ctrl` and `shift` through a real `MouseEvent` `init` because `userEvent.click` has no way to express a modifier | Frontend Architect | 2026-09-07 | **closed 2026-09-10 (W7-05)** |
| TD-026 | frontend | **The second live product defect wave 6 found and did not repair.** ADR-047 gives a cluster a `readOnly` flag and it reached none of the topic screens: all seven of `TopicsRoute`'s `writeBlockedReason` calls passed a hard-coded `readOnly: false`, so a cluster somebody had deliberately registered read-only offered a live `Create topic`, a live bulk `Delete` and a live `Empty`, and the refusal arrived from the server *after* the operator had read a consequence and typed the word `delete`. The permission half of the same gate was closed in wave 5; this half was wired to a constant on the screen being audited, which is why an audit of the permission half passed over it | wave 6 verification, 2026-09-07; not repaired in wave 6 | Same cause as TD-025: found by a pass with no tree to land a case in. Recorded separately because the two have different owners and a register that folds two defects into one row cannot be read to find out whether both were fixed | closed by reading the cluster's own flag at all seven call sites; gated by `feature-topics`'s *a read-only cluster refuses every write, and blames the deployment*, whose principal deliberately holds every permission so that only the cluster flag can refuse the controls | Frontend Architect | 2026-09-07 | **closed 2026-09-10 (W7-08)** |
| TD-027 | build | **The scalafix gate covers no test source anywhere in this repository.** `./mill __.fix --check` is treated by this project as a repository-wide gate — it is the command the wave plans, the CI workflow and every packet's acceptance list name — and `fix` is defined on `KuiModule` only. Mill's test modules are `ScalaTests with KuiTests`, and `KuiTests` extends `TestModule.Munit` and nothing else, so no test module has the task at all: `./mill resolve libs.kafka.test.fix` answers *Cannot resolve*, and `./mill resolve __.fix` resolves **84** targets of which not one is a test module (`build-tests.fix` and `libs.testkit.jvm.fix` are production modules whose names contain the word). Measured on 2026-09-11, and both figures in this sentence were re-counted when the row was closed and were wrong: it is **81 test modules and 499 `.scala` files under a `test/` directory**, not 83 and 485, and they were outside the gate against a green `__.fix --check` over 5353 sources. This is not a style question. `.scalafix.conf` carries `DisableSyntax`, `RemoveUnused` and the ordering rules this codebase's reviews are written against, and four packets in wave 7 landed most of their work in test trees, which is where an adversarial pass lands all of its work by construction. A gate whose name says `__` and whose reach is half the tree is worse than no gate, because every acceptance list in the plan reads as though it ran. | wave 8 integration, 2026-09-11 | Nothing chose this; `mill-scalafix` attaches `fix` to `ScalafixModule` and `KuiTests` was written from `TestModule.Munit` alone, so the omission is what happens when a trait is assembled rather than decided. It is recorded rather than repaired in this packet because the repair changes what every module in the build compiles — semanticdb has to be produced for test sources too — and W8-09 owns no build file | **Owned by W9-05, which runs last in wave 9 for the reason this row is hard to close: a formatter run over the test trees touches files five other packets are writing.** `KuiTests` (or a `KuiTestModule` beside it) mixes in `ScalafixModule` and points `scalafixConfig` at the same `.scalafix.conf` the production trait uses, `semanticdbEnabled` holds for test compilation, `./mill resolve __.fix` then resolves a target per test module, and `./mill __.fix --check` is re-run and its source count published. The exit is that figure moving from 5353 to a number that includes the test files, with the run green, and `./mill resolve '__.fix' | grep -c '\.test\.fix'` moving from 0 | Principal Scala Engineer (W9-05) | 2026-09-11 | **closed 2026-09-11 (W9-05), on the figures the exit condition named, re-measured here one gate at a time rather than read off a report.** `./mill resolve '__.fix' \| grep -c '\.test\.fix'` answers **81** where it answered 0, and `./mill resolve '__.fix'` resolves **163** targets in all. `./mill __.checkFormat` is **495/495 over 1,145 Scala sources across 162 reporting targets**, up from 252 over 646, and the difference is every test tree in the repository. `./mill __.fix --check` is **10672/10672** over the same 1,145 sources, up from 5353. `KuiTests` mixes in `ScalafmtModule` and `ScalafixModule` and points at `.scalafix-tests.conf`. **What it cost is the part worth keeping: 380 of the 499 test sources had to be reformatted the first time a formatter reached them**, including 12 of the 13 test sources wave 9 itself wrote before the gate landed — six waves of asking authors to hold a column rule by hand produced one correct file in thirteen. The first widened `fix --check` reported **126 `DisableSyntax` violations across 57 files**, and every one inspected was load-bearing rather than fixable, so `.scalafix-tests.conf` relaxes four sub-rules for `test/src` only, each with its measured site count beside it: `noNulls` **48** sites, `noThrows` **47**, `noAsInstanceOf` **30**, `noReturns` **1**. The other three `DisableSyntax` rules fired **zero** times over the same 499 files and stay on. X, and it is carried as TD-035 rather than left inside this closed row |
| TD-028 | identity | **A hand-written constant-time comparison where the JDK ships one.** `Pbkdf2PasswordHasher.constantTimeEquals` (`services/identity/infrastructure/src/kui/identity/infrastructure/Pbkdf2PasswordHasher.scala:100`) folds an XOR over `math.max(left.length, right.length)` bytes and compares the accumulator with zero. It is *correct today* — that is the difficulty. A branch-free loop is a property of the bytecode the JIT actually emits, not of the Scala, and no unit case can assert it: a timing test on a shared runner measures the runner. So the rule is ungated and will stay ungated, and the only honest defence is not to hand-write it | wave 8 adversarial pass, 2026-09-11 (filed, not repaired: the file is owned by no packet of wave 9) | The loop was written before anybody noticed the JDK's own answer, and four other call sites in this repository already use that answer: `PurgeToken.verify` (`services/message/application/.../purge/PurgeToken.scala:99`), `PlanToken`, `TopicPlanToken` and `KsqlPlanToken` all compare MACs with `java.security.MessageDigest.isEqual`, which the JDK documents as time-constant. One primitive, five call sites, four of them already right | **Delete the loop** and call `java.security.MessageDigest.isEqual(derived, expected)`. The exit is that `constantTimeEquals` no longer exists and `Pbkdf2PasswordHasherSuite` still passes unchanged — the existing equality cases are the whole gate this change needs, because the behaviour does not move and only the timing guarantee does | Security Engineer | 2026-09-11 | open |
| TD-029 | filter | **`CelFilterEngine.warm` is `private` and swallows its own failure, so nothing can ask whether the warm-up ran.** `libs/filter/src/kui/filter/CelFilterEngine.scala:152` compiles and evaluates `record.partition == 0` at resource acquisition and ends with `.handleError(_ => ())`, whose comment correctly says a failed warm-up must not refuse to start. The consequence is that the trial compile can fail on every start for a whole release and the only symptom is the slow first filter the warm-up exists to prevent | wave 8 adversarial pass, 2026-09-11 (filed, not repaired: `libs/filter` is owned by no packet of wave 9) | Swallowing the error is right; being unobservable is the part nobody chose. The method is `private` and returns `F[Unit]`, so there is no value for a case to read and no name for a case to call | Widen it to `private[filter]` and return whether the trial compile-and-evaluate completed — `F[Boolean]`, or a small ADT if the two failure modes are worth telling apart — keeping `handleError` so that start-up still cannot fail. The exit is a `CelFilterEngineSuite` case that drives `warm` over a compiler that refuses and asserts the answer, which is impossible to write today | Domain Architect (messages) | 2026-09-11 | open |
| TD-030 | kafka-auth | **`KeyStoreMaterializer` re-applies `700` to the key-store directory and no unit case can gate it honestly.** `libs/kafka-auth/src/kui/kafka/auth/KeyStoreMaterializer.scala:159` creates the directory with `DirectoryPermissions` and then sets the permissions again explicitly, because `createDirectories` on a path whose parent already exists can leave the leaf with the umask applied. The second call is the whole defence and it is invisible: the suite runs under the test runner's umask, where the first call already produces the right mode, so deleting the re-apply leaves every case green | wave 8 adversarial pass, 2026-09-11 (filed, not repaired: `libs/kafka-auth` is owned by no packet of wave 9) | This is not a missing assertion, it is a missing *environment*. A case that could see the difference has to run the materializer under a hostile umask (`0077` is not hostile; `0022` with a pre-existing parent is), and a JVM cannot change its own umask. That makes it a deployment check rather than a seam, which is a decision somebody has to take rather than a line somebody has to write | An integration check that runs `KeyStoreMaterializer` in a container started with a permissive umask and asserts the directory mode afterwards — `deployment/compose` or the CI job, not `libs/kafka-auth/test`. The exit is that deleting line 162's re-apply turns that check red. **If the decision is that the check is not worth a container, the honest close is to say so in this row and in the file's comment**, so the next reader knows the line is deliberately ungated | Infrastructure Lead | 2026-09-11 | open |
| TD-031 | alerts | **`InMemoryAlertStore.MaxReadMarkers` has no behavioural upper bound.** `services/alerts/infrastructure/src/kui/alerts/infrastructure/InMemoryAlertStore.scala:189` pins the read-marker cache at 2000 entries and hands the figure to `BoundedCache.make`. `InMemoryAlertStoreSuite` writes the literal `2000` rather than the constant — deliberately, so that the constant is not compared with itself — but it asserts that a marker written first is still readable, which is a statement about the cache *not* evicting. **Nothing asserts that it ever does.** Raising `MaxReadMarkers` to `Long.MaxValue` leaves every case in the module green, and the bound is the only thing standing between one principal per request and unbounded memory | wave 8 adversarial pass, 2026-09-11 (filed, not repaired: `services/alerts` is owned by no packet of wave 9) | A cache's eviction is observable only through a stat or through a write that fails, and `BoundedCache` exposes neither to this store today. The case that would close it therefore needs a seam and not an assertion, which is why the adversary filed it instead of landing it | Expose the marker cache's `stats` (or `estimatedSize`) from `InMemoryAlertStore`, then assert `estimatedSize <= MaxReadMarkers * slack` after writing markers for more principals than the bound. The exit is that raising `MaxReadMarkers` turns that case red where today it turns nothing red | Chief Architect | 2026-09-11 | open |
| TD-032 | consumer | **`GroupListView.stateCounts` and `GroupListView.notes` reach no response.** `services/consumer/application/src/kui/consumer/application/GroupListUseCase.scala:86` and `:88` declare both fields, `:133` and `:134` fill them from the coordinator's own summaries and from `GroupQuery.normalise`, and no DTO mapping reads either one, so no browser has ever seen a state count or a normalisation note. Two fields computed on every group list for nobody | wave 8 adversarial pass, 2026-09-11 (filed, not repaired: `services/consumer` is owned by no packet of wave 9) | The same shape as every *code-only* row in `docs/FEATURE_MATRIX.md`: built, unit-tested, green and wired into nothing. It was found by mutation — both fields can be set to `Map.empty` and `Nil` with the whole service's suite green — and a field that no assertion outside its own constructor reads is indistinguishable from a field that is not there | **Decide, and the decision is binary: render them on the list document or delete the two fields.** `notes` is the more valuable half — a list silently reduced to a bounded page size is exactly what a note is for — so the likely answer is to carry it into `GroupListResponse`. The exit is either a contract change with a browser assertion on the drawn note, or two deletions and a suite that no longer compiles against them | Domain Architect (messages) | 2026-09-11 | open |
| TD-033 | schema | **A provably dead `.filter` that a reader has to solve arithmetic to recognise.** `services/schema/infrastructure/src/kui/schema/infrastructure/RegistryCredentials.scala:180` reads `.filter(_ > MinimumLifetime).getOrElse(MinimumLifetime)`, and two lines below the expiry is `(lifetime - RefreshMargin).max(MinimumLifetime / 2)`. With `RefreshMargin = 30s` and `MinimumLifetime = 1.minute`, the clamp answers the same instant for every input with or without the filter: any `expires_in` above a minute is unchanged by the filter, and anything at or below it lands on `MinimumLifetime / 2` either way. **Re-derived by hand in wave 9 rather than transcribed** — the filter is dead, and deleting it changes no observable behaviour | wave 8 adversarial pass, 2026-09-11 (filed, not repaired: `services/schema` is owned by no packet of wave 9) | Two guards were written for one danger at different times and neither author saw the other. That is not a bug and it is not free: one of the two is decoration, a reader has to do the algebra to find out which, and a mutation test on either one stays green — which is how it was found | **Delete the `.filter` and say in the scaladoc that the clamp is the floor**, or keep the filter and delete the `.max`. The exit is one guard, a comment naming the arithmetic above, and a `RegistryCredentialsSuite` case at `expires_in = 10` seconds and at `expires_in = 3600` that pins the expiry instant — so the next person to add a third guard finds a case rather than a puzzle | Security Engineer | 2026-09-11 | open |
| TD-034 | connect | **`ConnectorFacts.complete` has eight test call sites in seven files and no production caller.** `services/connect/domain/src/kui/connect/domain/Connectors.scala:197` builds a `ConnectorFacts` with an empty failure list, and every caller in the repository is a test fixture. It is a constructor the suites find convenient and the product never reaches. **The arithmetic in this row was wrong when it was written and is corrected here rather than restated:** it said *seven test callers* and *the seven suites that call it*; `grep -rn "ConnectorFacts.complete" --include='*.scala'` answers **eight call sites across seven files**, and two of those seven — `ConnectRig.scala` and `ConnectTestServer.scala` — are rigs rather than suites, so *seven suites* over-counts in the other direction too. The row's substance, that there is no production caller, re-checked and holds | wave 8 adversarial pass, 2026-09-11 (filed, not repaired: `services/connect` is owned by no packet of wave 9) | **This one is deliberately not called an ungated rule, and the distinction matters.** There is no behaviour here for a case to pin: any mutation of `complete` is caught by the five suites and two rigs that reach it, and no mutation of it can reach a user, because nothing in production does. It is a hygiene decision for whoever next owns the file, recorded so that "unowned" is a decision and not an oversight | Either keep it and say in its scaladoc that it is a test constructor, or move it into the connect test tree. The exit is that a reader of `Connectors.scala` can tell which of the two it is without running `grep`. **No case closes this row**, which is why it carries an owner rather than a suite name | Kafka Specialist | 2026-09-11 | open |
| TD-035 | build | **Two exemptions the widened style gate had to be given, both created by TD-027 and neither one a style preference.** (a) `services/gateway/app/test/.../GatewayWiringSuite.scala:97` carries **the repository's only `// format: off`**. `new Thread(() => try … catch { … }, "name")` — a braceless `try` as a *non-final* argument — parses under scalafix as the author wrote it, does **not** parse after scalafmt has rewritten it (`` `outdent` expected but `,` found ``), and scalafmt is **not idempotent** on it: pass 1 dangles the argument and pass 2 packs it onto the catch's closing brace. `scalac` compiles both. (b) `.scalafix-tests.conf` relaxes `noReturns` for one site, `build-tests`' `ContrastSuite`, which scans a stylesheet for a matching brace with a `while` loop and returns the index from inside it | TD-027's close, 2026-09-11 | Invisible for eight waves because no production source writes a braceless `try` as a non-final argument, and because no test source was inside either gate. Both were found the first time a formatter and a linter were pointed at the same test tree, and both were exempted rather than rewritten because rewriting (a) is a scalafmt bug report and rewriting (b) deletes a legible loop to satisfy a rule aimed at production control flow | (a) rewrite the thread body as a named `private def` so no `try` is an argument at all, delete the `// format: off`, and confirm `./mill services.gateway.app.test.checkFormat` and `.fix --check` are both green twice in a row — the second run is the point, because non-idempotence is what this is. (b) rewrite `ContrastSuite`'s scan as a recursive helper or an `indexWhere`, then set `noReturns = true` in `.scalafix-tests.conf` and watch the gate stay green. Each half is independently closeable and neither blocks the other | Principal Scala Engineer | 2026-09-11 | open |
| TD-036 | build | **`build.mill` and `mill-build/build.mill` are 4,374 lines of Scala outside both style gates.** `./mill __.checkFormat` and `./mill __.fix --check` reach 1,145 sources across 162 reporting targets and neither reaches the build file itself, because a Mill build script is not a module of the build it defines. It is the largest single Scala file in the repository by a wide margin — 4,357 lines — and it is the file that decides what every other file is compiled and linted with | wave-9 close, 2026-09-11, when TD-027 moved the gate from 646 sources to 1,145 and this was what was left outside | Nothing chose it. `checkFormat` and `fix` are tasks on modules, and the build script is the thing that declares modules; scalafmt can format it (`scalafmt --config .scalafmt.conf build.mill` is an ordinary invocation) but no Mill task does. It is recorded rather than repaired because the repair is a new build-level task with its own failure modes, and because a reformatting of 4,374 lines is a diff no reviewer reads | a task — `checkBuildFormat`, beside `checkArchitecture` — runs scalafmt in `--test` mode over `build.mill` and `mill-build/build.mill` and fails on a difference, and CI's `style` job calls it. The exit is that a deliberately misformatted line in `build.mill` turns that job red, run in both directions | Principal Scala Engineer | 2026-09-11 | open |
| TD-037 | deployment | **`./deployment/compose/smoke.sh` passes over a stack whose entire topics product answers 401.** Measured in wave 9 in both directions: change `KUI_PRINCIPAL_KEY` on `kui-topic` in `deployment/compose/docker-compose.yml` and the script prints `PASSED` in 107 seconds with *"topic capability: available"* twice, while `GET /api/v1/clusters/measured/topics` answers **HTTP 401 `KUI-UNAUTHENTICATED`** and `/consumer-groups` answers 200 beside it. Six of the nine routed services are covered only by a capability probe, which is a health call carrying no signed principal — so it proves the container is up and proves nothing about whether the gateway can talk to it | wave-9 verification, 2026-09-11 | The probe was the cheapest thing that could be written when the script only had to prove containers were running, and every service added since has inherited it. The machinery for the stronger check exists and is used exactly once, for Connect: a read through the gateway at a published path, asserted for a 2xx | a `for` over the contract set asks the gateway for one published read path per service and requires a 2xx from each. The exit is that re-applying the wrong `KUI_PRINCIPAL_KEY` on `kui-topic` turns the script red where it prints `PASSED` today — run in both directions, because a loop that asserts nothing passes both ways. **Owned by W10-05 in wave 10**; this row is here so that it outlives the wave plan | Infrastructure Lead | 2026-09-11 | open |
| TD-038 | security | **A masking rule that loads and looks correct returns a sixteen-digit card number in full.** `keep: {prefix: 20, suffix: 20}` passes validation, because `MaxKeep` is enforced **per end**: two individually legal ends together cover the whole field, `maskKeepingEnds`' `if maskedCount <= 0 then text` hands the payload back unchanged, and the cap's own stated reason — *"suffix: 44 on a card number returns it unmasked"* — is defeated by writing 20 twice. Verified empirically in wave 9 with a throw-away suite against `4111111111111111` | wave-9 verification, 2026-09-11 | The cap was written against one end and the rule has two. It is the shape this register exists for: a guard that is correct for every input its author had in mind and silent for the one that matters, in the one feature whose whole purpose is not showing somebody a card number | either refuse `keepPrefix + keepSuffix > MaxKeep` at load, so a configuration that used to load stops loading and says why, or make the engine mask the whole field when the ends leave nothing — which is the fail-safe direction and matches `maskPayload`'s own stated rule that masking too much is recoverable and masking nothing is not. The exit is a `MaskingConfigSuite` case, *a keep whose two ends together reveal the field is refused*, that goes red when the repair is reverted. **If the answer is the refusal, a deployment that relied on the old behaviour stops booting, and the honest close carries a migration note rather than a silent change.** Owned by W10-04 in wave 10 | Security Engineer | 2026-09-11 | **closed 2026-09-11 (W10-04), and closed in both directions rather than in one.** `MaskingConfig` refuses `keep.prefix + keep.suffix > MaxKeep` at load with a message that says the ends are each legal on their own — the sentence an operator needs, because the whole defect is that the file reads as valid — and `MaskingRule`/`MaskingEngine` additionally mask the **whole** value when the ends leave nothing to mask, which is the fail-safe half the loader cannot see for a field shorter than the two ends together. Both halves are pinned: `MaskingConfigSuite`'s *a keep whose two ends together reveal the field is refused* and *a keep bounded at both ends still hides a field shorter than the two ends together*, and `MaskingEngineSuite`'s *two legal ends that overlap do not hand back the card number they were written to hide*. **Recorded as debt and closed in the same wave on purpose:** the register's rule is that debt is recorded when it is taken, and this was taken in wave 9 and paid in wave 10 — a row that was never written is a row the next wave rediscovers |
| TD-039 | docs | **`ARCHITECTURE.md`'s *Key aggregates / value objects* column is read by nothing, and nine of the names in it resolve to no `.scala` declaration anywhere in this repository.** Found by chasing one of them: `MaskingPolicy` stood in the `message` row and `grep -rn MaskingPolicy --include='*.scala'` finds it **zero** times. Measured on 2026-09-11 by filtering that column to the tokens that look like Scala identifiers and asking `grep -rE '(class\|trait\|object\|enum\|type) +<Name>' services/<service>` for each: `Topic`, `TopicAnalysis`, `PollingMode`, `Member`, `MetricSnapshot`, `GraphDescription`, `PromQuery`, `AuditRecord` and `MaskingPolicy` resolve nowhere at all, and several more resolve outside the service whose row names them (`SeekMode` and `OffsetRange` are `libs/kernel`'s; `Principal`, `Role` and `Permission` are `libs/security-core`'s). `ArchitectureDocumentSuite` walks `services/<n>/domain/src` and compares the *service roster* sentence beneath the table; it does not read the table's cells in either direction | wave-10 documents pass, 2026-09-11 | The suite was written to close the failure that had actually happened — a roster count wrong for two waves — and the column beside it was never in scope. The correction of `MaskingPolicy` landed in wave 10; the other eight did not, because closing them needs a decision this row cannot take: **whether that column names Scala types or names concepts.** If it names types, five rows are rewritten; if it names concepts, the gate is a different one and the nine names are correct as prose. Landing a check first would be deciding that question by accident | the decision is taken and written into `ARCHITECTURE.md` beside the table, and then `ArchitectureDocumentSuite` grows the matching check — for the type reading, every identifier in the *Domain types* column resolves to a declaration under that service; for the concept reading, every **port** in the third column does, which is the half that is already almost true. The exit is that re-introducing `MaskingPolicy` into the `message` row turns that suite red. The suite is in `services/gateway/api/test/`, which the packet that measured this does not own — the seam, per house rule 22 | Chief Architect | 2026-09-11 | open |
| TD-040 | ksql | **`KsqlObjects.of` bounds `items` at 500 and leaves `unreadable` unbounded.** `services/ksql/domain/src/kui/ksql/domain/KsqlObjects.scala:160` takes `sorted.take(MaxObjects)` and records `truncated`, and passes `unreadable` straight through. A ksqlDB answering 10 000 rows KUI cannot describe therefore produces a 10 000-entry list in one contract document and a banner on the screen naming every one of them — the bound the row list has, on the list nobody looked at | wave-9 adversarial pass, 2026-09-11 (filed, not repaired) | It is a **missing** rule rather than a deleted one, which is why no mutation found it and why it is filed rather than closed: there is no assertion to break. The scaladoc beside `MaxObjects` argues the bound carefully for `items` and does not mention the other list, so the omission reads as deliberate and is not | bound `unreadable` the way `items` is bounded and report the cut the same way — either a second `truncated` figure or one shared count with the two kinds named — and add a `KsqlObjectsSuite` case, *an answer whose undescribable half is past the bound is cut and says how much it cut*, mirroring the one that already holds `items`. The exit is that removing the new bound turns that case red | Domain Architect (ksql) | 2026-09-11 | open |
| TD-041 | frontend | **Two functions called `formatBytes`, with one meaning, in a workspace whose kernel already exports one.** `frontend/packages/feature-topics/src/TopicListPage.tsx:771` exports a second `formatBytes(bytes: number): string` beside `frontend/packages/kernel/src/components/record.ts:156`, which is imported at call sites across four packages. The two can drift, and the cost of drifting is a byte figure that reads one way on the topics list and another way on the broker card beside it — which is a defect a reader attributes to the data | wave-10 screens pass, 2026-09-11 (filed by the packet that owns the kernel half and not the feature half) | Recorded rather than repaired because `feature-topics` belonged to no packet in the wave that found it, and because deleting an export is a change to a package's public surface rather than a chore. It is named here so that *unowned* is a decision | delete the `feature-topics` copy, import the kernel's, and confirm `pnpm -C frontend test packages/feature-topics` and `pnpm -C frontend typecheck` are both green. The exit is that `grep -rn 'function formatBytes' frontend/packages/*/src` answers one line | Frontend Architect | 2026-09-11 | open |
| TD-042 | frontend | **`frontend/packages/shell/src/chrome/ClusterSelector.tsx` is 214 lines, is exported from `shell/src/index.ts`, has stories and cases, and has zero production callers.** `[data-testid="cluster-selector-trigger"]` resolves to zero elements on every screen of a running stack. The shell chooses a cluster elsewhere — `Overview.tsx`'s own comment calls this *"the shell's other cluster chooser"* — so the component is tested, documented, code-split and unreachable | wave-10 documents pass, 2026-09-11 | The same **code-only** shape `docs/FEATURE_MATRIX.md` names for backend components, in the frontend, where nothing counts it. Deleting it is a decision and not a chore: it is a public export of the shell package, its suites and stories pass, and the a11y sweep counts its stories — so removing it moves three published figures at once and has to be done deliberately | either delete the component, its export, its stories and its cases together and re-measure the story count the a11y sweep publishes, or give it the caller it was written for and assert that caller in `frontend/e2e/shell.spec.ts`. The exit is that `[data-testid="cluster-selector-trigger"]` either resolves on a screen of a running stack or does not exist in the tree | Frontend Architect | 2026-09-11 | open |
