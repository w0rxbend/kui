# W8-02 — verification report

**Packet.** W8-02 — three services that shipped ungated: eight rules across `connect`, `alerts` and
`metrics`.
**Verified against.** `de70b99` (`feat(connect): the tenth service, the relay the ninth never got, and
an empty list`), working tree as of 2026-09-11.
**Verdict.** Confirmed. The owned rule is genuinely closed — I re-applied the plan's exact mutation
myself and it reddened the named case. All four acceptance commands reproduce the reported output. No
ownership violation: `git diff --name-only de70b99` restricted to this packet's work is exactly the
eleven files it declared, all inside its `Owns`. Four rules in these three services still survive
mutation with the suite green; **three of the four are not in the packet's own `knownUngated`.**

Every mutation below was applied to the **source**, run, and reverted from a byte copy taken before
it. `git diff --stat de70b99 -- services/*/…/src` is back to one file (`Connectors.scala`, the
packet's own three deletions). No git write command was run.

---

## 1. Acceptance — re-run, verbatim

| Command | Result | Reported |
| --- | --- | --- |
| `./mill --no-daemon services.connect.__.test + services.alerts.__.test + services.metrics.__.test` | `2821/2821, SUCCESS … 31s`, exit 0 | 2821/2821 SUCCESS ✔ |
| `./mill --no-daemon services.connect.__.checkFormat + …alerts… + …metrics…` | `60/60, SUCCESS … 8s`, exit 0 | 60/60 SUCCESS ✔ |
| `./mill --no-daemon services.connect.api.openApiCheck + …alerts… + …metrics…` | `1032/1032, SUCCESS … 6s`, exit 0 | 1032/1032 SUCCESS ✔ |
| `./mill --no-daemon checkArchitecture` | `checkArchitecture: 195 modules, 10 rules, no layering violations`, `233/233, SUCCESS … 3s`, exit 0 | 195 modules, 10 rules ✔ |

Counted independently from a run that actually executed (the M1 mutation run, which invalidated the
cache): **connect = 138 cases over 13 suites, 0 failed** — the packet's figure exactly. The per-suite
growth it claims is also exact, counted as `test(` at `de70b99` vs now: `InMemoryAlertStoreSuite`
16→18, `PrometheusExpositionSuite` 27→29, `ConnectHttpSuite` 18→21, `ConnectCapabilitiesSuite` 10→11,
`ConnectWiringSuite` 10→13 = **11 new cases**, `ConnectorsSuite` 11→11 (rewritten, not added to).

---

## 2. The owned rule — re-applied by me

| Field | Value |
| --- | --- |
| File | `services/alerts/infrastructure/src/kui/alerts/infrastructure/InMemoryAlertStore.scala:148` |
| Mutation | `.whenA(evaluation.opened.nonEmpty \|\| evaluation.resolved.nonEmpty)` → `.whenA(evaluation.opened.nonEmpty)` |
| Command | `./mill --no-daemon services.alerts.infrastructure.test` |
| Result | **RED**, exit 1. `752/752, 1 FAILED`. `==> X kui.alerts.infrastructure.InMemoryAlertStoreSuite.a pass that only resolves an event publishes a frame, so a cleared bell clears itself` — `munit.ComparisonFailException: InMemoryAlertStoreSuite.scala:296`, `=> Obtained Nil`, clue *"a pass that only resolved an event woke no subscriber"* |
| Reverted | Yes, from the byte copy. `git diff --stat de70b99 -- services/alerts/infrastructure/src` is empty. |

`ownedRuleGenuinelyClosed = true`. The assertion is on the product's own `changes` stream, the event is
seeded before the subscription opens, and `framesFor` returns an exact frame list rather than a delta
across a sleep — so the case fails on a count, not a timeout.

---

## 3. FINDINGS — one row per finding

Each row: the file, the exact mutation I applied, the suite command, and the shape of the case that
would close it.

### F1 — `ConnectHttp.expanded`: the `unreadable` list's order is ungated (**packet disclosed this**)

| | |
| --- | --- |
| File | `services/connect/infrastructure/src/kui/connect/infrastructure/ConnectHttp.scala:126` |
| Mutation | `unreadable = read.collect { case Left(name) => name }.sorted` → `… .reverse` |
| Command | `./mill --no-daemon services.connect.__.test` |
| Result | **GREEN** — `1186/1186, SUCCESS`, 138 cases, 0 failed |
| Promise it breaks | The *"KUI could not describe it"* row on the connectors screen reshuffles between polls, for the same reason the packet closed the three orderings beside it. Worse, `ConnectorFacts`' own scaladoc says `unreadable` is *"in the order it named them"* — which **neither** reader does: `expanded` sorts it and `perConnector` emits it in `names.sorted` order. The comment and the code disagree in both directions. |
| Shape of the case that closes it | In `ConnectHttpSuite`, a fixture whose **unreadable** names are three connectors whose document order, sorted order and reversed order are three different lists (the `expanded` fixture already has that property for readable connectors — give three of them broken `status` documents instead), then compare two positions: `assert(names.indexOf("archive-sink") < names.indexOf("elastic-sink"))` plus `assertEquals(names, names.sorted)`. Add the same two positions to `perConnector`'s roster case. **And settle the scaladoc in the same edit** — either it says "sorted" or the code stops sorting; today it says neither truthfully. |
| Disclosed by packet | Yes (`knownUngated[0]`, with the same measurement). |

### F2 — `Connector.reason` names a task that did not fail (**NOT disclosed**)

| | |
| --- | --- |
| File | `services/connect/domain/src/kui/connect/domain/Connectors.scala:171` |
| Mutation | `.orElse(tasks.sortBy(_.id.value).filter(_.state.isFailed).flatMap(_.reason).headOption)` → `.orElse(tasks.sortBy(_.id.value).flatMap(_.reason).headOption)` (the `.filter(_.state.isFailed)` deleted) |
| Command | `./mill --no-daemon services.connect.__.test` |
| Result | **GREEN** — `1186/1186, SUCCESS`, 138 cases, 0 failed |
| Promise it breaks | `reason`'s own scaladoc: *"Otherwise the **first failed task's** reason, in task-id order"*, and `ConnectorTask.reason`'s: *"a task with no trace is not a task with no problem … never a reason invented"*. Under the mutation a connector whose tasks are all healthy but one of which still carries a stale `trace` — a task that failed and was restarted keeps its trace in several Connect releases — draws that trace as the card's failure sentence under `ConnectorMappings`' `reason` field (`ConnectMapping.scala:91`). An operator is shown a resolved failure as a live one. This is the *same line* the packet mutated for its owned rule 4: it fixed the `sortBy` half of the expression and left the `filter` half with no case at all. |
| Shape of the case that closes it | In `ConnectorsSuite`, beside the id-order case: a connector with `trace = None` whose tasks are `[task(1, "RUNNING", Some("stale trace from a restart")), task(2, "FAILED", Some("task 2 said"))]`, asserting `assertEquals(connector.reason, Some("task 2 said"))`. Task 1 sorts first, so the assertion fails the moment the `isFailed` filter is dropped, and it is independent of the ordering case beside it. |
| Disclosed by packet | **No.** |

### F3 — `PrometheusExposition.ratioOf` accepts a non-rate line as the one-minute rate (**NOT disclosed**)

| | |
| --- | --- |
| File | `services/metrics/infrastructure/src/kui/metrics/infrastructure/PrometheusExposition.scala:243` |
| Mutation | `(!rated \|\| sample.name.toLowerCase.endsWith(RateSuffix)) &&` → `(!rated \|\| sample.name.toLowerCase.nonEmpty) &&` |
| Command | `./mill --no-daemon services.metrics.__.test` |
| Result | **GREEN** — `1237/1237, SUCCESS` |
| Promise it breaks | The *other half of the same `find` block* the packet closed. `ratioOf` is called with `rated = true` for both saturation gauges, and the `rated` flag exists because a JMX exporter publishes several views of one meter — `…avgidlepercent_count`, `…_total`, `…_oneminuterate`. With the suffix requirement relaxed, whichever view the exporter happened to print first becomes the gauge: a cumulative `_count` drawn on a 0–1 ring reads as a permanently saturated broker, or a permanently idle one. The packet closed `dimensionsOf(sample).isEmpty` on line 245 and left line 243 — one line above, inside the same predicate — untested. |
| Note | The naïve mutation (`(!rated \|\| …)` → `true`) **does not compile**: `-Werror` plus `-Wunused:all` turns the then-unused `rated` parameter into an error. That is why the mutation above keeps `rated` referenced; the packet hit the same compiler behaviour elsewhere and reported it. |
| Shape of the case that closes it | In `PrometheusExpositionSuite`, next to *"an idle ratio carrying a dimension is a slice too"*, a fixture that writes the **non-rate views first** (`kafka_server_kafkarequesthandlerpool_requesthandleravgidlepercent_count 88123` and `…_oneminuterate 0.8912`, likewise for the network processor) and asserts `requestHandlerIdleRatio == Some(0.8912)`. Order matters for the same reason the packet's own case says: `ratioOf` takes the first match, so a fixture whose `_oneminuterate` came first would be green under any guard. A second case in the refusal direction: a body carrying **only** `…_count` lines yields `None` for both ratios. |
| Disclosed by packet | **No.** |

### F4 — `PrometheusExposition.purgatoryOf`'s queue order is ungated (**NOT disclosed**)

| | |
| --- | --- |
| File | `services/metrics/infrastructure/src/kui/metrics/infrastructure/PrometheusExposition.scala:265` |
| Mutation | `.sortBy(_.operation)` deleted (queues left in exposition document order) |
| Command | `./mill --no-daemon services.metrics.__.test` |
| Result | **GREEN** — `1237/1237, SUCCESS` |
| Promise it breaks | The method's own scaladoc, one line above the mutation: *"Sorted rather than left in document order, so that two scrapes of the same broker put the same queue in the same place on a card."* This is precisely the rule the packet spent its item 6 closing three copies of in `connect`, in the *same* service pair it was auditing, and the fourth copy one file over went unmeasured. A JMX exporter's MBean iteration order is not stable across scrapes, so the purgatory rows on the brokers screen reshuffle under a reader between polls. |
| Shape of the case that closes it | In `PrometheusExpositionSuite`, a body whose `delayedoperationpurgatory` lines name three operations in an order that is neither sorted nor its reverse — e.g. `Fetch`, `Produce`, `DeleteRecords` (sorted: `DeleteRecords, Fetch, Produce`; reversed: `DeleteRecords, Produce, Fetch`) — then compare two positions rather than assert the whole list: `assert(ops.indexOf("DeleteRecords") < ops.indexOf("Fetch"))` and `assertEquals(ops, ops.sorted)`. Three entries are required; with two, `reverse` and `sortBy` can agree, which is the defect the packet found in `ConnectorsSuite`'s `[2, 1]` fixture. |
| Disclosed by packet | **No.** |

### F5 — The three new ADR figures are prose no gate reads (**packet disclosed; the repo has the mechanism it did not use**)

| | |
| --- | --- |
| Files | `docs/adr/ADR-052-metrics-endpoints.md:309-313`, `ADR-053-alert-events.md:251-259`, `ADR-054-connect-endpoints.md:280-290` |
| Mutation | None applied — verified by inspection: `grep -n 'checked:' docs/adr/ADR-05{2,3,4}*.md` returns nothing, and `scripts/feature-matrix-check.sh`'s `required` list names `ADR-048` and `frontend/packages/api/README.md` only. |
| Command | `bash scripts/feature-matrix-check.sh` — it reads none of these blocks. |
| Every figure re-measured here, and every one is **correct** | `jq '[(.paths\|length), ([.paths[]\|keys[]]\|length), (.components.schemas\|length)]' docs/api/openapi.json` → `[61, 72, 156]` ✔; the same over `services/connect/api/openapi.json` → `[7, 7, 10]` ✔; `jq '[.paths\|keys[]\|select(test("/connect/"))]\|length' docs/api/openapi.json` → `4` ✔; `grep -rln internalPrefix deployment/` → 4 files, `grep -rln 'internalPrefix:' deployment/` → 3 ✔; `deployment/metrics/kafka-jmx-exporter.yml:179` is the `internalPrefix` mention and `:174` is the `AND THE PREFIX THE READER USES IS KAFKA'S, NOT KUI'S` heading ✔; `kui-quickstart.yaml:49`, `kui-quickstart-auth.yaml:120`, `kui-demo.yaml:44` all still set it ✔; `ADR-053`'s deleted to-do is genuinely landed — `wire.golden.test.ts` asserts `body["event"] === ALERTS_EVENT_NAME` (at `de70b99` it was line 217; a sibling packet has since moved it to 251) ✔. |
| Shape of the case that closes it | Wrap each figure in the script's own opt-in block — `<!-- checked: openapi-totals -- claims: merged-paths, merged-operations, merged-schemas -->` … `<!-- /checked -->` — and add the matching extractor to `scripts/feature-matrix-check.sh`, which already fails a marked block that yields no assertion. The `61/72/156` figure is the one that goes stale this wave (W8-09 regenerates `docs/api/openapi.json`), so it is the one worth gating first. |
| Disclosed by packet | Yes (`knownUngated[2]`), and its `needsOutsideOwnership` correctly hands W8-09 the exact string to replace. |

### F6 — `InMemoryAlertStore.MaxReadMarkers`: upper direction still literal-only (**packet disclosed**)

| | |
| --- | --- |
| File | `services/alerts/infrastructure/src/kui/alerts/infrastructure/InMemoryAlertStore.scala:189` |
| Mutation | Not re-run (the packet's own `assertEquals(InMemoryAlertStore.MaxReadMarkers, 2000L)` in `InMemoryAlertStoreSuite:301` catches `2000L → 2L` by inspection, and the lower direction is behavioural over 2000 principals). |
| Residual hole | Raising the bound **and** the literal together is one edit from green: nothing observes that eviction happens above the bound, because `BoundedCache` evicts approximately and the store exposes no `stats`. |
| Shape of the case that closes it | Expose the marker cache's `stats` (`CacheMetrics` already computes them) on `InMemoryAlertStore`, then assert `estimatedSize <= MaxReadMarkers * someSlack` after `MaxReadMarkers * 2` distinct principals have written — the only form that survives Caffeine's approximate eviction. |
| Disclosed by packet | Yes (`knownUngated[1]`, with the limitation written into the case's own comment). |

### F7 — `ConnectorFacts.complete` is still a declaration with no production caller (**packet disclosed**)

| | |
| --- | --- |
| File | `services/connect/domain/src/kui/connect/domain/Connectors.scala:197` |
| Evidence | `grep -rn 'ConnectorFacts.complete' services/ libs/ apps/` → eight test call sites, no `src/` caller. The packet deleted the three callerless declarations the plan named (`ConnectorOperation.fromWire`, `ConnectorState.isPaused`/`Paused`, `ConnectorFacts.partial`) and left this one because it was outside the three. |
| Shape of the case that closes it | House rule 11: delete it and let the eight test sites construct `ConnectorFacts(connectors, Nil)`, or give it the production caller `perConnector`'s `.map` could be. Not W8-02's to widen; it is a one-line deletion plus eight mechanical test edits. |
| Disclosed by packet | Yes (`knownUngated[3]`), and its report of *"the packet named three and there were four"* is accurate. |

---

## 4. Mutations that went RED — recorded so nobody hunts them twice

| File / line | Mutation | Command | Result |
| --- | --- | --- | --- |
| `Connectors.scala:158` | `def isFailed = state.isFailed \|\| tasks.exists(_.state.isFailed)` → `state.isFailed` | `services.connect.__.test` | **RED**, 2 cases: `ConnectorsSuite.a running connector with a failed task is failed, which is the whole of the drawer's count` (:99) and `ConnectRoutesSuite.every task's state reaches the wire, with the worker's own trace on the one that failed` (:75) |
| `InMemoryAlertStore.scala:167` | `events.filterNot(…).sorted.take(MaxEventsPerCluster)` → `events.filterNot(…).take(…)` | `services.alerts.__.test` | **RED**: `InMemoryAlertStoreSuite.the store keeps at most 500 events per cluster, oldest first out` (:133) |
| `PrometheusExposition.scala:243` | `(!rated \|\| …endsWith(RateSuffix))` → `true` | `services.metrics.__.test` | **does not compile** — `-Wunused:all` + `-Werror` on the now-unused `rated`. Use the F3 form instead. |

---

## 5. Ownership, guard files, orphans

**Ownership — no violation.** `git diff --name-only de70b99` attributable to this packet is exactly the
eleven files it declared, all inside `services/{connect,alerts,metrics}/**` and the three ADRs it owns.
The one production file it changed is `services/connect/domain/src/…/Connectors.scala` (three
deletions). `services/connect/contract/**` and `services/alerts/contract/**` are byte-identical to
`de70b99` — verified, `git diff --stat` over both is empty — so W8-04's browser half and the
`wire.golden.test.ts` pair are genuinely unaffected. `frontend/packages/kernel/src/data/alerts/wire.golden.test.ts`
*is* modified in the tree, but by a sibling packet, not this one; W8-02 only cites it.

**Guard files — nothing enumerates what it deleted.** `grep -rn 'isPaused\|ConnectorState.Paused\|ConnectorOperation.fromWire'`
over `services/ libs/ apps/` returns only prose in the comments that record the deletion. No Scala
registry, `Set`, exhaustive match or count assertion names the removed members; `PAUSED` survives as a
**string** on the wire and in the browser (`feature-connect/src/model.ts`, `kernel/src/components/StatusPill.tsx`),
which is exactly the argument `ConnectorState`'s scaladoc makes, so deleting the constant moves no
document. `scripts/feature-matrix-check.sh` reads none of the three ADRs (see F5). The connect
`openapi.json` is regenerated and compared by `openApiCheck`, which passed.

**Orphans — every new thing has a caller.** `Rig.routes` ← `ConnectCapabilitiesSuite:224`;
`framesFor` ← three cases (`:256`, `:286`, `:412`); `warmUp`/`sentinel` ← `framesFor`;
`Worker`/`worker(…)`/`connectSettings`/`wiring`/`ask`/`document`/`connectorsPath`/`pausePath` ← the
three new `ConnectWiringSuite` cases; `frozen`/`analytics` ← the key and read-only cases. The only
callerless declaration left in the packet's tree is F7.

**Does it do what was asked, or exit zero?** It does what was asked. Items 1–6 are closed by cases that
assert on the product's output and whose fixtures were rebuilt so document order, sorted order and
reversed order are three different lists — the failure mode wave 7's `[2, 1]` fixture had. Item 11's
rig now drives `ConnectApi.routes` itself and hands the list back, and the new case compares it against
`ConnectApi.documented` rather than a written-out path list, so a fifth endpoint joins without editing
the file. `ConnectWiringSuite`'s three cases drive a real loopback Connect worker because `make` builds
its own backend and offers no seam — a defensible cost, not theatre. The red-baseline root-cause
(`framesFor`'s sentinel) is correct: fs2's `Topic` is single-subscriber-ordered, the assumption is
written into the helper's scaladoc, and the replacement turns a dropped publication into a failed count
instead of a timeout. Every number the packet published is reproducible and every one I checked was
right.
