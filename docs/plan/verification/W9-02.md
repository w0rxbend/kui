# W9-02 — verification pass

**Packet:** W9-02, *the comparison gate's other half, three ADR figures nothing read, and seven
filed findings given owners.*
**Baseline:** `a134d74c`. **Verified:** 2026-09-11, load average 23–28 throughout.
**Reverts:** every mutation below was applied to bytes saved by this pass into
`/tmp/claude-1000/-home-worxbend-kui/498665ec-79f4-4354-b359-b1c113fc78be/scratchpad/` and restored
from them; `md5sum` matches for `scripts/feature-matrix-check.sh`, `DEPENDENCY_MATRIX.md`,
`docs/FEATURE_MATRIX.md`, `DECISIONS.md`, ADR-048, ADR-052, ADR-053, ADR-054, ADR-055 and
`frontend/packages/api/README.md`. `git diff --stat a134d74c` over the packet's seven files is
byte-for-byte what it was before this pass began.

## 1. Acceptance, re-run

| Command | Result |
| --- | --- |
| `./scripts/feature-matrix-check.sh` | EXIT=0, `348 claims checked, all true`; `self-check: 7, rows: 18, merged-document: 50, milestones: 49, adr-index: 112, openapi-totals: 11, guard-fixtures: 13, dependencies: 88 over 12 named manifests`; `189 rows, 70 COMPLETE, 178 in scope, 39% delivered`; `65 paths, 76 operations, 160 schemas`; `56 rows over 56 ADRs`. **Every figure the packet published, reproduced exactly.** |
| (a) the one-line dependency attack, on the **finished** script | **RED, 2 disagreements, EXIT=1**, in two independent places — `dependency-fact` (`re-reading DEPENDENCY_MATRIX.md's own row -- 9.9.9 -- answers not in the cell`) and fixture 9 (`a dependency row publishing 9.9.9 against a pin of 8.2.2 drew 0 disagreement(s)`). Applied by hand: `"$pinned" "$(dependency_version_fact "$row" "$pinned")"` → `"$pinned" "$pinned"` at `scripts/feature-matrix-check.sh:1290`, with `\| vite \| 8.2.2 \|` → `9.9.9` in `DEPENDENCY_MATRIX.md:158` against `frontend/package.json:39` pinning `8.2.2`. |
| (b) the csrf one-line attack from M10 | **RED, 4 disagreements, EXIT=1** — two from `claim`'s claimed-side refusal (ADR-048 and `frontend/packages/api/README.md`), two from `audit_document_facts`' `document-claim` re-read. As reported. |
| `./mill --no-daemon tools.errorCodes.test` | `291/291, SUCCESS … 3s`; ErrorCodeDocSuite 0 failed / 8 total, BrowserConstantsSuite 0 failed / 9 total. EXIT=0. Mill queued behind another packet's `apps.allinone.__.test` rather than racing it. |
| `./mill --no-daemon frontend.apiConstants --check` | `constants.generated.ts is up to date (31 codes)`, `234/234, SUCCESS … 18s`. EXIT=0. |

**The owned rule is genuinely closed.** The mutation the wave plan names, applied by this pass and
not read out of the report, goes red in two named gates and reverts clean.

Three further claims of the packet's §3 were re-applied here and **hold**: `state-total`'s
claimed-side self-compare against a paragraph publishing `99 COMPLETE` is RED, and `adr-file`'s
self-compare against `DECISIONS.md` pointing ADR-052's row at ADR-053's document is RED. The
ADR gate is real too: `docs/adr/ADR-053:255` edited from `65 paths` to `61` prints
`says docs/api/openapi.json is 61 paths; it is 65` and exits 1.

**Every stated number checks out.** `services/metrics/api/openapi.json` = 8/8/13,
`services/alerts` = 6/6/12, `services/connect` = 7/7/10, `docs/api/openapi.json` = 65/76/160, and
the merged connect-path count is 4 — all re-derived with `jq`. The State recount
(72 RESEARCHING, 70 COMPLETE, 18 REVIEW, 8 IMPLEMENTING, 7 TESTING, 7 DEFERRED, 4 REJECTED,
2 PARTIAL, 1 SERVICE DONE NO UI, no BLOCKED; 189 total) reproduces under the same awk. Ten of the
thirteen MFE names exist on disk and `frontend/packages/api` is the eleventh directory. The seven
new `TECH_DEBT.md` rows point at real lines (checked one by one), TD-033's arithmetic re-derives
correctly (`RefreshMargin = 30.seconds`, `MinimumLifetime = 1.minute`, the `.filter` is dead for
every input), and TD-027's `./mill resolve '__.fix' | grep -c '\.test\.fix'` is **0** as claimed.
No orphans: every function the packet added has a production call site as well as a fixture.

## 2. Findings — mutations that left the suite green

Suite command for every row: `./scripts/feature-matrix-check.sh` (expected RED; observed
`348 claims checked, all true`, EXIT=0 unless stated).

| # | File | Exact mutation | Result | The case that would close it |
| --- | --- | --- | --- | --- |
| F1 | `scripts/feature-matrix-check.sh:583` | `if [[ ${groups[$at]} =~ ^[0-9]+(\.[0-9]+)*%?$ ]]; then` → `if false; then` — numeric claims fall back to the substring test, undoing the whole-token refusal this packet shipped | **GREEN** | Fixture 8 drives `figure_is_published` **directly** and never through `claim`, so the branch that routes a numeric claim to it is unguarded. A twelfth fixture must drive the shipped `claim` itself: `drive claim fixture-kind '18 operations' 8 8 '...'` must report 1, and `drive claim fixture-kind '8 operations' 8 8 '...'` must report 0 |
| F2 | `scripts/feature-matrix-check.sh:1254` | `dependency_cell_again() {` → `dependency_cell_again() {\n  dependency_cell "$1" "$2"; return` — the independent re-reader becomes the awk it exists to be independent of | **GREEN** | This is exactly what fixture 1 (`verify_fact_independence`) gates for `header_fact_from_document`, and section 3 got no analogue. A fixture must drive `dependency_cell_again` over a row the awk cannot answer for — `dependency_cell` requires `NF >= 6`, so a four-cell row `\| fixture \| 8.2.2 \|` discriminates: `dependency_cell_again` must answer `8.2.2` where `dependency_cell` answers empty |
| F3 | `scripts/feature-matrix-check.sh:1340-1344` | the whole `if [[ " ${manifest_pins[$name]:-} " == *" $claimed "* ]]` block → `pinned_state=pinned` | **GREEN** | `dependency-claim` is the gate on the mirror-image collapse (both sides out of the cell) and nothing exercises its refusing state. A fixture driving the audit over a synthetic ledger line whose claimed version no manifest pins must report 1 |
| F4 | `scripts/feature-matrix-check.sh:1351-1352` | `claim dependency-audit "" "$audited" "$recorded"` → `"$recorded" "$recorded"` | **GREEN** | The audit's own coverage figure compared with itself. A fixture driving `audit_dependency_rows` over a ledger of *n* `npm-version` lines with the loop narrowed must report 1 |
| F5 | `scripts/feature-matrix-check.sh:1333` | `recheck=$(dependency_version_fact "$row" "$claimed")` → `recheck=$fact` | **GREEN** | The second reader compared with itself — the precise defect `audit_dependency_rows` was written against, now present inside it. Closed by the same fixture as F3: drive the audit over a ledger line whose recorded fact the row does not carry, and require 1 |
| F6 | `scripts/feature-matrix-check.sh:1629` + `:1653` | `printf 'not a document in this repository'` → `printf '0'`; **and** `claim openapi-document "" "$file" "$present"` → `"$present" "$present"` | **GREEN singly and together** (the second half the packet disclosed as redundant; the first it did not) | With both, an ADR may publish totals about a document that does not exist and be compared against `0/0/0`. A fixture driving `openapi_fact` over a path that is not in the repository, asserting `not a document in this repository`, plus one driving `check_openapi_totals_region` over a block naming a missing file and requiring ≥1 |
| F7 | `scripts/feature-matrix-check.sh:1673` | none needed — append to `docs/adr/ADR-055-ksql-endpoints.md` a `<!-- checked: openapi-totals … -->` block reading `services/ksql/api/openapi.json` is **999 paths, 999 operations and 999 component schemas** | **GREEN** | Section 6's roster is a hard-coded three (`$adr052 $adr053 $adr054`) where section 5 globs `docs/adr/ADR-*.md`. A marker outside the roster is decoration: the twelfth service's ADR is outside the gate by default. Close it by globbing the ADR tree for `<!-- checked: openapi-totals` and failing on any file the roster does not name — the same shape `check_marked_file` already uses to refuse a document with no block |
| F8 | `docs/adr/ADR-054-connect-endpoints.md:287` (and `ADR-052:342`) | `**Four**` → `**Nine**` inside the block this packet marked | **GREEN** | A figure spelled as a word is invisible to both the comparison and the residue, which strikes digits only. `Four` is the merged document's connect-path count, which `jq '[.paths\|keys[]\|select(test("/connect/"))]\|length'` answers as 4 today. Either write it as a digit inside the sentence shape, or teach `report_unclaimed_figures` the number words it is allowed to see |
| F9 | `docs/FEATURE_MATRIX.md:637-664` | in the new wave-9 record paragraph, `7 \`TESTING\`` → `99 \`TESTING\``; and `**Ten of the thirteen named above exist**` (`:26`) → `**Ninety of the thirteen…**` | **GREEN** | Both figures sit outside every `<!-- checked: -->` marker. This packet re-published the full state totals and the MFE roster count in prose nothing compares — the drift at `25176c0` that the script's own header cites as the reason it exists. Wrap the record paragraph in `<!-- checked: rows … -->` (its state totals are already the `state-total` claim's shape) and give the MFE count a claim reading `frontend/packages/` off disk |
| F10 | `scripts/feature-matrix-check.sh:583` + `:790` | `if false;` (F1) **plus** `claim state-total "${claimed_token[$name]:-}" "${claimed[$name]}" …` → `"${actual[$name]:-0}" "${actual[$name]:-0}"`, with `docs/FEATURE_MATRIX.md:87` publishing `700 \`COMPLETE\`` against a table of 70 | **GREEN — `348 claims checked, all true`, EXIT=0**, the summary line printing `70 COMPLETE` directly beside the false paragraph | **This is a measured two-line attack on the file the whole script exists for**, equal in price to the two lines the packet publishes for the dependency row, and it defeats the claimed-side refusal the packet describes as the fix for sections 1, 4 and 5. Closing F1 closes it: with the whole-token branch gated, `70` is not published by `700 \`COMPLETE\`` and the refusal fires |

**Not a hole, recorded for the closer:** the packet's published price of **two lines** for the
dependency row stands — the single line (`dependency_version_fact` → `printf '%s' "$2"`) is RED via
fixture 9, confirmed here. F10 does not beat that price; it matches it on a different document.

## 3. Findings — outside this packet's ownership, and not reported

| # | File | What | Owner |
| --- | --- | --- | --- |
| F11 | `docs/overview/README.md:158` | The guard-file table publishes `\| ./scripts/feature-matrix-check.sh \| every count this repository publishes about itself \| 283 claims \|`. It is 348. The packet's `needsOutsideOwnership` names `docs/plan/WAVE-09.md` and `docs/plan/ROADMAP.md` and **misses this third quote** — and WAVE-09.md:779 lists `docs/overview/README.md` under *"unowned and correct as they stand"*, so nobody is looking at it | integrator |
| F12 | `docs/plan/WAVE-09.md:48, 60, 194, 306` and `docs/plan/ROADMAP.md:667, 771, 787, 1525` | `283 claims over seven sections` throughout; now 348 over eight. Correctly reported by the packet | integrator |

## 4. Findings — quality, not gates

| # | File | What |
| --- | --- | --- |
| F13 | `scripts/feature-matrix-check.sh:1584` | The `adr-file` claimed-side refusal prints `feature-matrix-check: a comparison: the \`adr-file\` claim compared …` — `where` is unset in the ADR loop, so the sentence names neither `DECISIONS.md` nor the ADR id, in the one gate the packet calls the sharpest of the five it found. The values are printed, so it is legible; the location is not. `scope adr-index "$file"` is set two lines above and could be read for the fallback |
| F14 | `TECH_DEBT.md:68` (TD-034) | *"seven test callers"* and *"the seven suites that call it"*: `grep -rn "ConnectorFacts.complete"` finds **eight call sites in seven files**, and two of the seven (`ConnectRig.scala`, `ConnectTestServer.scala`) are rigs rather than suites. The row's substance — no production caller — is correct |

## 5. Ownership

`git diff --name-only a134d74c` restricted to this packet's work is exactly the seven files it
reported: `scripts/feature-matrix-check.sh`, `DEPENDENCY_MATRIX.md`, `docs/FEATURE_MATRIX.md`,
ADR-052, ADR-053, ADR-054, `TECH_DEBT.md`. All seven are inside its **Owns** block. `DECISIONS.md`,
ADR-048, `frontend/packages/api/README.md` and `tools/**` are owned by it and untouched. Every
other path in the working tree belongs to a packet running in parallel (W9-01's deployment and
compose files, W9-03's `frontend/e2e/**`, W9-04's kernel and shell files, W9-06's masking tree);
none of it is traceable to W9-02's work. **No ownership violation.**
