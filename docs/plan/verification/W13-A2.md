# W13-A2 — the closer

**Owns:** every test tree, for additions; `docs/plan/verification/`. Baseline `350593e0`.
Every figure below was printed by the command beside it on **2026-09-12**. Every mutation was
applied by this packet and reverted **from bytes saved before the edit** — never `git checkout`,
`git restore` or `git stash` — and every restore is recorded with the `md5sum` it restored to.

**House rule 21 was followed: closed, did not hunt. No mutation of this packet's own choosing was
applied to anything a filed row did not name.** Wave 13's three verification passes filed
**twenty-two** rows. **Eleven are closed here** — every row in `scripts/feature-matrix-check.sh`
(W13-01's six plus the D-bis seam), three of W13-02's five, and one of W13-03's ten. One further
rule, in shipped code, was found *by* a closing case rather than looked for, and is closed too.

**The eleven left open, and why, so the next reader does not have to re-derive it.** W13-02's V-3
and V-4 both need the same thing — the seed script driven against a scripted Connect worker, which
is a test harness (a fake HTTP server, two scripted poll answers) rather than a case, and is the
one row on this list whose cost is not in the closing assertion. W13-03's V-01 through V-09 are
nine instances of **one** missing mechanism: a claim kind that reduces a documented listing or
figure to the command that prints it. Seven of them (V-01, V-02, V-03, V-11's `screens/`, V-04,
V-06, V-07) are the *same* claim kind — a directory or file listing compared against `git ls-files`
— and none of the prose carrying them is inside a marked region, so closing them needs markers,
which `WAVE-13.md` assigns to W13-01 and not to this packet. That is one packet's work with one
mechanism in it, not nine rows; filing it as nine is what makes it look larger than it is.

---

## 1. What closed, and the case that closes it

| Filed as | The rule | Where the case went | Control: mutation re-applied |
| --- | --- | --- | --- |
| **W13-01 F-1** | a `quotations` block in a third document is swept on the day it grows | `scripts/feature-matrix-check.sh` §9 — the two-name `for file in README.md "$matrix"` loop is now `marked_quotation_files`, derived; `quotation_handled_kind` is struck out where the **sweep** set is built and no longer in the derivation, so the roster covers all fifteen markers | no mutation needed. Before: the scratch page green at 439. After: **exit 1**, the quotation refused by name **and** the roster naming the undeclared block |
| **W13-01 D-bis** | the fixture seam works at all | `"$marked_markdown_root/$file"` in `marked_blocks_on_disk` and in `quotation_census` (the declared, minimal, behaviour-preserving seam) | bare `"$file"` restored → `marked_blocks_on_disk` answered `[]`, **3 disagreements** |
| **W13-01 F-4** | a marked block the roster does not name | fixture 26 `swept-roster-reconciled`, arm 2 | `undeclared=` → **1 disagreement**, exit 1 |
| **W13-01 F-3** | a roster entry with no block on disk | fixture 26, arm 4 | `unpresent=` → **1 disagreement**, exit 1 |
| **W13-01 F-5** | what the derivation walks and refuses to walk | fixture 27 `marked-markdown-pruned` | prunes → `(.git)`: **red**, naming `node_modules/vendored/README.md target/generated.md`. `docs/plan` clause deleted: **red**, naming `docs/plan/WAVE-99.md` |
| **W13-01 F-2** | the sweep's block count against its second reader | fixture 28 `sweep-block-count` | `"$blocks" "$expected"` → `"$blocks" "$blocks"` → **1 disagreement**, exit 1 |
| **W13-01 F-6** | `--census`, the figure that replaced a figure | fixture 29 `quotation-census-driven` | `attributions + 1` → `+ 0` → **red**, printing the self-contradictory `[0 attributions, 1 of them absent]` |
| **W13-02 V-1** | the guard's POSITION, not its presence | `libs/config/test/src/kui/config/ShippedScriptGuardPositionSuite.scala` (new) | guard moved onto the `printf` → **395/395, 1 FAILED**, `connect-seed.sh:150`. `ShippedScriptsSuite` stayed green, exactly as filed |
| **W13-02 V-2** | one level of function indirection | the same suite — function bodies are resolved into the substitution that calls them | `count_running()` helper → **395/395, 1 FAILED**, `connect-seed.sh:151` |
| **W13-02 V-5** | the evidence command that could not fail | `libs/config/test/src/kui/config/PublishedEvidenceCommandSuite.scala` (new) — the `ex='…'` range is read out of the comment and **run** | range reverted to wave 12's `/^      masking:/,/^$/` → **1 FAILED**, *returns no `kind: replace` line* |
| **W13-03 V-10** | the documentation of a rule drifting from the rule | `capability-rule-documented`, a claim comparing `README.md`'s enumeration against `negation_tokens` and `state_tokens` in both directions | `*never*` deleted from the README → **exit 1**. `state_tokens` grown by `skeleton` → **exit 1**. Both directions |

## 2. The rule this packet found rather than received

`ShippedScriptGuardPositionSuite`, pointed at the tree for the first time, named **three shipped
call sites in `deployment/compose/smoke.sh`** — `gateway_contracts` (:330) and `service_containers`
(:332, :393). Both functions carry unguarded `grep` stages in their bodies and are read into
assignments under `set -euo pipefail`. This is **V-2's class in shipped code**: `ShippedScriptsSuite`
cannot see them because it folds the assignment line and the pipeline is one function away.

`gateway_contracts` is the sharper of the two. The line immediately below its call site is

```bash
[[ -n "$contracts" ]] || fail "no service contract could be read out of ServiceContracts.byService"
```

— the sentence written for exactly the case in which the `grep` matches nothing, and `set -e` ends
the script one line above it. Repaired in the shape W13-02 already used twice in the same file:

```diff
-    grep -oE 'ServiceId\.unsafe\("[a-z0-9-]+"\)->' |
+    { grep -oE 'ServiceId\.unsafe\("[a-z0-9-]+"\)->' || true; } |
-    grep -E '^kui-' |
-    grep -Ev '^kui-(gateway|frontend)$' |
+    { grep -E '^kui-' || true; } |
+    { grep -Ev '^kui-(gateway|frontend)$' || true; } |
```

`deployment/**` is W13-02's. Declared in `needsOutsideOwnership` rather than left open, because the
alternative was shipping a red suite.

## 3. Ownership edges crossed, each declared

1. **`scripts/feature-matrix-check.sh`** (W13-01's entire file). Section 6 is that script's own test
   tree and W13-01's report assigns four `guard-fixtures` cases to this packet by name. Also the §9
   derivation (F-1), the two-token seam (D-bis) and the `capability-rule-documented` claim (V-10).
2. **`docs/overview/README.md:204`** (W13-03's). The one-integer `gate-table` edit its report
   assigns to whoever raises the total: `**439 claims**` → `**444 claims**`.
3. **`deployment/compose/smoke.sh`** (W13-02's). §2 above, six lines.

## 4. Acceptance, as it actually printed

| Command | Printed |
| --- | --- |
| `./scripts/feature-matrix-check.sh` | **`444 claims checked, all true`**, exit 0; `guard-fixtures: 29`, `capability-claims: 44`, `quotations: 12` |
| `./scripts/feature-matrix-check.sh --census` | `113 markdown files … 1626 attributions, 1614 of them absent`, exit 0 — **identical to W13-01's reading**, so the seam repair is behaviour-preserving at `marked_markdown_root=.` |
| `./mill --no-daemon libs.config.test` | **`395/395, SUCCESS`** |
| `./mill --no-daemon build-tests.test` | **`139/139, SUCCESS`** |
| `./mill --no-daemon libs.config.test.checkFormat` | **`9/9, SUCCESS`** |
| `./mill --no-daemon libs.config.test.fix --check` | **`415/415, SUCCESS`** |
| `bash -n` over both edited shell files | parses; `awk 'length>100'` is **31** in the checker and **4** in `smoke.sh`, `diff` against `350593e0` empty in both — no reflow |

## 5. The tree, checked before finishing

Every file this packet mutated is restored byte-identical to the `md5sum` its owner recorded:

```
f6086bc56de6d14153f8f733b09303d5  deployment/quickstart/seed/connect-seed.sh
1258e28a6ae32a3e7ad7adf021dda769  deployment/quickstart/kui-quickstart-auth.yaml
a02c7b71e99cd49b65981fe635889c0b  README.md
0e437fa81e4a833218e787ad523c5116  TECH_DEBT.md
```

No container is running (`docker ps` 0) and this packet started no background load.

**One mutation in this tree was not this packet's. It was present mid-pass and is gone now.**
`frontend/packages/feature-ksql/src/model.ts:286`:

```diff
-  if (count === 0) return "";
+  if (count < 0) return "";
```

That is a never-render-zero guard inverted: with it, a result that dropped nothing renders *"0 rows
arrived earlier and are no longer held"*. It appeared in the working tree during this pass, a Mill
daemon was running `services.gateway.application.test` throughout, and `frontend/packages/feature-ksql`
is in **no** packet's declared mutation path this wave — W13-A1's is `{shell,kernel}/src/**`. This
packet did not touch `frontend/` and deliberately did **not** revert it, because reverting another
session's in-flight mutation destroys its measurement. It was gone from `git status --porcelain` at
this packet's last reading, so its owner restored it and the tree is clean of it. Recorded anyway,
for two reasons: an integrator reading a `git status` taken one minute earlier would have found an
inverted never-render-zero guard beside a green build, which is the failure mode this wave's brief
calls the worst outcome; and it is the fifth wave in a row in which two sweeps sharing one checkout
have cost a reading. **Re-check `git diff --name-only 350593e0` against the list above before
committing.**
