# W10-05 — verification

Adversarial re-verification of packet **W10-05** ("The deployment, the build edge, and the gate that
dies without saying so"), against the working tree at baseline `ee81a440`, on 2026-09-11/12.
Every figure below was printed by the command beside it, by me, in this pass. Every mutation named
here was applied by me and reverted by me; `git diff --stat ee81a440` is back to
**47 files changed, 3,069 insertions(+), 373 deletions(-)** and all six W10-05 files carry exactly
the stats they carried when this pass started.

Concurrency note: four other packets were landing Scala and frontend changes in this working tree
throughout, so `git diff --stat ee81a440` totals moved by one line during the pass
(`services/gateway/api/src/kui/gateway/api/StreamProxy.scala`, 17 → 15, W10-06's). **No W10-05 file
moved.** Every compose run below used `KUI_PORT=18080 KUI_FRONTEND_PORT=18090` so that the live
quickstart other packets were using stayed up.

---

## 1. Acceptance, re-run

| # | Command | Result |
| --- | --- | --- |
| 1 | `./deployment/compose/smoke.sh` | **PASSED, exit 0.** New block `every contracted service answers a signed read, and not only a capability probe` printed nine 200s: alerts `/alerts/events`, cluster `/api/v1/clusters/{clusterId}`, connect `/connect/connectors`, consumer `/consumer-groups`, ksql `/ksql/objects`, message `/topics/smoke-traffic/messages/stream?limit=1`, metrics `/metrics/latency`, schema `/schemas/subjects`, topic `/topics` |
| 2 | `docker compose -f deployment/compose/docker-compose.yml up -d --wait` | exit 0, **35.283s**, 16 containers, every one `healthy` |
| 3 | `./mill --no-daemon __.openApiCheck` | `2544/2544, SUCCESS`, exit 0 (wave file's baseline figure of 2,629 is other packets' drift, not this one's) |
| 4 | `./mill --no-daemon __.compile` | `8251/8251, SUCCESS`, exit 0 |
| 5 | `deployment/quickstart/quickstart.sh down` then `quickstart.sh` | down: exit 0, 20.497s, *"Removed. Nothing from the quickstart is left running or stored."* up: exit 0, **1:05.11**, *"KUI is running: http://localhost:8090/ui/"*, 8 containers, 2 clusters registered |
| 6 | `./mill --no-daemon services.message.infrastructure.test.compile` (W10-04 build edge) | `763/763, SUCCESS`; `compileClasspath` names `otel4s-oteljava-testkit_3-1.1.0.jar`; `MaskingMetricsSuite.scala` imports `org.typelevel.otel4s.oteljava.testkit.OtelJavaTestkit` — **the edge has its caller** |
| 7 | `npx tsc --noEmit -p frontend/e2e/tsconfig.json` | exit 0, no output |
| 8 | `pnpm exec playwright test e2e/brokers.spec.ts` on a stack `quickstart.sh` had just built | `[globalSetup] waited 14s for the first scrape of 2 cluster(s) to land.` **11 passed (31.3s)**, `:157` among them |

The packet's reported figures reproduce. The one number I could not reproduce is in row 11 below.

## 2. The owned rule — genuinely closed

I re-applied the mutation myself (`deployment/compose/docker-compose.yml:212`, `kui-topic`'s
`KUI_PRINCIPAL_KEY` → `a-different-key-from-the-gateways-on-purpose-w1005-verify`), tore the stack
down, and ran `./deployment/compose/smoke.sh` from cold:

```
  schema answers /api/v1/clusters/{clusterId}/schemas/subjects: 200

FAILED: topic answers /api/v1/clusters/{clusterId}/topics was '401' after 90s, expected '200'
exit 1
```

Where wave 9 printed `PASSED` in 107s, the script is now red, and the message names the status code
that sends a reader to the principal key. Reverted; `git diff ee81a440 --
deployment/compose/docker-compose.yml` is empty and the file's md5 is back to
`2d3dfb52599eb0d5bec8730242975687`. **ownedRuleGenuinelyClosed: true.**

I also measured the loop's reach rather than assuming it. With **every** service's
`KUI_PRINCIPAL_KEY` wrong at once (lines 178, 212, 239, 266, 306, 380, 422, 481, 509), eight of the
nine derived reads answer `401 KUI-UNAUTHENTICATED` — so the loop's assertion has real power for
alerts, cluster, connect, consumer, ksql, metrics, schema and topic. The ninth is row 1 below.
Separately, `/api/v1/clusters/measured` is genuinely proxied (408 `KUI-UPSTREAM-UNAVAILABLE` with
`kui-cluster` stopped), so the `cluster` row is not a gateway-local decoy.

## 3. Findings

| # | File | Exact mutation | Suite command | The case that would close it |
| --- | --- | --- | --- | --- |
| 1 | `deployment/compose/smoke.sh` (the `STREAM_READS` arm, ~line 913) | `deployment/compose/docker-compose.yml:239` — `kui-message`'s `KUI_PRINCIPAL_KEY` → any other value | `./deployment/compose/smoke.sh` | **STAYS GREEN.** `message answers …/messages/stream?limit=1: 200` — because an SSE endpoint answers `HTTP/1.1 200 OK` and puts the refusal in the body. Measured verbatim with the wrong key: `event: error` / `data: {"code":"KUI-UPSTREAM-UNAVAILABLE","message":"message could not be reached",…}`; with the right key the same read prints `event: phase` … `event: consumed` … `event: done`. The loop asserts `%{http_code}` only, so the **one service whose read the packet had to hand-write is the one service the new block cannot fail on** — the exact wave-9 defect, surviving in the ninth of nine. Close it by reading the first frame, e.g. `curl -sN -m 20 "$base$path" \| head -c 200` and `fail` when it matches `^event: error`, or `await` on `event: phase` |
| 2 | `.github/workflows/ci.yml:170-208` (step *The interface image copies every workspace manifest*) | In `deployment/frontend/Dockerfile`, replace `COPY frontend/packages/feature-ksql/package.json ./packages/feature-ksql/` with a second copy of the `kernel` line | the step's own script (`bash` over `jobs.frontend.steps[].run`) | **STAYS GREEN** — `frontend/packages: 11 packages, 11 manifests copied`, exit 0. The gate compares two **counts**; the sorted-name `diff` that would name the missing package sits **inside the failure branch** and never runs on an equal count. A roster that has lost `feature-ksql` and gained a duplicate is the drift wave 9 actually repaired. Close it by making the name comparison the assertion: `diff <(ls frontend/packages \| sort) <(grep '^COPY frontend/packages/' … \| sed -E … \| sort)` as the exit-status test, with the count line kept only as the message |
| 3 | `.github/workflows/ci.yml:170-208` | Same file, break only the destination: `… ./packages/feature-ksql/` → `… ./packages/feature-kqsl/` | as above | **STAYS GREEN**, exit 0. The line is still counted, the manifest lands in a directory pnpm's workspace does not name, and `--frozen-lockfile` resolves the smaller workspace exactly as it does for a deleted line. Same one-line close as row 2 (the sorted-name derivation already strips to `\1`, so it catches the source name; add the destination by comparing `sed -E 's#.*\./packages/([^/]+)/#\1#'` too) |
| 4 | `build.mill:134-166` (`runDocumentMain`) | `Task.fail(s"$mainClass exited ${ran.exitCode}:\n$said")` → `Task.log.info(s"…")` (one word), then delete one path from `services/topic/api/openapi.json` | `./mill --no-daemon services.topic.api.openApiCheck`, `./scripts/run-tests.sh` | **STAYS GREEN.** `629/629, SUCCESS` over a genuinely stale committed document — the generator's sentence is printed as information and the gate passes. The whole of `openApiCheck`'s power now routes through 33 lines that no suite reads, in the one file that is outside `__.checkFormat`, outside `__.fix` and outside every test module (TD-036). The repair is correct — I confirmed the happy path: a stale document now fails as `[error] services.topic.api.openApiCheck kui.topic.api.OpenApiDocument exited 1:` followed by *"…is out of date with the endpoints it is generated from; run ./mill services.topic.api.openApi"* — but nothing can fail if it is undone. Close it with a case over a JVM-side helper (`Task.Anon` logic extracted to a testable object taking `(exitCode, out, err)` and returning `Either[String, Unit]`, exercised from `libs/testkit` or a `tools` module): exit 0 → Right, exit ≠ 0 with text → Left containing the text, exit ≠ 0 with silence → Left containing *"printed nothing on stdout or stderr"* |
| 5 | `build.mill:879` and `build.mill:4192` | None needed — the defect is present as shipped. `frontend.apiConstants` and `docs.errorCodes` still call `runMain`, not `runDocumentMain` | `./mill --no-daemon frontend.apiConstants --check` (CI runs this at `.github/workflows/ci.yml:91`) | **The failure W10-05 was asked to eliminate still exists on a gate CI runs on every push.** Measured by forcing a dead fork (`override def forkArgs = Seq("-XX:MaxRAMPercentage=notanumber")` on `tools.errorCodes`): the child's three lines appear under a *separate* key `tools.errorCodes.runMain`, and the error is the bare `[error] tools.errorCodes.runMain Subprocess failed` — detached, and lost among nine modules under a `__.` run. With the one-word change `tools.errorCodes.runMain(…)` → `tools.errorCodes.runDocumentMain(…)` the same failure reads `[error] frontend.apiConstants kui.tools.BrowserConstantsMain exited 1:` followed by the JVM's own words. `tools.errorCodes` already `extends KuiJvmModule`, so the seam exists: **two call sites, one word each**. Both build.mill mutations reverted |
| 6 | `frontend/tsconfig.json` + `frontend/package.json` (owned by nobody this wave) | Append `const w1005VerifyTypeError: number = "not a number";` to `frontend/e2e/globalSetup.ts` | `pnpm -C frontend typecheck` | **STAYS GREEN, exit 0.** `npx tsc --noEmit -p frontend/e2e/tsconfig.json` catches the same line (`e2e/globalSetup.ts(265,7): error TS2322`). The packet's report is exactly right, and it matters here because **W10-05's own 163-line delivery is the code outside the gate**. `e2e/tsconfig.json` sets `composite: false`, so it cannot be added to the solution file's `references` as it stands; the close is either `composite: true` plus a reference, or a second script `typecheck:e2e` wired into the `frontend` CI job beside the roster step |
| 7 | `frontend/e2e/globalSetup.ts:130` | Delete `if (dirs === undefined \|\| dirs === 0) pending.push(\`${cluster}: /log-dirs\`);`, `docker restart kui-quickstart-kui`, launch the instant `/api/v1/health/ready` answers 200 | `pnpm exec playwright test e2e/brokers.spec.ts:157` | **GOES RED, as the packet said** — `expect(locator).toContainText(/No disk capacity was reported/i) failed`, received `"kafkakafka:9092LEADERS—REPLICAS—disk—This broker's disks were not measured, so KUI cannot say how full they are."`, with `/log-dirs` at `{"s":"ok","n":0}` at readiness. Restored: `[globalSetup] waited 28s for the first scrape of 2 cluster(s) to land.` / `1 passed`. **No finding against the harness — it is load-bearing and CI's `browser` job (`quickstart.sh` then `pnpm e2e`, ci.yml:315-318) is the cold stack that exercises it.** Recorded here so the closer does not re-measure it |
| 8 | `frontend/e2e/globalSetup.ts:77-81` | None — a doc claim | `curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/health/ready` | The measured table in the new scaladoc is keyed on **`/health/ready`**, which answers **404** on the quickstart, at every age of the stack. The gateway's path is `/api/v1/health/ready` (`/health/ready` is the *per-service* path, `deployment/compose/docker-compose.yml:193`). The substance of the table — readiness leads the first scrape by ~29s — reproduces; the path a reader would copy does not, and I lost a command to it in this pass. One-word correction in the comment |
| 9 | `frontend/e2e/brokers.spec.ts:169-176` (W10-02's file; correcting a claim W10-05 filed for W10-A2) | — | `pnpm exec playwright test e2e/brokers.spec.ts:157` | W10-05 filed this as *"the no-capacity arm can never pass … the shipped card says 'This broker's disks were not measured'"*. **Half right.** `diskNote` (`frontend/packages/feature-clusters/src/model.ts:230-235`) has **two** no-capacity sentences: `heldBytes !== null` → *"No disk capacity was reported for this broker…"*, which the assertion matches, and `heldBytes === null` → *"This broker's disks were not measured…"*, which it does not. So the arm is dead only in the second sub-case — which is the **pre-scrape** state the packet induced, not "a deployment that genuinely reports no capacity" as filed. W10-A2 should close it as *the case asserts one of the product's two refusals*, not as *the case asserts the wrong sentence* |
| 10 | `frontend/packages/feature-clusters/**` (nobody's, this wave) — an observation for W10-01/W10-02 | — | — | In the pre-scrape window the harness now waits out, the broker card renders `LEADERS—REPLICAS—disk—` — three bare em dashes, captured verbatim from the failure in row 7. That is close to the shape DoD item 1 forbids, and the harness fix means **no case will ever see that window again**. Either the em dash is the product's honest refusal marker (in which case say so where `:246`/`:278` say it) or it is item 1's defect in a transient state. A judgement, not a defect I am asserting |
| 11 | `deployment/quickstart/README.md` (the consumer-group block) | None — a published figure | `curl -s localhost:8080/api/v1/clusters/quickstart/consumer-groups` on a stack `quickstart.sh` has just built | The README replaces two wrong numbers with a third that does not reproduce. It publishes `order-fulfilment … totalLag 10`. On the stack this pass created from cold, three minutes old and driven only by read-only cases: **`totalLag` 9** (per-partition `0,0,0,1,2,6`); on the older stack this pass inherited it was **11**. The stable, honest facts around it (`EMPTY`, 0 members, uneven lag, four groups, `analytics-indexer` `STABLE`/1/0, `kui-quickstart-connect` `STABLE`/0/`incomplete`) all reproduce exactly. **Every other figure in the file is correct**: `kafka-topics.sh --list \| wc -l` = **16**, `totalItems` = **12**, `showInternal=true` = **16**, the four hidden topics are the four named, the seed file holds nine rows of which one is internal. The packet's own filed row asks for a `deployment-claims` kind in `scripts/feature-matrix-check.sh`; until then the single-digit lag should be written as *"behind, with uneven lag across its six partitions"* rather than as a number that is wrong on the next boot |
| 12 | `TECH_DEBT.md:71` (W10-03's file) | None — a stale row | `grep -n TD-037 TECH_DEBT.md` | **TD-037 is still `open`** and still says smoke.sh *"passes over a stack whose entire topics product answers 401"* — over a tree where I just made it exit 1. The row says it exists *"so that it outlives the wave plan"*; W10-05 closed the hole and did not tell W10-03. This is the wave's own subject — a document asserting a hole the tree has closed — committed inside the repair wave |

## 4. Ownership

`git diff --name-only ee81a440` names 47 files. All six files W10-05 reports are inside its `Owns`
(`deployment/**` except `kui-quickstart.yaml`, `build.mill`, `.github/workflows/ci.yml`,
`apps/allinone/**`, `frontend/e2e/globalSetup.ts`, `frontend/e2e/fixtures.ts`).
**No ownership violation found.** `deployment/quickstart/kui-quickstart.yaml` did change, and its
diff is W10-04's masking block (DM-001/ADR-023), not W10-05's. `frontend/e2e/fixtures.ts` and
`apps/allinone/**` are untouched. `frontend/e2e/brokers.spec.ts` changed, and its diff is W10-02's
case work, not this packet's.

## 5. Guard files and orphans

* **Guards that enumerate what this packet changed:** `scripts/feature-matrix-check.sh` has eight
  claim kinds and none of them reads `smoke.sh`, `deployment/frontend/Dockerfile` or
  `deployment/quickstart/README.md`'s figures — the packet's own filed row is correct.
  `.github/workflows/ci.yml`'s *Build the container images* step derives its image list from
  `docker-compose.yml` and is self-tested with a fixture; it is unaffected. `deployment/compose/README.md`
  describes `smoke.sh`'s assertions in prose in 14 places and no gate reads it.
* **Orphans:** none. `runDocumentMain` has 22 call sites in `build.mill`; `service_read_path` has two
  (the connect block and the new loop, with `connect_read_path` deleted so there is one definition);
  `STREAM_READS`, `awaitFirstScrape`, `registeredClusters` and `sectionRows` are each called;
  the CI step is in the `frontend` job; the `otel4s-oteljava-testkit` edge is consumed by
  `MaskingMetricsSuite.scala`.
* **Self-tests:** `service_read_path`'s two fixtures do bite — inverting the tag filter or the
  `text/event-stream` filter fails the fixture before the stack is touched. The roster step's own
  fixture (two anchored `COPY` lines, one indented, one inside a `RUN`) also bites; what it does not
  cover is rows 2 and 3.

## 6. Numbers that were not quite right

* `build.mill` is **4,357** lines (`wc -l`), not the 4,344 the packet reported; `TECH_DEBT.md`'s
  TD-036 figure of 4,374 is `build.mill` + `mill-build/build.mill` and is correct.
* The packet's own lines in `build.mill` are within 110 columns as claimed (the three over-length
  lines, 876/1009/1490, are all pre-existing).
* The leaked `http-server` on `:6017` the packet flagged at its close is **gone** (`ss -ltnp` is
  clean); the a11y sweep that owned it has finished.

*House rule 18: this file was written as a stub before the first command ran and filled as the pass
went. W10-A2 reads this directory as its input.*
