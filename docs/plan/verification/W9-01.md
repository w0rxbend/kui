# W9-01 verification — the stack: a second cluster, eleven containers, and the rebuild that happens last

**Written FIRST, as a stub, per house rule 18 (amended).** Rows below are empty until the run that
fills them happens; a row that is still empty when this file stops being edited is a thing this
packet did not reach, which is strictly more than nothing.

Started 2026-09-11 from `a134d74c`, tree clean at start.

## What this packet announces to the wave (items 1–3), first hour

| announcement | value |
| --- | --- |
| second registered cluster id | **`staging-eu-01`** |
| second cluster display name | **`staging-eu-01`** — the design's own word (`SCREENS-V4.md` §1 `M08`), and the shell draws `Switched to ${name ?? id}`, so the *name* is what a browser case reads |
| second profile over one broker, or a second broker? | **A second registered PROFILE over ONE broker.** `bootstrapServers: [kafka:9092]`, the same broker `quickstart` uses. Two entries in the registry, two ids in `/api/v1/clusters`, two rows in the selector — and the same topics, groups and brokers behind both, because it is the same Kafka. No `metrics.sources` entry (that is `traffic.spec.ts:536`'s subject), no `schemaRegistry`, no `connect`, no `ksql`. |
| `kui-allinone:0.1.0-SNAPSHOT` image id, FIRST pair | **`ab2468f25417`** (built 2026-09-11 from the tree at `a134d74c`; wave 8's was `c575438c5bfc`) |
| `kui-frontend:0.1.0-SNAPSHOT` image id, FIRST pair | **`55bec7277ccf`** (wave 8's was `e9fadeecc330`) |
| `kui-allinone:0.1.0-SNAPSHOT` image id, SECOND pair (last hour) | **`00c497c06754`** |
| `kui-frontend:0.1.0-SNAPSHOT` image id, SECOND pair (last hour) | **`34db4fcf7948`** |
| Connect worker carries `quickstart-file-source` | **yes** — `curl -s localhost:8083/connectors` -> `["quickstart-file-source"]`, connector `RUNNING`, task 0 `RUNNING` |
| ksqlDB server carries `QUICKSTART_ORDERS` | **yes** — `SHOW STREAMS;` -> `QUICKSTART_ORDERS` over `orders.v1`, `KAFKA`/`JSON` |

## Acceptance, as run

| # | command | what it printed |
| --- | --- | --- |
| A1 | `docker compose -f deployment/compose/docker-compose.yml up -d --wait` | **exit 0, 62s.** 16 containers, 11 of them `kui-*`: `kui-gateway`, `kui-frontend` and the nine services. All `Healthy`. |
| A2 | `curl -sf localhost:8090/ui/ >/dev/null && curl -sf localhost:8080/api/v1/health/ready` | **exit 0.** `{"ready":true,"checks":[{"name":"process","healthy":true,"detail":null}],"at":"2026-09-11T15:32:34.498Z"}` |
| A3 | `./deployment/compose/smoke.sh` — run 1 of 3 | **PASSED, exit 0, 1m23s.** |
| A4 | `./deployment/compose/smoke.sh` — run 2 of 3 | **PASSED, exit 0, 1m51s.** |
| A5 | `./deployment/compose/smoke.sh` — run 3 of 3 | **PASSED, exit 0, 2m00s.** |
| A6 | `./mill --no-daemon apps.allinone.__.test` | **SUCCESS, 35 cases over 6 suites, 0 failed.** `AllInOneWiringSuite` 14, `SignInSeamSuite` 8, `CapabilitySeamSuite` 5, `InProcessServiceClientSuite` 4, `FaultIsolationSuite` 3, `InProcessStreamingSuite` 1. First attempt at 18:30 could not compile — `services/message/app/MessageWiring.scala` was mid-flight in W9-06 (`missing argument for parameter masking of method make`). Not this packet's, and green on the re-run fourteen minutes later. |
| A7 | `curl -s localhost:8080/api/v1/clusters` — two ids, not one | **`quickstart` and `staging-eu-01`.** The plan's `jq -r '.clusters[].id'` is the wrong path for this envelope and prints `jq: error (at <stdin>:0): Cannot index string with string ("id")` — `clusters` is an ADR-034 section, so the ids are at `.clusters.data[].cluster.id`, which is the path `frontend/e2e/traffic.spec.ts:543` already uses. |
| A8 | `pnpm -C frontend build && node frontend/scripts/bundle-shape.mjs` | **exit 0.** `vite v8.2.2`, 300 modules, built in 463ms, 18 chunks. `bundle-shape.mjs`: *"✓ 8 feature packages, all dynamically imported and none reachable statically from the entry chunk"*. Run immediately before the second image build, as item 6 asks. |

## Mutations this pass intends to apply

| # | target | exact mutation | what must happen | result |
| --- | --- | --- | --- | --- |
| **M1** (the mutation line) | `deployment/compose/docker-compose.yml` + `deployment/compose/kui.yaml` | deleted the whole `kui-connect:` service block (27 lines), the gateway's `depends_on: kui-connect` entry, and `kui.gateway.services.connect` in `kui.yaml` | preflight must refuse **before a container starts** | **RED, exit 1, and no container started** (`docker ps` = 0 before, 0 after; the failure's own `docker compose ps` table is empty). Quoted in full below. Reverted from saved bytes; `md5sum -c` OK on both files. |
| **M2** (the green one) | `deployment/compose/docker-compose.yml:212` | `KUI_PRINCIPAL_KEY: ${KUI_PRINCIPAL_KEY:-compose-development-key-…}` -> `KUI_PRINCIPAL_KEY: a-different-key-than-the-gateway-signs-with`, on `kui-topic` only | — | **GREEN. `smoke.sh` PASSED, exit 0, and printed `topic capability: available` twice**, over a stack where every topics call through the gateway answers **HTTP 401 `KUI-UNAUTHENTICATED`**. Run in both directions per house rule 9. Reverted; `md5sum -c` OK. |
| **M3** | `deployment/quickstart/kui-quickstart.yaml` | removed the second cluster's `id: "staging-eu-01"` line, restarted `kui-quickstart-kui` | — | **GREEN, and it is provably green.** `/api/v1/clusters` still answers `quickstart` / `staging-eu-01`: ADR-031's slug of the name `staging-eu-01` **is** `staging-eu-01`. So the `id:` line I wrote is decoration for *this* name. Kept anyway, for the reason the cluster above it gives in writing — a derived id moves when somebody edits the display name, and this one is in a URL, a Playwright case and a screenshot — and said out loud here rather than left for a reader to solve the algebra. Reverted; `md5sum -c` OK. |

## Item 5 — `AllInOneWiringSuite` carries the ksql stream: CONFIRMED, with a caveat about who gates it

**Read.** `apps/allinone/test/src/kui/allinone/AllInOneWiringSuite.scala` asserts, inside
`startsAndServesEveryGatewayAndProxiedRoute`, both of the eleventh service's paths by name —

```
paths.contains("/api/v1/clusters/ksql/objects")   "the in-process ksql service's object listing was not proxied"
paths.contains("/api/v1/clusters/ksql/stream")    "the public ksql push-query relay was not mounted"
```

— beside the alerts feed and the alerts stream, and `startupLogNamesTheDeploymentShapeAndTheServices`
pins the log string to `alerts,cluster,connect,consumer,identity,ksql,message,metrics,schema,topic`.

**Run.** `./mill --no-daemon apps.allinone.__.test` -> **SUCCESS**, six suites, **35 cases, 0 failed**:
`AllInOneWiringSuite` 14, `SignInSeamSuite` 8, `CapabilitySeamSuite` 5, `InProcessServiceClientSuite`
4, `FaultIsolationSuite` 3, `InProcessStreamingSuite` 1.

**Mutated, because "confirm, do not assume" means trying to defeat it.** The only lever inside
`apps/allinone/**` is the client roster: registering the ksql service's routes under `ConnectApi.Id`
(`AllInOneWiring.scala`, one identifier) makes the run **RED** — `AllInOneWiringSuite` 11 of 14 failed
and `SignInSeamSuite` 8 of 8 — but it fails through `GatewayWiring$BadContract: the operation id
'connect.connector.pause' is claimed by more than one endpoint`, plus a genuine
`theWiredServiceListMatchesTheDeclaredOne` comparison failure at `AllInOneWiringSuite.scala:164`. So
the roster half is gated from this tree and **the two ksql path assertions are not defeatable from
this tree at all**: the push-query relay is mounted inside `GatewayWiring.over`, which
`AllInOneWiring` merely calls. Deleting the relay is an edit in `services/gateway/**`, which is
**W9-A1's** this wave. Recorded rather than glossed: the assertion exists, it passes, and the only
mutation that would isolate it belongs to another packet. Reverted from saved bytes; `md5sum -c` OK.

## Two figures in files I own, re-measured rather than left standing

Both were prose that named a number nothing could check, and both had grown stale by exactly the
mechanism house rule 11 exists for.

* **`deployment/compose/smoke.sh:16`** said the run *"takes about a minute"*, measured when the stack
  ran eight containers. Re-measured on eleven: **83s, 112s, 121s**, three consecutive runs against
  images that already existed. The header now carries the new figure, its scope and where the growth
  went (`up --wait`, not the assertions).
* **`.github/workflows/ci.yml`** carried *"seven Mill images took 83s and `smoke.sh` took 126s and
  105s"* with its own sentence conceding the measurement *"has not been re-taken since and the list
  has grown twice"*. Re-taken: **eleven Mill images in 151s** (`deployment.docker.__.docker.build`;
  the job itself builds ten) and the three smoke figures above.
* **`deployment/quickstart/README.md`** opened with **"KUI does not connect to Kafka yet.
  Milestone 0 ... Kafka connectivity is Milestone 1, which is being planned as this is written"** and
  told the reader they would see an empty shell. That has been false for eight milestones and it is
  the first thing a newcomer reads. Replaced with what this stack actually answers, measured with
  `curl` beside the browser, including the two clusters and why the second one is deliberately
  thinner. A second paragraph saying *"nothing reads `kui.clusters` yet"* was false in the same way
  and is corrected in place.

## Findings

One row per finding, in the shape W9-A2 consumes: file, exact mutation, the command that runs it,
what happened, and the case or edit that would close it.

| # | file | exact mutation / observation | command | result | what closes it |
| --- | --- | --- | --- | --- | --- |
| **F1** | `deployment/compose/smoke.sh` | `KUI_PRINCIPAL_KEY` on `kui-topic` changed to a value the gateway does not sign with (one line in `docker-compose.yml:212`) | `./deployment/compose/smoke.sh` | **GREEN — the gate does not notice.** `topic capability: available`, PASSED, exit 0, over a stack where `GET /api/v1/clusters/measured/topics` answers **401 `KUI-UNAUTHENTICATED`**. Six of the nine routed services (`topic`, `consumer`, `message`, `metrics`, `schema`, `ksql`) have **no document read at all** in this script; the strongest thing it says about them is that the gateway can reach them. | One read per routed service, derived rather than written down. The machinery exists and is used exactly once: the script already asks the gateway for the path it publishes for Connect and then calls it. Generalising that to a `for` over `contracts`, requiring a 2xx, would have caught this. **`deployment/**` — mine by ownership, not done here because a closing wave should not add nine new assertions to the one gate its milestone turns on. Filed for `TECH_DEBT.md` through W9-02.** |
| **F2** | `deployment/frontend/Dockerfile:43-50` | none — read, not mutated | `ls frontend/packages \| wc -l` vs `grep -c '^COPY frontend/packages/' deployment/frontend/Dockerfile` | **11 against 8.** The manifest list that feeds `pnpm install --frozen-lockfile` named eight of the eleven workspace packages; `feature-alerts`, `feature-connect` and `feature-ksql` — added in waves 7 and 8 by packets that did not own this file — were absent, and **the image built and carried all eight feature chunks anyway**, so nothing was ever red. Two waves stale, same shape as `ADR-053:255`. | **Closed here**, in my own tree: the three lines added, and the comment above them now carries the two-command check whose numbers must be equal (11 and 11 today) and says what the drift costs when it is not benign. I did **not** establish which later step recovers the omission and I am not going to guess — the honest statement is that the omission was survivable and I have not measured why. |
| **F3** | `deployment/quickstart/README.md` | none — read, not mutated | — | The file's second section opened **"KUI does not connect to Kafka yet. Milestone 0 … Kafka connectivity is Milestone 1, which is being planned as this is written"** and told a newcomer they would see an empty shell. False since M1. A second paragraph said *"nothing reads that section yet"* of `kui.clusters`. **Eight milestones of drift in the first file a newcomer opens**, in a directory that has been on three waves' acceptance lists. | **Closed here.** Both replaced with what the running stack answers, measured with `curl` and printed in the file so the next reader can re-run it. |
| **F4** | `apps/allinone/**` | the ksql push-query relay's mount | `./mill --no-daemon apps.allinone.__.test` | `AllInOneWiringSuite` asserts `/api/v1/clusters/ksql/stream` is served, and **no mutation available inside `apps/allinone/**` can make that particular assertion fail** — the relay is mounted inside `GatewayWiring.over`, which this tree only calls. The roster half *is* gated from here (proved: 11/14 red). | A mutation in `services/gateway/**` deleting the ksql relay from `GatewayWiring`, which is **W9-A1's** tree this wave. Recorded so that "the all-in-one asserts the stream" is not read as "the all-in-one's suite can fail on the stream". |

## What the second cluster actually looks like on the wire — for W9-03

Measured on the first image pair, 2026-09-11, `curl localhost:8080`:

```
$ curl -s localhost:8080/api/v1/clusters | jq -r '.clusters.data[].cluster.id'
quickstart
staging-eu-01

$ curl -s localhost:8080/api/v1/clusters \
    | jq -c '.clusters.data[] | {id:.cluster.id, name:.cluster.name, capability:.capability}'
{"id":"quickstart","name":"Quickstart (local)","capability":{"status":"available"}}
{"id":"staging-eu-01","name":"staging-eu-01","capability":{"status":"available"}}

$ curl -s localhost:8080/api/v1/capabilities \
    | jq -c '[.entries[]|select(.key.cluster=="staging-eu-01")]|map({s:.key.service,st:.state.status})'
[{"s":"alerts","st":"available"},{"s":"cluster","st":"available"},
 {"s":"connect","st":"not_configured"},{"s":"consumer","st":"available"},
 {"s":"ksql","st":"not_configured"},{"s":"message","st":"available"},
 {"s":"metrics","st":"not_configured"},{"s":"schema","st":"not_configured"},
 {"s":"topic","st":"available"}]

$ curl -s "localhost:8080/api/v1/clusters/staging-eu-01/metrics/throughput?range=24h" \
    | jq -c '.throughput.status'
"not_configured"
```

Three things follow and W9-03 should know all three before it writes a case.

1. **The toast will read exactly `Switched to staging-eu-01`**, because the display name is the id
   and `App.tsx:1222` is `Switched to ${name ?? to}`.
2. **`traffic.spec.ts:536` can un-skip**: `other` is now `staging-eu-01` and its throughput answers
   `not_configured`, which is the branch that case was written for.
3. **Four capability rows are `not_configured` on the second cluster and five are `available`.** That
   is deliberate (it is what gives ADR-032's hide-the-row rule a subject on a deployment where the
   cluster beside it has all four), but it means the second cluster's drawer has **no** Schema
   Registry, Connect or ksqlDB row and **no** metrics on its dashboard. A case that switches to
   `staging-eu-01` and then looks for a Connect row will fail, and it will be right to.

## `smoke.sh`, the gate nobody had run since the tenth service — it PASSES

Three consecutive runs on the distributed **eleven-container** stack (`kui-gateway`, `kui-frontend`
and nine services), every backend image rebuilt from this tree by the derivation CI uses
(`./mill deployment.docker.__.docker.build`, 11 images, 151s). Exit 0 all three times, 1m23s / 1m51s
/ 2m00s. Preflight, identical on all three:

```
=== every image this stack names exists on this machine
  images present: kui-alerts:0.1.0-SNAPSHOT kui-cluster:0.1.0-SNAPSHOT kui-connect:0.1.0-SNAPSHOT
  kui-consumer:0.1.0-SNAPSHOT kui-gateway:0.1.0-SNAPSHOT kui-ksql:0.1.0-SNAPSHOT
  kui-message:0.1.0-SNAPSHOT kui-metrics:0.1.0-SNAPSHOT kui-schema:0.1.0-SNAPSHOT
  kui-topic:0.1.0-SNAPSHOT

=== every contract the gateway holds has an address and a container
  contracts routed: alerts cluster connect consumer ksql message metrics schema topic
```

and the body (run 1, verbatim):

```
=== producing a little traffic, so that there is something to measure
  500 records produced to smoke-traffic, 500 read back

=== the broker beside this stack is measurable, and KUI measures it
  the exporter serves all 12 line shapes the reader reads
  the exporter serves the per-topic byte rate the producers card needs
  the broker has taken 32263.0 bytes in since it started
  the broker has served 31081.0 bytes out since it started
  the measured cluster's throughput: ok
  buckets carrying a measured bytesInPerSecond: yes
  buckets carrying a measured bytesOutPerSecond: yes
  buckets carrying a measured recordsPerSecond: yes
  the unmeasured cluster's throughput: not_configured
  latency buckets carrying a measured produceP99Millis: yes
  latency buckets carrying a measured fetchP99Millis: yes
  the request-handlers document carries a figure: yes
  the top-producers document carries a figure: yes
  the record-size document carries a figure: yes

=== the ninth service answers, and says what it evaluated
  the alerts feed: ok
  the alerts feed says when the rules last ran: yes
  the alerts feed names the rules it evaluated: yes
  at least one alert rule read the facts it needs: yes

=== the tenth service answers, on a path the gateway derives rather than one written here
  the connect capability: available
  the gateway publishes the connect read at: /api/v1/clusters/{clusterId}/connect/connectors
  every Connect worker the cluster names answered: ok

=== stopping kui-cluster: one real process dies
=== the gateway survives it and says what is wrong
  cluster capability: unavailable
  alerts capability: available
  connect capability: available
  consumer capability: available
  ksql capability: available
  message capability: available
  metrics capability: available
  schema capability: available
  topic capability: available
  the gateway's own endpoint: disabled
  the interface while a service is down: 200

=== starting kui-cluster again: recovery needs no intervention
  proxied cluster list: ok
  cluster capability: degraded

=== PASSED: the gateway survived the service, reported it, and recovered on its own,
===         and the interface stayed up throughout
```

`cluster capability` after recovery is `degraded` on runs 1 and 3 and `available` on run 2. Both are
accepted by the script's own `await`, which is right: the capability fold carries the memory of the
outage for one poll cycle and which side of that cycle the read lands on is a race, not a defect.

**So the answer to the wave's open question is: it passes, and the eleventh service is the reason it
can.** The preflight's contract set scraped from `ServiceContracts.byService` is nine — `alerts
cluster connect consumer ksql message metrics schema topic` — and the compose file runs exactly those
nine as `kui-*` containers beside the gateway and the interface. The two services added since this
gate last ran, `connect` and `ksql`, each landed a contract, an address in `kui.yaml` and a container
in the same wave, which is the thing house rule 15 was written to make happen and which wave 8 did
without anybody running the check that proves it.

## M1 — the mutation line, quoted

`kui-connect` deleted from `docker-compose.yml` (the service block and the gateway's `depends_on`
entry) and from `kui.yaml` (`kui.gateway.services.connect`). Container count before the run: **0**.

```
=== every image this stack names exists on this machine
  images present: kui-alerts:… kui-cluster:… kui-consumer:… kui-gateway:… kui-ksql:…
  kui-message:… kui-metrics:… kui-schema:… kui-topic:…     <- kui-connect gone from the derivation

=== every contract the gateway holds has an address and a container

FAILED: the gateway holds contracts for [alerts cluster connect consumer ksql message metrics
schema topic ] and this stack runs [alerts cluster consumer ksql message metrics schema topic ].
  CONTRACTED BUT NOT RUN: [connect ]. The gateway will publish these
  services' routes and have nowhere to send them, so every screen behind one 404s against a stack
  that looks healthy. Give each a container in docker-compose.yml and an address under
  kui.gateway.services in kui.yaml -- or, if it is deliberately not routed here, name it in
  UNROUTED_CONTRACTS above with the reason.
NAME      IMAGE     COMMAND   SERVICE   CREATED   STATUS    PORTS

=== tearing down
```

Exit 1. The `docker compose ps` table the failure prints is **empty** and `docker ps` is 0 after the
run: the refusal happened before `up` was called, which is the half of the claim that matters — a
check that started eleven containers and then complained would cost two minutes and a teardown every
time somebody got it wrong. The advice printed is the CONTRACTED-BUT-NOT-RUN branch, which is the
correct one of the two for this mutation.

## M2 — the green one, and it is a real fault

**`smoke.sh` does not notice a service whose principal key disagrees with the gateway's, and the
whole of that service's product is 401 while the gate says `available`.**

One line, in `deployment/compose/docker-compose.yml`, on `kui-topic` alone:

```
      KUI_PRINCIPAL_KEY: ${KUI_PRINCIPAL_KEY:-compose-development-key-not-for-any-real-deployment}
   -> KUI_PRINCIPAL_KEY: a-different-key-than-the-gateway-signs-with
```

`./deployment/compose/smoke.sh` -> **PASSED, exit 0**, with `topic capability: available` printed
twice. Against the same stack, brought up by hand:

```
$ curl -s -w '\nHTTP %{http_code}\n' localhost:8080/api/v1/clusters/measured/topics
{"code":"KUI-UNAUTHENTICATED","message":"Unauthenticated","details":[],
 "correlationId":"5c39339a16d4ba67","timestamp":"2026-09-11T15:41:56.780Z","retryable":false}
HTTP 401

$ curl -s -o /dev/null -w 'HTTP %{http_code}\n' localhost:8080/api/v1/clusters/measured/consumer-groups
HTTP 200        <- the control: a service whose key was not touched
```

Both directions, per house rule 9: with the key reverted and the stack recreated,
`/api/v1/clusters/measured/topics` answers **HTTP 200**. So this is not an equivalent mutant and it
is not a property of the cluster id I happened to use.

**Why the gate cannot see it.** Every assertion `smoke.sh` makes about a service other than `cluster`
is a *capability* read — `/api/v1/capabilities`, which is the gateway's own view of whether the
service is reachable and healthy — plus, for three services, one document read each (`cluster` list,
`alerts` feed, `connect` connectors). The capability probe is a health call and carries no signed
principal, so a mismatched `KUI_PRINCIPAL_KEY` is invisible to it. Six of the nine routed services
(`topic`, `consumer`, `message`, `metrics`, `schema`, `ksql`) have **no document read** in this
script at all, so for six of nine the strongest thing the gate says is "the gateway can reach it".

**The cheapest closure, and it is not mine to write.** One read per routed service, derived the way
the Connect read already is — `smoke.sh` already asks the gateway for the *path* it publishes for
Connect (`the gateway publishes the connect read at: /api/v1/clusters/{clusterId}/connect/connectors`)
rather than writing it down, so the machinery for "ask the gateway what the path is, then call it and
require a 2xx" exists and is used once. Generalising it to every contracted service is a `for` loop
over `contracts` and would have caught this in the preflight-shaped part of the run. It is
`deployment/**` and therefore mine; I have not done it in this packet because it is a new assertion
over nine services in a closing wave whose gate list is already fixed, and a `smoke.sh` that fails
for a tenth new reason in the last wave is a worse outcome than a filed finding. **Filed as a
`TECH_DEBT.md` row through W9-02, owner: whoever owns `deployment/**` next.**

## The last hour — the second image pair (house rule 19, amended)

Wave 8 obeyed the first half of house rule 19 exactly and still shipped a `kui-frontend` carrying
seven feature chunks against a tree that built eight, because the frontend build broke after the
images were made and was repaired by somebody else. So the second pair is built after the building
packets stop moving, and **every browser result in this wave is quoted against it**.

*The rows below are filled by the rebuild; each names the tree it was built from, because "the second
pair" only means anything with a tree hash beside it.*

| | first pair | second pair |
| --- | --- | --- |
| built at | 2026-09-11 18:24 | **2026-09-11 19:36** |
| tree | `a134d74c` + nothing (this packet's own YAML edit landed after) | `a134d74c` + the wave's working tree, **32 files, 2,015 insertions**, `git diff a134d74c \| md5sum` = `cd03d7d99703…`, unchanged across six consecutive three-minute samples from 19:24 to 19:39 at load 1.0–11.2 |
| `kui-allinone:0.1.0-SNAPSHOT` | `ab2468f25417` | **`00c497c06754`** |
| `kui-frontend:0.1.0-SNAPSHOT` | `55bec7277ccf` | **`34db4fcf7948`** |
| feature chunks **in the image** | **8** | **8** |

**The chunk count is read out of the image, not out of `frontend/dist`.** That is the whole lesson of
wave 8: `bundle-shape.mjs` reads `frontend/dist/.vite/manifest.json`, which is the *tree's* answer,
and the container serving `/ui/` had a different one for fourteen hours. The command is

```
docker run --rm --entrypoint sh kui-frontend:0.1.0-SNAPSHOT \
  -c 'ls /usr/share/nginx/html/ui/assets/' | grep -E '^feature-[a-z]+-[A-Za-z0-9_-]+\.js$'
```

and on the first pair it answers eight, including `feature-ksql`:

```
feature-alerts-BGYCiaKe.js   feature-clusters-DfkFvAa-.js  feature-connect-R_GKveWK.js
feature-consumers-bBRjLyOl.js feature-ksql-l4GpaqCO.js     feature-messages-DW4OLMTH.js
feature-schemas-CzWD1d3P.js  feature-topics-D4ns2HYt.js
```

**The second pair is proved current with the tree by three facts, not by having been built late.**

1. The entry chunks in the image are byte-for-byte the ones `pnpm -C frontend build` produced from
   this tree minutes earlier: `index-DczEQZ42.js` and `routing-CIVBjTZ7.js` in
   `frontend/dist/assets/` and in `/usr/share/nginx/html/ui/assets/`.
2. The served entry chunk carries **W9-04's mid-wave kernel change**:
   `grep -c data-state index-DczEQZ42.js` inside the image is **2**, and `data-state` exists in this
   tree only because `frontend/packages/kernel/src/components/StatusPill.tsx` grew it this wave.
   That is the wave-8 defect asked in the affirmative: *does the container serving `/ui/` contain the
   change the tree contains?*
3. The eleven backend images were rebuilt in the same window (`./mill --no-daemon
   deployment.docker.__.docker.build`, 144s), so `kui-message` carries W9-06's masking wiring rather
   than the first pair's.

**And the gate was re-run against them.** A fourth `./deployment/compose/smoke.sh`, on the
second-pair backend images, **PASSED, exit 0** — run on shifted ports (`KUI_PORT=18080
KUI_FRONTEND_PORT=18090`, both of which the script honours) so that the quickstart could stay up for
the browser packets, at load average 6.8. The three runs the acceptance list asks for were on the
first pair and on the default ports; this one says the answer did not change when the message
service grew a masking engine.

## Running stack as this packet freezes

```
kui-quickstart-kui       kui-allinone:0.1.0-SNAPSHOT   00c497c06754
kui-quickstart-frontend  kui-frontend:0.1.0-SNAPSHOT   34db4fcf7948
```

plus the broker, the JMX exporter, the registry, the Connect worker and the ksqlDB server. Two
clusters answering, `quickstart-file-source` RUNNING on the worker, `QUICKSTART_ORDERS` on the
server, `localhost:8090/ui/` 200 and `localhost:8080/api/v1/health/ready` `true`. **Every browser
result in this wave should quote `00c497c06754` / `34db4fcf7948`.**

## What this packet leaves for somebody else

| # | what | who |
| --- | --- | --- |
| 1 | **F1 as a `TECH_DEBT.md` row**: `smoke.sh` passes over a service whose principal key disagrees with the gateway's, because six of nine routed services have no document read in it. Exact edit: a `for` over `contracts` asking the gateway for each service's published path and requiring a 2xx, generalising the one the Connect step already does. Owner: whoever owns `deployment/**` next. | **W9-02** (item 6 is the mechanism) |
| 2 | **The ksql stream relay has no mutation that can defeat its all-in-one assertion from `apps/allinone/**`** — the relay lives in `GatewayWiring`. A local case for it belongs to whoever owns `services/gateway/**`. | **W9-A1**, and `W9-A2` if it wants the closure |
| 3 | The acceptance line `curl -s localhost:8080/api/v1/clusters \| jq -r '.clusters[].id'` as written in `WAVE-09.md` **errors** — `clusters` is an ADR-034 section and the ids are at `.clusters.data[].cluster.id`. Nothing to repair in the tree; recorded so the next reader does not conclude the endpoint is wrong. | nobody — the plan file is deleted at the end of this wave |

## Files this packet changed

```
deployment/quickstart/kui-quickstart.yaml   the second registered cluster
deployment/quickstart/README.md             eight milestones of stale prose, re-measured
deployment/frontend/Dockerfile              the manifest roster, 8 -> 11, with its own check
deployment/compose/smoke.sh                 one re-measured figure in the header, no logic
.github/workflows/ci.yml                    one re-measured figure in a comment, no step
```

No production Scala, no logic in any shell script, and `apps/allinone/**` is byte-identical to
`a134d74c` — the mutation applied to it was reverted from bytes saved before it.

---

# VERIFICATION PASS — W9-01, independent and adversarial

**Written as a stub first (house rule 18) at 19:45 and filled row by row as each run happened.**
Everything below was re-run by the verifier on 2026-09-11 between 19:45 and 20:20, from the same
tree, `uptime` 2.6–5.1 on 16 cores throughout (house rule 20). The quickstart stack was left up
because other packets are reading it, so the distributed stack was brought up on shifted ports
(`KUI_PORT=18080 KUI_FRONTEND_PORT=18090`) — the only deviation from the acceptance list as written,
and it is the same shift W9-01's own fourth run used.

**Headline: the packet's report is accurate.** Every acceptance line re-runs to what it published,
the owned mutation goes red in the verifier's hands, the green mutation reproduces verbatim in both
directions, and all three of the "the image is current with the tree" proofs hold when checked from
the image rather than from the report. Four rules it shipped are not gated by anything, one figure it
wrote this wave is already wrong, and both are below.

## 1. Acceptance, re-run

| # | command | verifier's result | agrees with the packet? |
| --- | --- | --- | --- |
| A1 | `docker compose -f deployment/compose/docker-compose.yml up -d --wait` | **exit 0, 36.7s.** 16 containers, all `(healthy)`; 11 of them KUI images (gateway, frontend, nine services), 5 third-party (`kui-compose-kafka`, `-kafka-connect`, `-kafka-metrics`, `-ksqldb`, `-schema-registry`) | yes (their 62s was a colder run) |
| A2 | `curl -sf .../ui/ >/dev/null && curl -sf .../health/ready` | **exit 0.** `{"ready":true,"checks":[{"name":"process","healthy":true,"detail":null}],"at":"2026-09-11T16:45:56.376Z"}` | yes |
| A3 | `./deployment/compose/smoke.sh` ×3 | **PASSED, exit 0, three times: 66s / 103s / 100s.** Preflight identical all three: `contracts routed: alerts cluster connect consumer ksql message metrics schema topic`, `images present:` ten `kui-*` images. Body identical to the packet's quote, including `500 records produced to smoke-traffic, 500 read back`, all 12 exporter line shapes, `the unmeasured cluster's throughput: not_configured`, and the isolation/recovery sequence | yes. **The degraded/available race is confirmed and it is a race:** the verifier saw `available` on run 1 and `degraded` on runs 2 and 3 — the mirror image of the packet's 1/3/4-vs-2 split. Both accepted by `await_not`, correctly |
| A4 | `./mill --no-daemon apps.allinone.__.test` | **SUCCESS, 35 cases over 6 suites, 0 failed**, exactly the split published: `AllInOneWiringSuite` 14, `SignInSeamSuite` 8, `CapabilitySeamSuite` 5, `InProcessServiceClientSuite` 4, `FaultIsolationSuite` 3, `InProcessStreamingSuite` 1 | yes |
| A5 | `curl -s localhost:8080/api/v1/clusters \| jq -r '.clusters[].id'` — **as written in the plan** | **errors**, exactly as reported: `jq: error (at <stdin>:0): Cannot index string with string ("id")`, exit 5 | yes — the plan's path is wrong, not the endpoint |
| A5′ | `… \| jq -r '.clusters.data[].cluster.id'` | `quickstart` / `staging-eu-01`. Names: `Quickstart (local)` / `staging-eu-01`; both capabilities `available` | yes |
| A6 | `pnpm -C frontend build && node frontend/scripts/bundle-shape.mjs` | **exit 0**, `vite v8.2.2`, built in 492ms; `✓ 8 feature packages, all dynamically imported and none reachable statically from the entry chunk` | yes |
| A7 | capability fold for `staging-eu-01` | `connect/ksql/metrics/schema = not_configured`; `alerts/cluster/consumer/message/topic = available`; `…/staging-eu-01/metrics/throughput?range=24h` → `"not_configured"`; `curl localhost:8083/connectors` → `["quickstart-file-source"]`; `SHOW STREAMS;` → `QUICKSTART_ORDERS` over `orders.v1` | yes, every value |

## 2. The image pair, checked from the image and not from the report

The packet offered three proofs that the second pair is current with the tree. All three hold.

| proof | verifier's measurement |
| --- | --- |
| (a) entry chunks byte-identical to what this tree builds | `md5sum` **inside** `kui-frontend:0.1.0-SNAPSHOT` (`34db4fcf7948`) vs the `frontend/dist` this pass rebuilt: `index-DczEQZ42.js` `f0b7f993fd7393aee7b4fdeb0430647d` on both sides, `routing-CIVBjTZ7.js` `252e10ce6c34957e5b2bf231978df09b` on both sides. The image also lists exactly **8** `feature-*.js` chunks, `feature-ksql-l4GpaqCO.js` among them |
| (b) `grep -c data-state` in the served entry chunk is 2 | **2** inside the image, **2** in the local dist. (It is 4 occurrences on 2 lines; the published figure is `grep -c`, which counts lines, and is right as written) |
| (c) the backend images carry W9-06's mid-wave masking | `kui-message:0.1.0-SNAPSHOT`'s `app.jar` carries **44** `*asking*` entries including `kui/config/MaskingConfig.class` and 3 `ConfiguredRecordMasking` entries; `kui-allinone` (`00c497c06754`) carries 13. Both images were built after W9-06's wiring landed |
| `./mill resolve 'deployment.docker.__.docker.build'` | **11 targets** — the ten the CI job builds plus `allinone`. The figure written into `ci.yml` is right |
| `deployment/frontend/Dockerfile` roster claim "11 and 11" | `ls frontend/packages \| wc -l` = **11**; `grep -c '^COPY frontend/packages/'` = **11**. Right as written |

## 3. THE OWNED RULE — re-applied by the verifier, red in the verifier's hands

| | |
| --- | --- |
| rule | `smoke.sh`'s contract preflight refuses to start the stack, **before any container starts**, when a service the gateway holds a contract for has no container — and names the correct one of the two opposite repairs |
| mutation, applied by hand | deleted the whole `kui-connect:` service block (28 lines) from `deployment/compose/docker-compose.yml`, the gateway's `kui-connect: {condition: service_healthy, required: false}` `depends_on` entry, and the four-line `connect:` address block under `kui.gateway.services` in `deployment/compose/kui.yaml` |
| command | `KUI_PORT=18080 KUI_FRONTEND_PORT=18090 ./deployment/compose/smoke.sh` |
| result | **RED. exit 1**, `FAILED: the gateway holds contracts for [alerts cluster connect consumer ksql message metrics schema topic ] and this stack runs [alerts cluster consumer ksql message metrics schema topic ].` → `CONTRACTED BUT NOT RUN: [connect ]` — the correct branch of the two. **Containers started: 0.** `docker ps` outside the quickstart was 0 before and 0 after, and the failure's own `docker compose ps` table printed a bare header |
| revert | from bytes saved before the edit; `md5sum -c` **OK** on both files, and `git status` no longer lists either. `git -C /home/worxbend/kui diff --stat a134d74c` shows neither file |

**`ownedRuleGenuinelyClosed: true`.**

## 4. The green mutation, reproduced — the fault the gate cannot see is real

Re-applied independently: `deployment/compose/docker-compose.yml:212` (`kui-topic`'s environment),
`KUI_PRINCIPAL_KEY: ${KUI_PRINCIPAL_KEY:-compose-development-key-…}` →
`KUI_PRINCIPAL_KEY: a-different-key-than-the-gateway-signs-with`.

```
$ ./deployment/compose/smoke.sh
  topic capability: available        <- printed twice
=== PASSED                            exit 0, 107s

$ curl -s -w '\nHTTP %{http_code}\n' localhost:18080/api/v1/clusters/measured/topics
{"code":"KUI-UNAUTHENTICATED","message":"Unauthenticated","details":[],
 "correlationId":"1c84db1fb49b3672","timestamp":"2026-09-11T17:01:23.628Z","retryable":false}
HTTP 401
$ curl -s -o /dev/null -w 'HTTP %{http_code}\n' .../clusters/measured/consumer-groups
HTTP 200        <- control, a service whose key was not touched
$ # key reverted, stack recreated:
$ curl -s -o /dev/null -w 'HTTP %{http_code}\n' .../clusters/measured/topics
HTTP 200        <- house rule 9, both directions. Not an equivalent mutant.
```

The packet's F1 is confirmed verbatim, including the correlation-id-bearing envelope and the control.

## 5. NEW UNGATED RULES — mutations this pass applied that left every suite green

One row per finding. All four were reverted from saved bytes; `md5sum -c` OK on every file and
`git diff --name-only a134d74c` is unchanged.

| # | file | exact mutation | suite command | result | the case that would close it |
| --- | --- | --- | --- | --- | --- |
| **V1** | `deployment/quickstart/kui-quickstart.yaml:255` | `- name: "staging-eu-01"` → `- name: "Staging (EU)"`, `id:` left alone | `./mill --no-daemon libs.config.__.test` | **GREEN.** `ShippedConfigurationSuite` 16 cases, 0 failed; whole module SUCCESS. The suite asserts `clusters.map(_.id.value)` and the distinct `bootstrapServers` and **never the display name** — and `frontend/e2e/shell.spec.ts:359` deliberately reads the second cluster's name off `/api/v1/clusters` rather than writing it down ("which is `deployment/`'s to choose and this file's to discover"). So the word the design's M08 capture asserts, *"Switched to staging-eu-01"*, is asserted by **nothing in the tree**, and the packet's own stated reason for choosing the name ("the *name* is the word a browser case reads") is currently false of every browser case | one line beside the two that are already there, in `libs/config/test/.../ShippedConfigurationSuite.scala`: `assertEquals(loaded.clusters.map(_.name), List("Quickstart (local)", "staging-eu-01"))`, with SCREENS-V4 §1 M08 cited as the reason. Owner: whoever owns `libs/config/**` next (W9-06 this wave) |
| **V2** | `deployment/quickstart/kui-quickstart.yaml:76-84` | added a `staging-eu-01:` source under `kui.metrics.sources`, pointing at the same exporter as `quickstart:` | `./mill --no-daemon libs.config.__.test` | **GREEN.** `ShippedConfigurationSuite` re-ran (verified by name in the output: 16 total, 0 failed), whole module SUCCESS. The deliberate *absence* that gives `traffic.spec.ts:536` its subject — a registered cluster whose throughput answers `not_configured` — can be taken away in four lines with every Scala gate green and no browser case red until somebody runs Playwright | in the same suite: assert that exactly one registered cluster has a metrics source, e.g. `assertEquals(loaded.metrics.sources.keySet, Set("quickstart"))` beside the id assertion, with the `traffic.spec.ts:536` citation the YAML comment already carries. Owner: `libs/config/**` |
| **V3** | `deployment/frontend/Dockerfile:72` | deleted `COPY frontend/packages/feature-ksql/package.json ./packages/feature-ksql/` — i.e. re-created the exact defect the packet found and closed, one package instead of three | `docker build -f deployment/frontend/Dockerfile -t kui-frontend:w901verify .` | **GREEN. exit 0**, and the image is *indistinguishable*: 8 `feature-*.js` chunks including `feature-ksql-l4GpaqCO.js`. Nothing in the repository reads this file except four compose files that build it. The packet's repair is three `COPY` lines plus a comment naming a two-command check **that no gate runs** — house rule 13's "an owned rule or a case, never a paragraph" is not met, and the roster can drift again exactly as it did for two waves. **The drift is visible in the build output and nobody looks:** with ten manifests the install step prints `Scope: all 11 workspace projects` and with eleven it prints `Scope: all 12` (pnpm counts the root), so `pnpm install --frozen-lockfile` resolved a workspace of 10-of-11 packages and did not fail | a `build-tests` case, or four lines in `scripts/feature-matrix-check.sh`, comparing `ls frontend/packages \| wc -l` against `grep -c '^COPY frontend/packages/' deployment/frontend/Dockerfile` and failing when they differ — the check the comment already writes out in English. Owner: `build-tests/**` or W9-02's script; the Dockerfile is `deployment/**` |
| **V4** | `deployment/compose/docker-compose.yml:212` | `KUI_PRINCIPAL_KEY` on `kui-topic` set to a value the gateway does not sign with | `./deployment/compose/smoke.sh` | **GREEN — reproduced by the verifier**, see §4. This is the packet's own F1 and it is real, not overstated: six of the nine routed services (`topic`, `consumer`, `message`, `metrics`, `schema`, `ksql`) are covered only by a capability probe, which is a health call carrying no signed principal | the packet's own prescription, and it is right: a `for` over `contracts` asking the gateway for each service's published read path (the machinery exists and is used once, for Connect) and requiring a 2xx. Already filed by the packet to **W9-02 / `TECH_DEBT.md`**; this pass seconds it |

**Controls, to show the mutations above are not simply unreachable.** Two mutations to the same
YAML block went **RED**, so the tooling and the method were working on the runs that came back green:

| control | mutation | result |
| --- | --- | --- |
| C1 | second cluster's `bootstrapServers` → `kafka-eu:9092` | **RED**, `ShippedConfigurationSuite` 1 failed of 16 (`values are not the same`) — the *one broker* half is gated |
| C2 | second cluster entry deleted entirely | **RED**, 1 failed of 16 — the *two registered clusters* half is gated |

So exactly the two properties W9-06's assertion names are gated, and everything else about the entry
— its display name, its deliberate thinness — is not.

## 6. A figure the packet wrote this wave that is already wrong

| # | file | claim as written | measured on the stack the file tells the reader to re-run | what closes it |
| --- | --- | --- | --- | --- |
| **V5** | `deployment/quickstart/README.md`, "What you actually see today, honestly" | "The topics screen lists **12** topics (**18** counting the internal ones the *show internal topics* switch hides) and **4** consumer groups" | **12** ✓ and **4** ✓ — but with internal topics shown it is **16**, not 18: `GET …/topics?showInternal=true&pageSize=100` → 16 items, 4 of them internal (`__consumer_offsets`, `__transaction_state`, `_confluent-ksql-kui-quickstart-_command_topic`, `_schemas`), and `kafka-topics.sh --list` on the broker agrees at 16. 12 + 4 = 16; 18 needs six internal topics and this stack has four | correct `18` → `16`, **and** notice why it survived: `deployment/quickstart/README.md` carries **zero** `<!-- checked: -->` regions and `scripts/feature-matrix-check.sh` reads only the root `README.md`, `docs/FEATURE_MATRIX.md`, ADR-048, `frontend/packages/api/README.md`, `DEPENDENCY_MATRIX.md` and `DECISIONS.md`. The packet replaced eight-milestone-stale prose with new prose that was wrong by two on the day it was written, in the one file it had just finished calling out for exactly that. A `checked:` region over these three counts is W9-02's mechanism and would gate all three |
| **V6** | same paragraph | "**4** consumer groups, **one of them genuinely live and behind**" | Sampled twice, four seconds apart: `analytics-indexer` is the live one (`STABLE`, 1 member) and its `totalLag` is **0**; the group carrying lag is `order-fulfilment` (`totalLag` 12) and it is **`EMPTY`, 0 members**. No group is both live and behind. The sentence is inherited from the text this section replaced, where it was equally unchecked | either soften to what the stack shows — one live group and one with standing lag — or seed a live consumer that stays behind. Low severity; it is a sentence, not a screen |

## 7. Ownership, guard files, orphans

* **Ownership is clean.** `git diff --name-only a134d74c` restricted to this packet's Owns
  (`deployment/**`, `apps/allinone/**`, `.github/workflows/ci.yml`) is exactly the five files the
  packet lists: `ci.yml`, `compose/smoke.sh`, `frontend/Dockerfile`, `quickstart/README.md`,
  `quickstart/kui-quickstart.yaml`, plus its untracked `docs/plan/verification/W9-01.md`. **Nothing
  under `apps/allinone/**` was changed at all**, which is correct — item 5 was a confirmation, not an
  edit. Every other modified file in the tree belongs to W9-02, W9-03, W9-04, W9-05 or W9-06.
* **One edge that cannot be attributed from the tree, and it is the packet's own claim.**
  `libs/config/test/.../ShippedConfigurationSuite.scala` now asserts
  `List("quickstart", "staging-eu-01")` and one distinct `bootstrapServers` — the assertion that
  gates W9-01's headline work, in a file W9-01 does not own. The packet says W9-06 wrote it. The
  circumstantial evidence agrees (mtime 18:40, inside W9-06's masking window; the same file's other
  edits this wave are all masking) but **authorship is not recoverable from an uncommitted tree**,
  and a closer should know that this is the packet's word rather than a measurement. Either way the
  assertion exists, it is green, and C1/C2 above prove it bites.
* **Guard files, checked rather than assumed.** `ShippedConfigurationSuite`'s shipped-file roster did
  not move (the second cluster adds no file — the plan predicted this and it held: 16 cases, same as
  before). `bundle-shape.mjs` reads the filesystem and still finds 8. `smoke.sh`'s two derived
  rosters (`gateway_contracts`, `service_containers`) agree at nine. `docs/FEATURE_MATRIX.md`,
  `DEPENDENCY_MATRIX.md`, `constants.generated.ts`, `10-tokens.css` and `styles/index.css` are
  untouched by this packet. **No guard file enumerates anything this packet changed**, which is the
  finding in V3 and V5 restated: `deployment/**` is almost entirely outside every counting gate this
  repository has.
* **Orphans: none, and the reason is that the packet created no code.** The one new artefact is the
  second `kui.clusters` entry; its callers are `KuiConfigSource` (read at start-up — proved by the
  live `/api/v1/clusters`), `ShippedConfigurationSuite` (two assertions, both shown to bite) and
  `frontend/e2e/shell.spec.ts` + `traffic.spec.ts:536`, which discover it at run time. The three new
  `COPY` lines are consumed by the `pnpm install` two lines below them. The verification file is read
  by W9-A2.
* **Does it do what the packet asked, or merely exit zero?** The stack it describes is the stack that
  runs: 16 containers, 11 KUI images, nine routed services, both seeds live, two clusters on the
  wire, and the capability fold of the thin second cluster is the one the report prints. No panel in
  the acceptance path renders a zero or a bare em dash where a sentence is promised — the second
  cluster's throughput answers the `not_configured` *status*, which is the branch ADR-032 and
  `traffic.spec.ts:536` both want, and the quickstart's own dashboard draws figures from a real
  exporter.

## 8. What a closer should carry forward from this pass

1. **V4 / F1 is the real one** and the packet filed it correctly: `smoke.sh` is green over a stack
   whose entire topics product answers 401. Confirmed independently, in both directions.
2. **V3**: the Dockerfile roster was repaired with a comment. The same drift is one deleted line away
   and the build still succeeds — the repair needs a case, not a paragraph.
3. **V1/V2**: the second cluster is gated on its id and its broker and on nothing else. The design's
   toast word and the deliberate metrics omission — the two reasons the entry is shaped the way it is
   — are both free to move.
4. **V5**: `deployment/quickstart/README.md` has no gate and one of its three fresh figures is wrong
   already (18 → 16).
5. The plan's own acceptance command (`.clusters[].id`) errors, exactly as the packet reported; the
   working path is `.clusters.data[].cluster.id`.
