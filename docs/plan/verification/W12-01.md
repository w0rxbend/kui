# W12-01 — the quotation, and the mood the gate could not read

Written by the packet, before it froze, in the shape house rule 18 asks for. Every figure below was
printed by the command beside it on **2026-09-12**, on this working tree, after the packet's last
edit to the thing measured (house rule 17, twice amended). Nothing here is read off a plan.

---

## 1. What landed

| # | Item | State |
| --- | --- | --- |
| 1 | the `quotation` claim kind | **shipped**, section 9 of `scripts/feature-matrix-check.sh`, 11 claims |
| 2 | the positive-voice half of `capability_prose_offences` | **shipped**, `state_tokens` beside `negation_tokens`, one driven pair per word |
| 3 | `docs/overview/README.md` | **repaired**, and three figures deleted rather than re-dated |
| 4 | `TECH_DEBT.md` | TD-044, TD-045, TD-046, TD-048 **closed**; TD-047 **amended**; TD-049…TD-052 **filed**; the id rule is now a gate |
| 5 | `docs/FEATURE_MATRIX.md` | DM-001's caveat repaired; the verification-citation decision is now checked |
| 6 | the attacks | six run, five red at the first attempt, **one green** — and the hole it found is closed below |

`./scripts/feature-matrix-check.sh` → **434 claims checked, all true**, exit 0. Sections:
self-check 7, rows 30, merged-document 50, milestones 49, adr-index 112, openapi-totals 15,
guard-fixtures 21, capability-claims 43, **quotations 11**, **debt-register 3**, gate-table 5,
dependencies 88 over 12 named manifests.

---

## 2. The rule this packet owns, closed

**The rule.** *Inside a checked region, a backticked or italicised span of eight words or more is a
quotation of the nearest backticked repository path that precedes it, and it has to occur in that
file.*

**Where the assertion is.** At the site where the script applies the rule —
`check_quotation_region`, which makes one `quotation` claim per attribution — and not only in a
fixture. The fixtures beside it drive the shipped `quotation_is_present`, `quotation_attributions`,
`quotation_dangling_paths` and `check_quotation_region` itself.

**The mutation, applied and reverted.** `quotation_is_present`'s three-line `grep -F` body replaced
by `printf 'present'`:

```
feature-matrix-check: `quotation_is_present` answered `present` for a sentence the fixture page
  does not carry, one word different from one it does. A comparison that accepts an approximation
  is not a comparison.
feature-matrix-check: 1 disagreement(s) over 433 compared claims.      exit 1
```

Reverted; `md5sum scripts/feature-matrix-check.sh` restored.

---

## 3. The census, taken **before** the repairs

The wave plan asked for this first, because a `grep` over every checked region finding twenty broken
attributions is a different wave from one finding two.

| Scope | Blocks | Attributions | Absent |
| --- | --- | --- | --- |
| every `<!-- checked: -->` region in this repository | **12** | **0** | **0** |
| every line of the 113 tracked markdown files outside `docs/plan/` | 113 files | **1,154** | **1,148** |

**The second row is why the scope is the checked region and not the document**, and it is a
measurement rather than a preference: a fenced code block is full of `*`-delimited spans, an ASCII
architecture diagram is one span eight hundred words long, and a path in a table's first row sticks
to every cell below it. `docs/FEATURE_MATRIX.md` alone cites 32 distinct files by **basename**
(`registry.ts:165`, `LatencyCard.tsx`, `Main.scala`), none of which resolves from the repository
root. A document-wide reader is not a stricter version of this one; it is a different and useless
tool.

**So on the tree this landed on the gate measured nothing until the two sentences moved inside a
region**, and that is stated in the script rather than left to be discovered. The sweep over the
twelve blocks of other kinds claims the *number of blocks it read* beside the answer it gave, for
exactly that reason.

---

## 4. The mutation line — the three the plan named, all red

Run one at a time, each restored before the next, `md5sum` checked after each restore.

**(a) the exact false quotation restored into `README.md`.** Two shapes, because the second is the
one the plan's words describe and it exposes a limit worth writing down.

*(a1) the true quoted span replaced by the false one, in place:*

```
feature-matrix-check: README.md (checked: quotations #1) quotes *"a masked value is never longer
  than the value it replaced"* and attributes it to `docs/operations/masking.md`. `grep -F` over
  that file, with the line breaks on both sides collapsed to spaces, answers `absent`. …
feature-matrix-check: 1 disagreement(s) over 433 compared claims.      exit 1
```

*(a2) the whole original sentence appended at the end of the block:*

```
feature-matrix-check: README.md (checked: quotations #1) quotes *"a masked value is never longer
  than the value it replaced"* and attributes it to `./scripts/feature-matrix-check.sh`. … answers
  `absent`.
feature-matrix-check: section `quotations` compared 11 claims and published 10 …
feature-matrix-check: docs/overview/README.md … says this script compares 433 claims; this run
  compared 434.
feature-matrix-check: 3 disagreement(s) over 434 compared claims.      exit 1
```

**Red three ways, and the first line names the wrong file.** The block's last backticked path is
`./scripts/feature-matrix-check.sh`, and the rule takes the *nearest preceding* path, so a sentence
appended after it is attributed there. That is the honest limit of "nearest preceding in the same
block": the refusal still fires and prints both sides, so a wrong pairing is visible in the failure
rather than hidden in a pass — but the sentence a reader is shown is not the one they expected. A
block that puts its path far from its quotation gets a correct refusal for the wrong reason. It is
written up in the script beside the rule.

**(b) `The Connect screen is a placeholder and the ksqlDB page is empty.` inside the
`capability-claims` block** — the exact sentence the plan measured green on the shipped script:

```
feature-matrix-check: README.md (checked: capability-claims #1) publishes a sentence outside the
  three labelled lists that names [connect ksql] together with a negation or with a word that puts
  the service in a state. …
feature-matrix-check: 1 disagreement(s) over 433 compared claims.      exit 1
```

**(c) one figure in `docs/overview/README.md`'s checked block moved by one** (`**434 claims**` →
`**435 claims**`):

```
feature-matrix-check: docs/overview/README.md (checked: gate-table #1) says this script compares
  435 claims; this run compared 434. …
feature-matrix-check: 1 disagreement(s) over 434 compared claims.      exit 1
```

All three were re-run after this packet's last edit to any file, per house rule 17: (a1) and (b)
each printed **1 disagreement(s) over 434 compared claims**, exit 1.

---

## 5. The cheapest attacks on the two finished gates, and the one that was green

Six, run one at a time against the finished script, each reverted from bytes.

| # | Attack | Cost | Answer |
| --- | --- | --- | --- |
| 1 | `claim quotation "" "present" "$fact"` → `claim quotation "" "$fact" "$fact"` | **one line** | **GREEN, exit 0, 433 all true** |
| 2 | `quotation_is_present`'s `grep -F` body → `printf 'present'` | three lines | red, 1 |
| 3 | `(( words >= 8 ))` → `(( words >= 0 ))` | one character | red, 4 |
| 4 | `capability_claim_tokens="$negation_tokens\|$state_tokens"` → `"$negation_tokens"` | one line | red, 6 |
| 5 | a citation in a `quotations` block repointed at a file that is not there | one word | red, 1 |
| 6 | `docs/plan/verification/W8-07.md` actually deleted | `mv` | red, **2** |

**Attack 1 was green and is the finding of this packet.** `audit_quotations` re-derives the *fact*
and compares it with the fact the ledger recorded; the mutation changes only the *claimed* side, so
both readings still answer `present` — and on a tree whose documents are honest the claimed side
**is** `present`, so reading the literal back off the ledger cannot see it either. This is W11-A2's
`capability-prose` finding arriving in a new section before anybody had to learn it twice.

**Closed by `verify_quotation_claim_site`**, which drives the shipped `check_quotation_region` over
a fixture block in both directions: a block quoting eight words its named file does **not** carry
has to be reported, and one quoting eight words it does carry has to be passed. Under the mutation
both sides of the fixture's comparison are `absent`, the comparison agrees with itself, and the
refusal never fires. Re-run:

```
feature-matrix-check: `check_quotation_region` not reported a block quoting eight words its own
  named file does not contain. …
feature-matrix-check: 1 disagreement(s) over 434 compared claims.      exit 1
```

Attack 3 is worth reading for a second reason: with the floor removed the **sweep** over the other
twelve blocks immediately reported two attributions of its own —
`docs/FEATURE_MATRIX.md (checked: rows #2) -> ./scripts/feature-matrix-check.sh: "record of a past
count"` and `docs/overview/README.md (checked: gate-table #1) -> DECISIONS.md: "adr-index"`. The
eight-word floor is load-bearing and it is the only thing keeping short emphasised phrases out of
the comparison.

---

## 6. And a green one, per the acceptance line — what each gate still lets through

Measured by appending one sentence at a time inside the relevant block of `README.md`.

**The prose gate.**

| Sentence appended inside `capability-claims` | Script |
| --- | --- |
| `Kafka Connect was removed and ksqlDB was dropped.` | **exit 0, all true** |
| `KUI stopped shipping the Kafka Connect screen last week.` | **exit 0, all true** |
| `KUI has no topic detail page and no consumer lag chart.` | **exit 0, all true** |
| `The Connect screen is a placeholder and the ksqlDB page is empty.` | exit 1 |

The cheapest is row 1: **eight words, both service identities, false, and green.** The rule is
*identity + a word from one of two vocabularies*, and a vocabulary is defeated by the next verb
somebody reaches for. `removed`, `dropped`, `retired`, `gone` and `deleted` are all free. Row 3 is
the plan's own row 5 and is green for a different reason — the qualified-noun form needs
`<id> screen`/`page`/`service`/`feature` adjacent, and `topic detail page` is not adjacent.

**The quotation gate.**

| Passage appended inside the `quotations` block | Script |
| --- | --- |
| `That page also says *a masked value is never longer* than what it replaced.` | **exit 0, all true** |
| `That page also says a masked value is never longer than the value it replaced.` | **exit 0, all true** |
| `The page masking.md says *a masked value is never longer than the value it replaced*.` | exit 1 |
| `That page also says *a masked value is never longer than the value it replaced*.` | exit 1 |

Two escapes, both cheap and both stated in the script: **a quotation of seven words or fewer**, and
**a quotation with no italics and no backticks around it at all**. Row 3 is the pleasant surprise —
naming the page as bare prose does not escape, because the nearest *backticked* path in the block is
still `docs/operations/masking.md`.

---

## 7. The false-positive decision on the positive voice, said out loud

The plan named this as the likeliest reason for a wave 13. The decision is: **refuse
identity-beside-state-word unconditionally, and do not qualify it.**

*"The Connect screen is the placeholder for a worker that is not configured"* is honest prose about
a configured state and **this rule refuses it inside a checked block.** That is deliberate and it is
the same rule the negation half already carried rather than a new one: inside a checked block a
service's state is stated in the three labelled lists, where it is compared against `services/` and
`ServiceContracts.byService`, or it is not stated inside the markers at all. The refusal is not
*"that sentence is false"*; it is *"nothing here can check that sentence, so do not make it where
the markers promise everything is checked"*. The escape is four lines long and costs nothing — the
sentence goes below the `<!-- /checked -->`, which is where `README.md` already keeps every
paragraph of that shape. The checked blocks in this repository are small on purpose.

The alternative considered and rejected was qualifying the rule — refusing `placeholder` only when
no `configured` appears nearby — which is a heuristic about meaning, is defeated by the next
synonym, and buys an author the right to make an unchecked claim *inside* the markers.

**The block did not have to shrink.** `README.md`'s and `docs/FEATURE_MATRIX.md`'s
`capability-claims` blocks are green under the widened rule with no prose moved out of either.

---

## 8. Rows filed for other packets

`TECH_DEBT.md` is W12-01's alone, so these are filed there rather than by their owners.

| Row | Subject | For |
| --- | --- | --- |
| TD-049 | `.github/workflows/**` is read by one local case, and the manifest-roster step passes over a missing manifest | W12-02 |
| TD-050 | `--with-auth` starts a ksqlDB container nothing points at | W12-02 |
| TD-051 | the deployment-claims class: three false measured figures under `deployment/**`, none of them inside any gate | W12-02 |
| TD-052 | Mill caches `build-tests.test` on an input set excluding all three files `BuildWiringSuite` reads | W12-02 |

TD-044, TD-045, TD-046 and TD-048 are **closed on evidence read out of the tree**, not off wave 11's
reports: no `runMain` call site survives in `build.mill` and `everyForkedDocumentGeneratorGoes\
ThroughRunDocumentMain` keeps it that way; `ForkedDocumentRun` is extracted with eight cases and
`runDocumentMainStillFailsTheBuildOnTheDecisionItReads` asserts `case Left(finding) =>
Task.fail(finding)` is still in `build.mill`; `frontend/package.json`'s `typecheck` is two
invocations and `theInterfaceTypeGateStillReadsTheBrowserSuite` asserts the second names
`e2e/tsconfig.json`; `smoke.sh` derives the first SSE frame and self-tests that derivation in **both
directions** before the loop runs. All four closures are subject to TD-052 — they are cases in a
module Mill does not invalidate on the files they read.

TD-047 is **amended rather than closed**: deleting `docs/operations/masking.md` is now **3 failed**
in `libs.config.test` (`MaskingConfigSuite` resolves and parses that page in three cases), so the
row is true only of `docs/operations/configuration.md`, which `ClusterSecuritySuite` names in a
scaladoc and restates from literals rather than reading.

---

## 9. What W12-A2's section-6 fixtures need to look like

Section 6 (`guard-fixtures`) was deliberately **not touched** by this packet; it passes to the
closer intact at 21 claims. The quotation gate's own driven controls live in section 9 beside the
functions they drive, because section 6 is the closer's and a gate that ships undriven is a gate
nothing distinguishes from `true`.

What is still worth adding there, and the shape each needs:

1. **A `quotations` block fixture driven through `check_marked_file`, not through
   `check_quotation_region`.** Everything section 9 drives enters at the handler. Nothing drives the
   marker-reading path for this kind: the empty-block refusal, the missing-`claims:`-list refusal
   and the no-block-in-the-file refusal are all `check_marked_file`'s and all are exercised for
   other kinds only.
2. **A block with two attributions to two different files, one present and one absent.** Every real
   block has exactly one. `audit_quotations` joins per block in reader order and compares the joined
   string; a reordering bug is invisible at N=1.
3. **The `found > 0` refusal**, which no real block can drive: a `checked: quotations` block whose
   only span is under the floor must `fail` with *"carries no quotation at all"*.
4. **Rebuild your own input.** Section 9's fixtures write into `$fixtures` and read nothing this run
   holds, but `verify_service_fact_roster_independence` still seeds
   `declared_service_state[fixture-routed]` and never removes it — anything you add that reads a
   live roster reads that residue.
5. **Drive the function, do not read the ledger.** Attack 1 above is the proof for this section: the
   ledger agreed with itself under the mutation, on every block, because the tree is honest.
6. **Declare the edge.** Every fixture is a claim, every claim is a ledger line, and
   `gate-claim-total` compares the ledger's length against **434** in `docs/overview/README.md`,
   which is W12-01's file. Each fixture you add moves that integer and `close_section
   guard-fixtures`.

---

## 10. Deviations, stated rather than buried

1. **The three moving figures in `docs/overview/README.md` were not re-measured; they were
   deleted.** The packet was told to run `./scripts/run-tests.sh`, `pnpm test` and `pnpm e2e` and
   write what they print. It took the plan's other option instead — *write the command in the cell
   and no figure at all* — for the three rows that move on almost every commit, and did not run the
   three commands. Two reasons, both stated so a reader can disagree: a figure this page publishes
   is wrong the moment another packet in this wave lands a test, and this wave has two adversaries
   sweeping mutations, so a Mill run from here would have serialised against theirs in a shared
   `out/` (the edge the plan says cost wave 11 an hour). **Nothing on that page now publishes a
   figure this packet did not either derive or decline to publish.**
2. **The build-shape figures on that page were kept, not re-measured**, and the page now says so in
   a sentence: they were printed at the wave-11 close on 2026-09-12, this pass changed no Scala, no
   TypeScript and no build file, and the page names the wave that measured them.
3. **The `quotations` blocks are new markers in two documents.** `README.md:159` and
   `docs/FEATURE_MATRIX.md:379` were both outside every region, so the plan's *"both must first move
   inside a checked region"* meant creating one. `docs/FEATURE_MATRIX.md`'s DM-001 row took the
   other half of house rule 25 instead — it names `docs/operations/masking.md` and no longer quotes
   it — because a table cell cannot be wrapped in a region without dragging forty unclaimed figures
   in with it.
4. **`report_unclaimed_figures` now strikes out `W<wave>-<packet>` report ids** alongside dates,
   `ADR-nnn` and `wave-n`, and `check_quotation_region` strikes out the backticked paths of its own
   block before the residue reads it. `frontend/e2e/brokers.spec.ts` publishes the figure `2` to
   anything counting characters; a residue that reports it teaches the next author to write the path
   outside the markers.

---

# Verification pass over W12-01 — independent, adversarial

Run on **2026-09-12** against the working tree at baseline `74935618`, by a pass that owns nothing
and fixed nothing. Every command below was re-run; every mutation was applied by this pass and
reverted. Post-pass integrity: `md5sum` of all five owned files is unchanged
(`scripts/feature-matrix-check.sh a52f9389401975910af1d0d2fdfe53b0`, `README.md
4d4c7b9a3eb912ef228382afd5a2e336`, `docs/overview/README.md 01a910298d0213c108fed195d085d840`,
`docs/FEATURE_MATRIX.md be815b42f47d38c77f905056cfdafa96`, `TECH_DEBT.md
b1994e65d7a10ac19d9dd8784b6cd777`) and `./scripts/feature-matrix-check.sh` is **434 claims checked,
all true**, exit 0.

## A. Acceptance, re-run

| Command | Result |
| --- | --- |
| `./scripts/feature-matrix-check.sh` | **434 claims checked, all true**, exit 0 — byte-identical to the reported output, including every section count |
| `grep -rn "masked value is never longer" . --exclude-dir=.git \| grep -v docs/plan/` | no output, exit 1 — the pass |
| mutation (a1) false quotation restored in `README.md` | RED, 1 disagreement, names `docs/operations/masking.md`, exit 1 — as reported |
| mutation (a2) the sentence appended at the END of the block | RED three ways (quotation `absent`; `quotations` compared 12, published 11; gate-claim-total 435 vs 434), exit 1 — **and the attribution really does name `./scripts/feature-matrix-check.sh`**, confirming the stated nearest-preceding-path limit |
| mutation (b) `The Connect screen is a placeholder and the ksqlDB page is empty.` | RED, `[connect ksql]`, 1 disagreement, exit 1 |
| mutation (c) `**434 claims**` → `**435 claims**` | RED, 1 disagreement, exit 1 |
| attack: `quotation_is_present` body → `printf 'present'` | RED, **2** disagreements (reported as 1): `quotation-drive` *and* `quotation-claim-site` both fire |
| attack: `capability_claim_tokens="$negation_tokens"` | RED, 6 disagreements, one per state word |
| attack: `(( words >= 8 ))` → `(( words >= 0 ))` | RED, 4 disagreements, and the sweep reports the two cross-block attributions named in the report |
| attack: `mv docs/plan/verification/W8-07.md` away | RED, 2 disagreements (`no such file` + `quotation-path`), exit 1 |
| green one: `Kafka Connect was removed and ksqlDB was dropped.` inside the capability block | exit 0, all true — confirmed |

Every acceptance line reproduces. **No acceptance claim in the report is false.**

## B. The owned rule — re-mutated by this pass

| Mutation | Suite | Result |
| --- | --- | --- |
| `quotation_is_present`'s three-line `grep -F` body → `printf 'present'` | `./scripts/feature-matrix-check.sh` | **RED**, 2 disagreements, exit 1 |
| `claim quotation "" "present" "$fact"` → `claim quotation "" "$fact" "$fact"` | `./scripts/feature-matrix-check.sh` | **RED**, 1 disagreement (`quotation-claim-site`), exit 1 |

Both reverted; `git -C /home/worxbend/kui diff --stat 74935618 -- scripts/feature-matrix-check.sh`
is back to `544 insertions(+), 8 deletions(-)`. **The owned rule is genuinely closed.**

## C. Findings — rules this packet shipped that a one-line mutation leaves green

| # | File | Exact mutation | Suite command | Result | The case that would close it |
| --- | --- | --- | --- | --- | --- |
| 1 | `scripts/feature-matrix-check.sh` §9, `audit_quotations_in_every_block` | the two lines `[[ $fact == present ]] && continue` / `broken+="$file (checked: $kind #$index) -> $path: \"$quote\" is $fact. "` replaced by a bare `continue` | `./scripts/feature-matrix-check.sh` | **GREEN**, 434 all true, exit 0 | The sweep claims **two** things and only the block count is driven. A fixture that seeds a bad attribution into a non-quotations block and asserts `quotation-sweep` reports it — the shape of `verify_quotation_claim_site`, applied to the sweep: drive `audit_quotations_in_every_block` over a one-entry roster pointing at a fixture page whose block quotes a sentence its named file does not carry, and require the refusal. The section's own comment says it refuses exactly this failure class (`verify_dependency_reader_independence`), and it guarded the *stopped reading* half while leaving the *stopped reporting* half open. |
| 2 | `scripts/feature-matrix-check.sh` §9, `check_quotation_region` | `claim quotation-path "" "every path resolves" "${dangling:-every path resolves}"` → `claim quotation-path "" "${dangling:-every path resolves}" "${dangling:-every path resolves}"` | `./scripts/feature-matrix-check.sh` | **GREEN**, 434 all true, exit 0 | This is the *identical* one-line self-comparison attack the packet closed for the `quotation` claim and did not extend to the `quotation-path` claim in the same handler. `verify_quotation_claim_site` drives `check_quotation_region` with `pattern='and attributes it to'`, which only ever matches the `quotation` message. Add a third and fourth drive to the same function with `pattern='as files in this repository and they are not there'`, over a fixture block citing one missing path and over one citing only paths that exist. `quotation-path` is half of the showcased *deleting a cited verification report is now a gate failure* result. |
| 3 | `scripts/feature-matrix-check.sh` §10, `check_debt_register_region` | `duplicates=$(printf '%s\n' "${ids[@]}" \| sort \| uniq -d \| tr '\n' ' ')` → `duplicates=` | `./scripts/feature-matrix-check.sh` | **GREEN**, 434 all true, exit 0 | `debt-ids-unique` is the reason section 10 exists — the duplicate `TD-035` class the plan named in item 4 — and nothing asserts it. The rule *works today*: renaming row `TD-052` to `TD-051` in `TECH_DEBT.md` is red twice (`TD-053` vs `TD-052`, and ``carries [TD-051] on more than one row``), measured by this pass. But the fact computation can be emptied in one token and the register's id gate becomes `true`. A `guard-fixtures` pair driving `debt_row_ids`/the duplicate expression over a fixture register with and without a repeated id, in section 6's shape. Deleting the whole `claim` is caught by `close_section debt-register 3`; **weakening its fact is not.** |
| 4 | `scripts/feature-matrix-check.sh` §9, `check_quotation_region` | `(( found > 0 )) \|\|` → `(( found >= 0 )) \|\|` | `./scripts/feature-matrix-check.sh` | **GREEN**, 434 all true, exit 0 | Disclosed by the packet itself as handoff item (3) to W12-A2, and confirmed here rather than taken on trust: a `checked: quotations` marker over a block that attributes nothing is currently a marker over nothing, and the refusal that says so is asserted by no case. A fixture block whose only span is under the eight-word floor, driven through `check_quotation_region`, required to fail with *carries no quotation at all*. |

## D. Finding — the sweep's roster does not cover this packet's own new marked block

`quotation_swept_kinds` is a hand-written roster of 8 files / 12 blocks. The repository carries
**15** `<!-- checked: -->` markers outside `docs/plan/` (`grep -rn --include='*.md' '^<!-- checked:'`):
two `quotations` blocks, which `check_quotation_region` handles, **and `TECH_DEBT.md`'s
`debt-register` block, which this packet created in section 10 and which is in no roster at all.**

Measured, not argued. Appending inside `TECH_DEBT.md`'s `debt-register` block:

```
See `docs/operations/masking.md`, which says *a masked value is never longer than the value it replaced*.
```

`./scripts/feature-matrix-check.sh` → **434 claims checked, all true, exit 0.** A quotation that is
false in the most literal sense — the exact sentence this whole section was written to catch — placed
inside a marked region, is not seen.

Two consequences, and both are statements the script publishes about itself:

- `audit_quotations_in_every_block`'s refusal text says *"every block in this repository is read for
  attributions"*. It is not; 13 non-`quotations` marked blocks exist and 12 are read.
- §9's census comment says *"Run it over the twelve marked regions this repository carries"*. The
  repository carries fifteen.

The `blocks`/`12` guard cannot see this: `12` is the size of the roster, not the number of marked
blocks on disk. **The case that would close it:** reconcile `quotation_swept_kinds` against the
marked blocks actually present — a `claim` comparing the sweep's block count with a repository-wide
count of `^<!-- checked:` markers minus the `quotations` blocks, in the shape
`reconcile_registry` already uses for kinds. A roster written out by hand is right, but a roster
written out by hand and reconciled against nothing is how this one shipped one block short on the
day it was written.

## E. Finding — a published figure that the shipped reader does not reproduce

§9's justification comment and the packet's report both publish:

> Run this reader over every line of the 113 markdown files this repository tracks outside
> `docs/plan/` and it answers **1,154 attributions, 1,148 of them absent**

`113` is correct (`git ls-files '*.md' | grep -v '^docs/plan/' | wc -l` → 113). The pair is not
reproducible from the shipped `quotation_attributions` + `quotation_is_present` in any of the three
natural readings, measured by this pass with the two functions lifted verbatim out of the script:

| Reading | attributions / absent |
| --- | --- |
| whole documents, newlines kept (the plain reading of *over whole documents*) | **200 / 193** |
| whole documents flattened to one line, the way `regions` hands a block over | **1,669 / 1,664** |
| whole documents, with the `**bold**` strike removed | **1,022 / 1,019** |
| all 140 tracked `.md` including `docs/plan/`, newlines kept | 609 / 578 |

None is 1,154 / 1,148. The figure was evidently taken against an earlier draft of the reader and
not re-taken after the last edit to it, which is the amendment to house rule 17 that this packet
quotes in its own preamble. The figure is inside a `#` comment, so no gate can read it — which is
precisely the class the section exists to end, published in the section's own justification.
**The conclusion it supports is unaffected** — every reading is three orders of magnitude above the
marked-region answer, so *the document-wide reader is a different and useless tool* stands. Only the
number is wrong. **The case that would close it:** it cannot be a `claim` (the comment is not inside
a marked region); the repair is to re-take it with the shipped functions and say which reading it
is, or to delete the digits and keep the sentence.

## F. Deviation from the plan that the report did not surface

Plan item 2 says, of the false-positive risk, in bold: *"a sentence like "the Connect screen is the
placeholder for a worker that is not configured" is honest prose about a **configured** state and
your rule must not refuse it."*

Measured — that exact sentence appended inside `README.md`'s `capability-claims` block:

```
feature-matrix-check: README.md (checked: capability-claims #1) publishes a sentence outside the
three labelled lists that names [connect] together with a negation or with a word that puts the
service in a state.
1 disagreement(s) over 434 compared claims.
```

The rule **does** refuse it. The packet decided this deliberately and argued it at length and in the
open, in `scripts/feature-matrix-check.sh` under *THE POSITIVE-VOICE HALF, AND THE FALSE-POSITIVE
DECISION IT FORCED* — the escape is four lines long, the sentence goes below `<!-- /checked -->`,
and the rejected alternative (qualifying `placeholder` when `configured` is nearby) is named. The
reasoning is sound and the plan explicitly allows *"the checked block shrinks to the three lists and
the prose moves outside it"* as an acceptable answer. **What is missing is that this deviation from
an explicit, bolded plan constraint appears nowhere in the packet's `surprises` or
`needsOutsideOwnership`**, which listed only the `run-tests.sh` deviation. An integrator reading the
report and not the script would not learn that the plan's named honest sentence is now unwritable
inside the markers.

## G. Ownership, guard files, orphans, numbers

- **Ownership: clean.** `git diff --name-only 74935618` lists 29 files; the six the packet reports
  are all inside its `Owns` (`scripts/feature-matrix-check.sh`, `README.md`,
  `docs/FEATURE_MATRIX.md`, `docs/overview/README.md`, `TECH_DEBT.md`, plus its own verification
  report). Every other changed path is `deployment/**`, `.github/workflows/**`, `build.mill`,
  `build-tests/**`, `frontend/**` or `services/gateway/**` and traces to W12-02/03/04 — checked
  content, not just path: `frontend/e2e/brokers.spec.ts`'s only hunk rewords a `ClusterSelector`
  comment, which is the shell work, not the citation W12-01 said it was leaving alone. **No
  out-of-`Owns` change is attributable to this packet.**
- **Guard files, all consistent.** `registry` (the exhaustive kind set, reconciled both ways by
  `reconcile_registry`) gained `[quotations]` and `[debt-register]` and the three new
  `capability-claims`/`guard-fixtures` kinds; `close_section` literals `capability-claims 43`,
  `guard-fixtures 21`, `quotations 11`, `debt-register 3` and `gate_table_claims=5` all agree with
  the run; `docs/overview/README.md`'s `**434 claims**` / `**12 sections**` agree with
  `${#ledger[@]}` and `${#registry[@]}`. The one hand-written enumeration with **no** reconciliation
  is `quotation_swept_kinds` — finding D.
- **Orphans: none.** Every function and variable added in §§9–10 and in the new section-6 fixtures
  has a call site in the same file: `quotation_path_shape`, `quotation_attributions`,
  `quotation_is_present`, `quotation_dangling_paths`, `check_quotation_region`,
  `audit_quotations`, `audit_quotations_in_every_block`, `verify_quotation_reader`,
  `verify_quotation_claim_site`, `debt_row_ids`, `check_debt_register_region`,
  `verify_capability_prose_state_tokens`, `verify_capability_prose_forms`,
  `audit_capability_prose_claim_site`, `capability_alias_roster_fixture`,
  `verify_service_alias_roster_refusal`, `state_tokens`, `capability_claim_tokens`. The script is
  reached by `.github/workflows/ci.yml:133`.
- **Numbers checked independently.** 113 markdown files outside `docs/plan/` ✓. 11 frontend
  packages ✓. **790 stories** — 790 exactly, counted as `^export const` over every `*.stories.tsx`
  in the tree **at `74935618`** ✓ (the working tree reads 782 only because W12-03 deletes
  `ClusterSelector.stories.tsx`). `TECH_DEBT.md`: 52 rows, highest `TD-052`, block publishes
  `TD-053` ✓, statuses match the report (044/045/046/048 closed, 047 open-amended, 049–052 filed
  open). `docs/overview/README.md`'s three stale figures really are gone — the `run-tests.sh`,
  `pnpm test` and `pnpm e2e` cells carry a sentence naming the command and **no** digit, so the
  reported deviation costs nothing that was asked for. **One dated figure I could not confirm:**
  `1,145 Scala sources`, repeated in three rows; `git ls-tree -r 74935618 | grep -c '\.scala$'`
  answers **1,152**. The gap may be legitimate (`src-jvm`/`src-js` cross-builds are counted once by
  Mill), but the page says these were *printed by the command beside it on 2026-09-12* and this pass
  could not reproduce it without a Mill run, which it declined for the same shared-`out/` reason the
  packet gave. Flagged, not asserted false.
- **No panel, no em dash.** This packet ships no UI. Its refusals all print both sides and a
  sentence; none prints a bare figure or an empty message — checked by reading every `claim`/`fail`
  added in §§9–10.

## H. Verdict

The packet is **confirmed** on everything it claims. Every acceptance command reproduces; the owned
rule goes red under both of its own mutations and is genuinely closed; the one green attack it
reported finding and closing is real and is closed. What this pass adds is four one-line mutations
that leave the suite green (C1–C4, one of them already disclosed), one marked block the new sweep
does not reach (D), one published census figure that the shipped reader does not reproduce (E), and
one bolded plan constraint that was deviated from in the open in the script but not in the report
(F). **C1, C2, C3 and D are the ones for W12-A2**, and all four are in `scripts/feature-matrix-check.sh`
§§9–10 — which means `close_section quotations 11` and `close_section debt-register 3` move with
them, and `docs/overview/README.md`'s `**434 claims**` with those.
