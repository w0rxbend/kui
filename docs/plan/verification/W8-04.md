# W8-04 verification — `feature-connect` and the browser evidence M9 is blocked on

Verified 2026-09-11 against `de70b99` + the working tree, on the stack W8-10 published:
`kui-allinone:0.1.0-SNAPSHOT` **`1d2c9b08ae2e`** and `kui-frontend:0.1.0-SNAPSHOT` **`d7d94c760bec`**
(both confirmed by `docker images`), with `quickstart-file-source` on the quickstart Connect worker
(confirmed by `curl -s http://localhost:8083/connectors`).

Every mutation below was applied to bytes I saved myself, one at a time, run, and reverted by copying
the saved bytes back; `md5sum -c` is clean and
`git diff --stat de70b99 -- frontend/packages/feature-connect frontend/e2e/connect.spec.ts` is back to
the packet's own **9 files, 746 insertions, 161 deletions**.

## Acceptance, re-run

| command | packet said | I measured |
| --- | --- | --- |
| `pnpm -C frontend test packages/feature-connect` | 75 passed (4 files) | **75 passed (4 files)** — matches |
| `pnpm -C frontend typecheck` | exit 0 | **RED.** 19+ errors, **all in `packages/feature-ksql`** (W8-05 mid-flight): `wire.test.ts(19,3) TS2305: Module "./wire.js" has no exported member 'decodeRowFrame'`, `TS2339 Property 'objects' does not exist on type 'KsqlObjects'`, … **Zero errors name `feature-connect`** (`grep -c feature-connect` → 0). Not this packet's, but the gate is red as I write. |
| `node frontend/scripts/boundaries.mjs` | 421 files / 11 packages | **422 files in 11 packages: no boundary violations** — one more file than the packet saw; sibling drift, no violations |
| `pnpm -C frontend build && node frontend/scripts/bundle-shape.mjs` | fails, on W8-06's `@import "../../feature-ksql/styles/82-ksql.css"` with the styles dir missing | **Still fails — but that reason is gone and the packet's diagnosis no longer holds.** `frontend/packages/feature-ksql/styles/82-ksql.css` now exists and `registry.ts:187` now carries the `@kui/feature-ksql` dynamic import. The build now dies in rolldown on `feature-ksql/src/index.ts(19)` *Missing export* `KSQL_RESULT_SECTION_KEY`. `bundle-shape.mjs` then dies `ENOENT frontend/dist/.vite/manifest.json`. **This packet's half of that gate — `feature-connect` present as a dynamic entry — is therefore still unverified by anyone.** |
| `pnpm -C frontend e2e e2e/connect.spec.ts` | 4 passed, 0 skipped | **4 passed (2.7s), 0 skipped, 0 failed** — confirmed, twice-named cases all run |

Old-spec baseline arithmetic checks out: `git show de70b99:frontend/e2e/connect.spec.ts` has exactly
four `test(` and two `test.skip(` guards, so the packet's `1 failed, 2 skipped, 1 passed` is the
internally consistent figure and the plan's "one failure and three skips" was the looser one.

Case-count claim checks out: `^\s*(it|test)\(` over the four suites at `de70b99` = **62**
(24 + 19 + 18 + 1); today = **75** (36 + 20 + 18 + 1).

Column claim (item 17) checks out: `awk 'length>100'` returns **0** for all nine touched files, and 0
for the seven package files it did not touch.

Items 15 and 16 check out: `connect.test.tsx:601` reads `ErrorCodes.ConnectRebalancing`,
`wire.ts:169` reads `ReasonCodes.Starting`, and `NotDescribedPanel`'s doc now argues *for* the pill.

## The owned rule (item 8) — genuinely closed

`ownedRuleGenuinelyClosed: true`. Applied by me, not quoted from the report.

| file | mutation I applied | suite | result |
| --- | --- | --- | --- |
| `frontend/packages/feature-connect/src/ConnectorPanel.tsx:87` | `busy={props.pending !== undefined}` → `busy={false}` | `pnpm -C frontend test packages/feature-connect` | **2 failed \| 73 passed.** `connect.test.tsx > a command in flight > marks the connector it was sent against, and no other card on the page` and `> makes that connector's controls swallow a second press: one click, one POST`. Reverted; `md5sum -c` OK. |

## Findings — one row per finding

Each row is a mutation I applied in the source and a suite that stayed **green** under it, unless the
row says otherwise.

| # | file | exact mutation applied | suite command | result | the case that would close it |
| --- | --- | --- | --- | --- | --- |
| **F1** | `frontend/packages/feature-connect/src/wire.ts:304` (`decodeConnector`) | `runningTasks: asNumber(record["runningTasks"]) ?? 0,` → `runningTasks: asNumber(record["runningTasks"]) ?? tasks.length,` | `pnpm -C frontend test packages/feature-connect` | **75/75 GREEN — ungated, and not in the packet's `knownUngated`.** This is the mirror image of the plan's item 4, which the packet closed on `taskCount`. Under it a connector whose `runningTasks` the service omits renders *"3 of 3 tasks running"* over three tasks whose states nobody checked — a fabricated all-green on the one figure §3.14 puts in front of an operator, and `RESTARTING` counts as running in it. | A `wire.test.ts` case decoding a connector entry with `tasks: [3 entries]` and **no** `runningTasks` key, asserting `decoded.runningTasks === 0` and not `3`; plus a `connect.test.tsx` case asserting the rendered `connector-tasks` sentence reads `0 of 3 tasks running.` |
| **F2** | `frontend/packages/feature-connect/src/ConnectorPanel.tsx:211` (`taskSentence`) | deleted the line `if (connector.taskCount === 0) return "This connector has no tasks.";` | `pnpm -C frontend test packages/feature-connect` | **75/75 GREEN — ungated, undisclosed, and it is a *regression this packet introduced*.** The panel then renders **`0 of 0 tasks running.`** — the bare zero the design forbids. `model.test.ts` at `de70b99` carried `it("says a connector has no tasks rather than drawing 0/0")`; correction item 14 deleted `taskCaption()` **and that case with it**, and the sentence it moved the promise to has no case of its own. The packet's *"`ConnectorPanel.taskSentence` says the same two figures … as well"* is true about the words and false about the gate. | The deleted case, re-aimed: a `connect.test.tsx` case mounting a connector with `taskCount: 0, runningTasks: 0, tasks: []` and asserting `connector-tasks` reads `This connector has no tasks.` and does **not** contain `0 of 0`. |
| **F3** | `frontend/packages/feature-connect/src/model.ts:188` (`failureReason`) | deleted the line `if (!connector.failed) return undefined;` | `pnpm -C frontend test packages/feature-connect` | **75/75 GREEN — ungated, undisclosed.** Lower blast radius than F1/F2 today, and that is the whole finding: the only thing keeping a reason off a healthy card is `ConnectorPanel`'s `<Show when={props.connector.failed}>`, one level above. `failureReason` is exported from `index.tsx` as public API with a doc that promises *"or `undefined` when it is not failing"*, and nothing holds it to that promise — a second caller, or that `Show` being widened, draws a stale `reason` under a RUNNING connector. | A `model.test.ts` case: a connector with `failed: false` and a non-empty `reason` (the service does send both — `reason` survives a recovery in the golden's shape) asserting `failureReason(connector) === undefined`. |
| **F4** | `frontend/e2e/connect.spec.ts:214` | none — read, not mutated | `pnpm -C frontend e2e e2e/connect.spec.ts` | **The state assertion in *"every connector the API named is a card, in the state the API reported"* proves nothing on this stack.** `card` is the panel (`[data-testid="connector"]`), which contains both the pill **and** `connector-tasks`. For the only connector deployed — `quickstart-file-source`, RUNNING, 1/1 — the panel text carries *"1 of 1 tasks running."*, so `toContainText(wordFor("RUNNING"))` = `toContainText("running")` is satisfied by the task sentence **whatever the pill says**. A product regression drawing an unreported state as `running`, or drawing no pill at all, passes this case. | Scope the locator to the pill: `card.locator(".kui-connector__head .kui-pill")` (or a `testId` added to `ConnectorCard`'s `StatusPill`) and assert `toHaveText(wordFor(state))` — exact text, on the pill, not `toContainText` on the panel. |
| **F5** | `frontend/e2e/connect.spec.ts:257` (`wordFor`) | none — read, not mutated | — | **An uncompared copy of the kernel's words, in a file whose whole thesis is that copies get read off disk.** `wordFor` transcribes `connectorChip`'s five labels (`ConnectorCard.tsx:60-73`). It is **correct today** — I compared all five, including `RESTARTING → UNKNOWN → "state not reported"`. The file's own comment concedes it ("written out rather than imported"), and the shape is exactly the one house rule 12 exists to end: the *wire* is now read off disk and the *words* are still hand-copied. | Either give `ConnectorCard`'s pill a stable `data-state` attribute the spec can compare to the wire's state word without knowing any label (kernel change, W8-06's tree), or a `packages/kernel` case that pins all five `connectorChip` labels by literal so a rename reddens in the language that owns them before it reddens a browser. |
| **F6** | `frontend/e2e/connect.spec.ts` | none — read, not mutated | — | **The rewrite dropped a case.** `de70b99`'s spec had four cases; the new file has four; but the old *"a deployment with no Connect worker says so, and offers nothing to retry"* is gone, replaced by *"the gateway answers a document shaped like the service's own golden"*. The old case only ever ran on a stack with no worker configured, so nothing measurable was lost on this stack — recorded so W8-A3 knows the not-configured browser path is now covered by **no** e2e case in any deployment. | Nothing owed if the wave accepts it (house rule 6 argues against asserting absence). If it is wanted back, it belongs in a second Playwright project against a no-Connect stack, not as a `test.skip` in this file. |
| **F7** | `frontend/packages/feature-ksql/src/index.ts:19`, `frontend/packages/feature-ksql/src/wire.ts` | none — re-ran the packet's own acceptance | `pnpm -C frontend typecheck`; `pnpm -C frontend build && node frontend/scripts/bundle-shape.mjs` | **Two of W8-04's five acceptance lines are red for reasons that are not W8-04's, and the reason the packet *named* is no longer the reason.** The `@import "../../feature-ksql/styles/82-ksql.css"` blocker it filed under `needsOutsideOwnership` is **resolved** — that file exists and the registry entry landed — and the build now dies on `feature-ksql`'s missing exports instead. **W8-04's own half of the bundle-shape gate (`feature-connect` as a dynamic entry) has been verified by nobody since the build broke.** | Owned by W8-05. The line to re-run once its `wire.ts` exports settle is `pnpm -C frontend build && node frontend/scripts/bundle-shape.mjs`, and the figure to re-confirm is the packet's `dist/assets/feature-connect-*.js` entry under the manifest's `dynamicImports`. |

### The packet's own three disclosed ungated rules — all three re-measured, all three true

Filed here so they are in **one** file, per house rule 18. I applied each myself.

| file | mutation | suite | result |
| --- | --- | --- | --- |
| `frontend/packages/feature-connect/src/ConnectorPanel.tsx:89-93` | replaced the `{...cardActions({…})}` spread with `onPause={() => props.onCommand?.(toggle())}` / `onRestart={() => props.onCommand?.("restart")}` / `{...(props.refusal === undefined ? {} : { actionsDisabledReason: props.refusal })}` | `pnpm -C frontend test packages/feature-connect` | **75/75 GREEN.** Confirmed. The rule moved into `model.ts` and is gated there; nothing holds the panel to *calling* it. Closing it needs a `ConnectorCard` that refuses both props at once (`frontend/packages/kernel/src/components/ConnectorCard.tsx`, W8-06's tree). |
| `frontend/packages/feature-connect/src/ConnectorList.tsx:256` (`answered`) | `return worker.page.kind === "ok" \|\| worker.page.kind === "stale";` → `return worker.page.kind === "ok";` | same | **75/75 GREEN.** Confirmed. Closing case: mount a single **stale** worker whose page is empty and assert `[data-testid="connect-empty"]` is present. |
| `frontend/packages/feature-connect/src/wire.ts:313` (`decodeTask`) | `id: asNumber(record["id"]) ?? -1,` → `?? 0` | same | **75/75 GREEN.** Confirmed. Closing case: decode a task entry with no `id` and assert the panel names no task, or drop an unidentified task the way `decodeConnector` drops a nameless connector. |

### Rules I mutated that are genuinely held (no action owed)

Recorded so the next verifier does not spend the runs again. All `pnpm -C frontend test packages/feature-connect`.

| file | mutation | went red |
| --- | --- | --- |
| `model.ts` `pillState` | `default: return "UNKNOWN"` → `"RUNNING"` | 1 — *draws a restarting connector as unknown and never as running* |
| `model.ts` `taskSegment` | `return "unknown"` → `"running"` | 1 — *draws a restarting task's segment as unknown and never as running* |
| `model.ts` `cardActions` | refusal arm returns **both** the reason and the two handlers | 1 — *hands a refused connector the sentence and no handler of any kind* |
| `model.ts` `connectVoice` | `filter((one) => one.failed)` → `filter((one) => one.state === "FAILED")` | 3 |
| `model.ts` `connectVoice` | `const partial = silent > 0 ? ", from the workers that answered" : ""` → `""` | 2 |
| `model.ts` `failureReason` | `task: owner?.id` → `task: undefined` | 1 — *…with the task that reported it* |
| `wire.ts` | `REBALANCING_REASON_CODE = ReasonCodes.Starting` → `ReasonCodes.NotConfigured` | 3 |
| `wire.ts` `decodeConnector` | `failed: record["failed"] === true` → `connectorState(record["state"]) === "FAILED"` | 9 |
| `wire.ts` `decodePage` | `const items = asArray(root["items"]); if (items === undefined) return Unreadable;` → `?? []` | 1 — *refuses an ok section carrying no item list, rather than answering an empty page* |
| `ConnectorPanel.tsx` | `<Show when={props.refusal}>` → `<Show when={undefined}>` | 1 — *says which Connect cluster it is refusing, in words and not only in a tooltip* |
| `ConnectorPanel.tsx` | `toggle: toggle()` → `toggle: "pause"` | 1 — *offers Resume to a paused connector and sends the resume path* |

## Ownership

`git diff --name-only de70b99` lists 84 files. Everything attributable to W8-04 is inside its `Owns`:
the nine files it declared, and no others. **No ownership violation.** The other 75 are other packets'
(`services/ksql`, `feature-ksql`, the shell, `build.mill`, the deployment files, `SCREENS-V4.md`,
`vitest.config.ts`, `kernel/styles/index.css`) and none of them carries a Connect change.

## Orphans

- `model.cardActions` / `type CardActions` — one production caller, `ConnectorPanel.tsx:89`; three
  cases in `model.test.ts`. **Not an orphan.**
- `model.THROUGHPUT_NOT_MEASURED` — no product caller and, as the doc argues, cannot have one. Read by
  `connect.test.tsx:387`, which compares it to the kernel card's **rendered** text. That is a real
  join and it reddens if either copy moves. Still exported from `index.tsx` as public API with no
  product reader; acceptable given the argument, recorded because it is the second such export.
- `model.taskCaption` — deleted. No reference survives anywhere in `frontend/` or `docs/` except the
  tombstone comment at `model.ts:156` and the plan. **Clean deletion** — with the caveat in **F2**.
- `e2e/connect.spec.ts`'s `goldenDocument`/`workersIn`/`connectorsIn`/`subjectOf`/`wordFor` — all
  called within the file.
- `services/connect/contract/test/resources/golden/connectors-response.json` — now read by the browser
  suite as well as by `wire.golden.test.ts`. The three goldens the package mirrors
  (`connectors-response`, `connectors-partial`, `connectors-not-configured`) are **byte-identical**
  to `frontend/packages/feature-connect/src/documents/` (`cmp` clean); `connector-operation.json` has
  no browser twin and needs none.

## Guard files

- `frontend/scripts/bundle-shape.mjs` reads its feature roster **off the filesystem**, so nothing went
  stale when the eleventh package landed. No hard-coded list to update.
- `build-tests`' `CssReferencesSuite` / `kernel/styles/index.css:172` — `81-connect.css` kept its name
  and its one `@import` line. Untouched.
- `frontend/packages/api/src/constants.generated.ts` — read from, never written to (house rule 3 kept).
- `e2e/connect.spec.ts`'s `STATUSES` is a hand-written five-element mirror of ADR-039's section
  statuses. It fires in the right direction (an unknown status fails the case), so it is a guard and
  not a stale roster — recorded because it is the second transcription in that file after `wordFor`
  (**F5**).
- `frontend/README.md:36` lists the feature packages in prose and is checked by nothing. Not this
  packet's and not moved by it.

## Verdict

The packet's headline claims are true as written: the spec is genuinely rewritten against the
service's own golden, all four cases run against a real stack with a real connector, the owned
mutation is genuinely red, the eleven table rows I sampled are genuinely held, the two extra rules it
found are real, and its three disclosed ungated rules are all three accurately measured.

Three rules are ungated that it did not disclose — **F1**, **F2**, **F3** — and **F2** is a promise
that was gated at `de70b99` and is not gated now, lost inside correction item 14. **F4** is a browser
assertion that cannot fail on the state this stack deploys.
