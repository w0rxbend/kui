# W10-04 — verification pass

**Packet:** Masking: reachable, documented, and not defeated by writing 20 twice
**Verified:** 2026-09-11, against `ee81a440` as the baseline.
**Verdict:** **CONFIRMED.** Every acceptance command re-runs green, every stated number I could
check is correct, the owned rule goes red under its own mutation applied by me, and the sixteen
changed files are all inside the packet's `Owns`. Four green mutations survive; two of them the
packet found and filed itself, and two are new. House rule 18.

---

## 1. Acceptance, re-run

| Command | Result |
| --- | --- |
| `./mill --no-daemon -k libs.securityCore.jvm.test + libs.config.test + services.message.__.test` | exit 0, **`1595/1595, SUCCESS`**, 25s. Tallied from the per-suite lines: **60 suites, 728 cases, 0 failed** — the packet's figures exactly. |
| `./mill --no-daemon checkArchitecture` | exit 0, **`233/233, SUCCESS`**, 2s, `checkArchitecture: 195 modules, 10 rules, no layering violations`. |
| `./scripts/run-tests.sh` | exit 0, **`81 modules (81 with tests), 4328 test cases, all passing.`** |

Per-suite counts all match what was claimed: `MaskingConfigSuite` 21, `MaskingEngineSuite` 28 (JVM),
`ConfiguredRecordMaskingSuite` 15, `MessageWiringSuite` 6, `MaskingMetricsSuite` 6,
`ShippedConfigurationSuite` 16.

> **A note for W10-A2 about the first `run-tests.sh` run, so the transcript is not misread.** My
> first invocation exited **1** with `StreamProxySuite` red on two cases. That was not this packet:
> a concurrent verifier had `(pieces.last, pieces.init)` → `(Vector.empty, pieces.init)` live in
> `services/gateway/api/src/kui/gateway/api/StreamProxy.scala` at the time, a file W10-04 does not
> own. Once that agent reverted, the re-run was clean at 4328. **Anyone running a whole-tree command
> in this wave should check `git diff` for another packet's live mutation before attributing a red.**

## 2. The owned rule — re-mutated by me, red both halves, reverted

| | |
| --- | --- |
| **Rule** | a keep whose two ends together reveal the field is refused, and the engine masks the whole value when the kept ends leave nothing |
| **Mutation A** | `libs/config/src/kui/config/MaskingConfig.scala:235`, `if kept > MaxKeep then` → `if kept > MaxKeep * 4 then` |
| **Result** | **RED, exactly one case.** `libs.config.test` `395/395` → `1 FAILED`; `MaskingConfigSuite` *"a keep whose two ends together reveal the field is refused"*, `munit.FailException` at `MaskingConfigSuite.scala:31`, *"expected the load to fail, but it succeeded"*. |
| **Mutation B** | `libs/security-core/src/kui/security/masking/MaskingEngine.scala:260`, `if maskedCount <= 0 then replacementFor(chars, total)` → `then text` |
| **Result** | **RED on four cases**, at exactly the lines claimed: `MaskingEngineSuite.scala:127`, `:145`, `:161` and `MaskingConfigSuite.scala:360`. |
| **Reverted** | Yes. `md5 3148d627d99b17f91cbf987c5e5c8af2` (`MaskingConfig.scala`) and `29e6dc55cbbfb445e7d0926a50a44153` (`MaskingEngine.scala`) — byte-identical to the packet's stated post-work state. |

The refusal's **message** is asserted too, not merely its existence: `MaskingConfigSuite:315-317`
pins the reported key as `kui.clusters.0.masking.0` and requires the sentence to contain
`keeps 40 characters` and `at most 20`. A gutted message is red. That is better than the packet
claimed for itself.

I also re-measured the packet's house-rule-17 claim about the shipped file, because it is the
strongest structural statement it makes. Deleting **one** `UnknownKeys.Known` entry
(`List("kui", "clusters", "*", "masking", "*", "topicValuesPattern")`, `KuiConfigSource.scala:2892`)
reddens **exactly five** cases, **three** of them because `kui-quickstart.yaml` itself stops loading
(`is not a KUI configuration key`). Claim confirmed as stated. Reverted.

---

## 3. Findings — rules that stay green under mutation

One row per finding: file, exact mutation, suite command, and the case that would close it.

### F1 — the withheld decode-error sentence can be emptied, and nothing notices *(NEW)*

| | |
| --- | --- |
| **File** | `services/message/infrastructure/src/kui/message/infrastructure/ConfiguredRecordMasking.scala:104` |
| **Mutation** | `private[infrastructure] val WithheldCause: String = "this payload could not be decoded, and the detail is withheld because a masking rule is in force for this topic"` → `private[infrastructure] val WithheldCause: String = ""` |
| **Suite** | `./mill --no-daemon -k libs.securityCore.jvm.test + libs.config.test + services.message.__.test` → **`1595/1595, SUCCESS`** |
| **Why it is green** | `ConfiguredRecordMaskingSuite`'s new case asserts `masked.decodeErrors.map(_.cause) == List(ConfiguredRecordMasking.WithheldCause)` — the constant against itself, which is a tautology under any value — and then `assert(!cause.contains("`4`"))`, which the empty string satisfies. Both halves pass on `""`. |
| **Why it matters** | This is the decision item 4 asked the packet to take, and its stated content is the half that survives: *"'this record could not be decoded' survives while the quotation does not"*, and the scaladoc argues **Drop the error** down precisely because *"the screen would show a record with an empty payload and nothing saying why"*. An empty `cause` is that outcome reached by another road. `DecodeError.cause` crosses the wire in `MessageDto`, so the reader gets a record with no payload and a blank explanation — a bare field where the promise requires a sentence. Nothing in the repository references `WithheldCause` outside this file and its suite. |
| **Case that would close it** | In `ConfiguredRecordMaskingSuite`, assert the sentence's **content** and not the constant: *"the withheld cause still says the record could not be decoded"* — `assert(masked.decodeErrors.head.cause.contains("could not be decoded"))` and `assert(masked.decodeErrors.head.cause.nonEmpty)`, written against literal text rather than against `ConfiguredRecordMasking.WithheldCause`. A constant compared to itself gates nothing. |

### F2 — the composition root can be rewired to the noop and `kui.masking.applied` silently stops *(NEW)*

| | |
| --- | --- |
| **File** | `services/message/app/src/kui/message/app/MessageWiring.scala:129` |
| **Mutation** | `maskingMetrics <- Resource.eval(MaskingMetrics.otel4s[F](meter))` → `maskingMetrics <- Resource.eval(MaskingMetrics.noop[F].pure[F])` |
| **Suite** | `./mill --no-daemon -k services.message.__.test` → **`1442/1442, SUCCESS`** |
| **Why it is green** | `MaskingMetricsSuite` (this packet's new 6 cases) drives `MaskingMetrics.otel4s` **directly**, and `ConfiguredRecordMaskingSuite` drives a counting fake. Neither reaches `MessageWiring`, and `MessageWiringSuite`'s 6 cases are about the signing key, `ProfileVersion` and the start-up line. Nothing asserts which implementation the running process is handed. |
| **Why it matters** | It is this packet's own thesis one layer out. Item 3 was written because *the real otel4s adapter is the only thing that decides what `{cluster, topic, target}` actually contain* — the packet closed that and left the adjacent question open: **is the real adapter the one that runs?** `MaskingMetricsSuite`'s own scaladoc says the panel that goes empty is the one an operator checks to answer *"is this topic actually being masked in production"*. Under this mutation that panel is empty forever, the six new cases stay green, and the first writer of a metric declared in wave 1 has no writer again. One line, no second edit — `-Werror` does not fire here, because `noop` is a real call. |
| **Case that would close it** | In `MessageWiringSuite`, over `OtelJavaTestkit.inMemory` the way `MaskingMetricsSuite` already does: *"the message service's wiring hands the masking mask a meter that actually records"* — build the wiring's masking dependency, apply a mask on a topic a rule reaches, collect, and assert one `kui.masking.applied` point. If the composition root is too heavy to drive, the cheaper seam is to have `MessageWiring` expose the constructed `MaskingMetrics` the way `describeMasking` was widened to `private[app]` under rule 22 — the same seam, for the same reason. |

### F3 — the start-up line's prose is unasserted, so it can be made to say the opposite *(the packet's own house-rule-5 disclosure; I reproduced it)*

| | |
| --- | --- |
| **File** | `services/message/app/src/kui/message/app/MessageWiring.scala:380` |
| **Mutation** | `"Masking is never applied on produce " + "or resend"` → `"Masking is also applied on produce " + "and resend"` |
| **Suite** | `./mill --no-daemon -k services.message.__.test` → **`1442/1442, SUCCESS`** |
| **Why it is green** | The two new `MessageWiringSuite` cases assert the **count** (`2 rule(s)`), the cluster id, and the **absence** of the roster (`nationalInsuranceNumber`, `payments`, `[Pp]assword`, `MaskingRule(`, `KeepEnds`). No case reads the sentence. |
| **Why it matters** | The mutated sentence is false and is exactly the claim an operator would act on — it is the difference between *"the topic still holds the real value"* and *"producing through KUI is safe"*. The same sentence is repeated in `docs/operations/configuration.md:557`; the two can now disagree silently. |
| **Case that would close it** | In `MessageWiringSuite`: *"the start-up line says masking does not apply on produce or resend"* — `assert(line.contains("never applied on produce"))`. One line, and it makes the log's only load-bearing clause a gated one. |

### F4 — `MaskingMetrics.otel4s`'s `.withDescription` has no reader *(the packet's own disclosure)*

| | |
| --- | --- |
| **File** | `services/message/infrastructure/src/kui/message/infrastructure/MaskingMetrics.scala:44` |
| **Mutation** | any rewrite of `.withDescription("Reads on which a masking rule was in force, by cluster, topic and target")` |
| **Suite** | scoped acceptance; green by inspection — `MaskingMetricsSuite` reads name, attributes and value off the SDK and never `getDescription`. |
| **Why it matters** | Low. The description is what a metrics catalogue shows, not what a dashboard queries. Filed because it was disclosed and because the closing assertion is one line. |
| **Case that would close it** | Extend `MaskingMetricsSuite`'s first case to assert `metricData.getDescription` alongside the attribute map. |

### F5 — `masking.md` states a rule for all three kinds that is false for `replace` *(NEW, documentation)*

| | |
| --- | --- |
| **File** | `docs/operations/masking.md:47-49` |
| **The claim** | *"Three things hold for all of them, and each is a decision rather than an accident: **A masked value is never longer than the value it replaced.**"* |
| **The code** | `MaskingEngine.scala:202` and `:229` — `case MaskingKind.Replace(replacement) => Some(Json.fromString(replacement))` / `=> replacement`. The literal is written verbatim with no length bound, so `kind: replace` with `replacement: "<redacted by policy>"` over a 4-character field yields a **longer** value. The invariant is real for `mask` only, and `MaskingRule.scala`'s own kind table — correctly — attaches *"the result is never longer than the input"* to the `Mask` row alone. |
| **Why it matters** | The doc gives the rule a security rationale (*"somebody counting characters would learn what the mask was hiding"*), which invites an operator to rely on it for the kind where it does not hold. It is the one factual error I found in 234 lines. |
| **Fix** | Move the bullet under the `mask` row, or qualify it: *"A `mask`ed value is never longer than the value it replaced; a `replace`ment is whatever literal you wrote, and it is the same literal for every matched field, so its length says nothing about the value."* No test change — see F7 on why there is nothing to make red. |

### F6 — `kui-quickstart-auth.yaml` now contradicts its own header, and the new suite case pins it there *(NEW)*

| | |
| --- | --- |
| **File** | `deployment/quickstart/kui-quickstart-auth.yaml:3-6` — **W10-05's file, not W10-04's** |
| **The claim** | *"This is ./kui-quickstart.yaml with two blocks filled in — `kui.auth` and `kui.rbac` — and nothing else changed … so that the difference between the default (no login at all) and this one is a diff of two files rather than a paragraph."* |
| **What is now true** | `grep -c masking deployment/quickstart/kui-quickstart-auth.yaml` → **0**, against 16 in the file it claims to copy. The diff of the two files is now three blocks, not two. |
| **Why it matters** | `quickstart.sh --with-auth` is the configuration where hiding data matters most — it is the one with a `viewer` role — and it is the one with no masking. Worse, `ShippedConfigurationSuite`'s replacement case actively **asserts** that every shipped file other than `kui-quickstart.yaml` has no masking rule, so the divergence is now gated *in place*: whoever adds the block to the auth variant will be told by a red test that they were wrong. |
| **Ownership** | W10-04 correctly told W10-05 about `deployment/quickstart/README.md` and did not touch it. It did not notice this second file in the same directory. **This is a hand-off W10-04 should have made and did not**, and it is the one thing I would have sent back. |
| **Case that would close it** | For **W10-05**: add the same `masking:` block to `kui-quickstart-auth.yaml`, and in `ShippedConfigurationSuite` narrow the "every other shipped file has none" assertion to exclude it and assert instead that *"the auth quickstart differs from the default quickstart in `kui.auth` and `kui.rbac` and nothing else"* — which is the header's actual promise and is checkable by loading both and comparing every other section. |

### F7 — the three operator documents are gated by nothing at all *(NEW, structural)*

| | |
| --- | --- |
| **Files** | `docs/operations/masking.md` (234 lines, new), `docs/operations/configuration.md` (the 9 new key rows), `docs/operations/observability.md` (the repaired metric table) |
| **Mutation** | any. Delete `docs/operations/masking.md` entirely; `./scripts/run-tests.sh` stays green at 4328. |
| **Evidence** | `grep -rn "docs/operations" --include="*.scala"` finds only prose references in scaladoc and one message assertion (`CloudHandlersSuite:93`, about `configuration.md`'s *name*, not its content). Nothing reads these three files. `MaskingEngine.scala:13` and `MaskingConfig.scala:231` now both assert in prose that `masking.md` exists — a claim that becomes false silently, which is the exact shape of the sentence this packet replaced there. |
| **The sharpest instance** | `observability.md:75` still opens *"Every metric KUI emits is in this table"*. The packet repaired the table by hand and — to its credit — added *"**What that suite does not do is read this page** … Nothing compares the two: if you add a metric, add its row here by hand."* So the page is now honest about being ungated, and it is still ungated. **I re-checked it in both directions and it is currently correct**: all 27 names in `MetricNames.scala` (the 26 of `MetricNames.all` plus `kui.gateway.aggregation.section`) have a row, and the table has no row that is not a declared name. The only extra `kui.*` token on the page is `kui.topic.list` at line 158, which is a **span** name in the tracing section, not a table row. |
| **Case that would close it** | A `libs/observability` suite case: *"every declared metric name has a row in `docs/operations/observability.md`, and the table names nothing that is not declared"* — read the file, pull the leading `` `kui.…` `` of each table row, and `assertEquals` both set differences against `Nil`. This is the same shape as `TopicsConfigSuite`, which already asserts defaults against `configuration.md`'s table, so the precedent and the helper both exist in this repository. It belongs to whoever owns `libs/observability` next; nobody owned it this wave. |

---

## 4. Ownership

`git diff --name-only ee81a440` lists 47 files plus 3 untracked. **Fourteen tracked and two
untracked are W10-04's, and every one is inside its `Owns`:**

```
libs/config/src/kui/config/MaskingConfig.scala
libs/config/test/src/kui/config/MaskingConfigSuite.scala
libs/config/test/src/kui/config/ShippedConfigurationSuite.scala
libs/security-core/src/kui/security/masking/MaskingEngine.scala
libs/security-core/src/kui/security/masking/MaskingRule.scala
libs/security-core/test/src/kui/security/masking/MaskingEngineSuite.scala
services/message/app/src/kui/message/app/MessageWiring.scala
services/message/app/test/src/kui/message/app/MessageWiringSuite.scala
services/message/application/src/kui/message/application/RecordMasking.scala
services/message/infrastructure/src/kui/message/infrastructure/ConfiguredRecordMasking.scala
services/message/infrastructure/test/src/kui/message/infrastructure/ConfiguredRecordMaskingSuite.scala
services/message/infrastructure/test/src/kui/message/infrastructure/MaskingMetricsSuite.scala   (new)
deployment/quickstart/kui-quickstart.yaml
docs/operations/masking.md                                                                      (new)
docs/operations/configuration.md
docs/operations/observability.md
```

**No ownership violations.** Specifically checked and clean:

* `libs/config/src/kui/config/KuiConfigSource.scala` is in the packet's `Owns` and is **not in the
  diff at all** — byte-identical to `ee81a440`, as claimed after the item-6 measurement.
* `docs/FEATURE_MATRIX.md` and `TECH_DEBT.md` **are** modified, and the masking edits in them are
  **W10-03's**, not W10-04's — `DM-001`'s row is re-audited in W10-03's voice and `TD-038` is closed
  there with W10-04 named as the closer. That is the hand-off working: W10-04 told W10-03 and did
  not edit the matrix, which is what item 7 asked and what wave 8 lost a correction to.
* `deployment/quickstart/README.md` is modified by W10-05 and contains **no** masking text
  (`git diff … | grep -i mask` → empty), consistent with W10-04 having only flagged it.
* The remaining 33 changed files (frontend, `services/gateway`, `build.mill`, `.github/`,
  `scripts/feature-matrix-check.sh`, `ARCHITECTURE.md`, `README.md`) carry no masking content and
  plainly belong to W10-01/02/03/05/06.

## 5. Guard files

| Guard | Enumerates | State |
| --- | --- | --- |
| `KuiConfigSource.UnknownKeys.Known` (`:2887-2896`) | the ten `kui.clusters.*.masking.*` paths | **Correct and now strongly gated.** Ten entries; `configuration.md` documents nine rows, the tenth being `fields.*`, the YAML list leaf — the packet's arithmetic is exact. Deleting one entry reddens five cases (measured above). |
| `MetricNames.all` (`:108-135`) | 26 metric names | Carries `MaskingApplied`. **`AggregationSection` (`:42`) is declared, emitted at `TopicOverviewUseCase.scala:230`, and absent from `all`** — confirmed. `MetricNamesSuite` therefore pins 26 while the process emits 27. Correctly reported by W10-04 as outside its ownership; `libs/observability` was owned by nobody this wave. |
| `MetricNamesSuite.expected` (`:15-43`) | the same 26, hand-written a second time | In sync. Note that `observability.md:76-78` says this suite pins the list *"against `ARCHITECTURE.md` §13"* — it does not read that file either; it compares `MetricNames.all` to the in-suite copy, and `ARCHITECTURE.md` appears only in a comment. The sentence is a fair description of the mechanism but overstates the counterparty by one hop. |
| `ShippedConfigurationSuite.shipped` roster | every shipped configuration file | Still 16 cases. The replacement case is **stronger** than the one it replaced: it asserts the two rule kinds, the field lists, both topic patterns, the engine's output over a seed-shaped record, `clusters(1).masking == MaskingConfig.empty`, and that every other shipped file has none. See **F6** for the one thing that last clause now pins in the wrong place. |
| `MaskingKind` scaladoc table (`MaskingRule.scala:33-38`) | the three kinds and their notes | Correct, and — unlike `masking.md` — correctly scopes the length invariant to `Mask` only. See **F5**. |
| `docs/operations/observability.md` metric table | all 27 emitted names | Currently exact in both directions (verified). Ungated — see **F7**. |

## 6. Orphans

Everything created has a caller.

| Created | Caller |
| --- | --- |
| `MaskingConfig.refuseAKeepThatRevealsTheField` | `MaskingConfig.scala:191`, in `validate`'s for-comprehension. Deleting the call **does not compile** (`-Werror`/`-Wunused` on the orphaned private method) — I confirmed this is why the packet's mutation had to change the arithmetic instead. |
| `MaskingEngine.replacementFor` | `maskKeepingEnds`, both branches (`:260`, `:263`). |
| `ConfiguredRecordMasking.WithheldCause` | `withheldWhereMasked`, and `ConfiguredRecordMaskingSuite`. Nothing else in the repository references it — which is F1's root. |
| `ConfiguredRecordMasking.withheldWhereMasked` | `maskWith`, via `decodeErrors = record.decodeErrors.map(withheld)`. |
| `MaskingMetricsSuite` (new file) | Runs under `services.message.infrastructure.test`; 6 cases confirmed in the acceptance tally. Its build edge (`otel4s-oteljava-testkit`) is present, landed by W10-05 as house rule 15 requires. |
| `MaskingMetrics.otel4s` | `MessageWiring.scala:129` — **the real adapter is wired in production.** Not an orphan; see F2 for the fact that nothing gates this. |
| `docs/operations/masking.md` | Linked from `configuration.md:516` and `:548` (including the anchored migration note, which resolves — the `## A configuration that used to load` heading exists), and from `README.md:152` and the scaladoc of `MaskingEngine` and `MaskingConfig`. `docs/operations/` has no index page, so no index needed updating. |
| `MessageWiring.describeMasking` as `private[app]` | `MessageWiringSuite`, two cases. The rule-22 seam is real and used. |

## 7. Reading the code — does it do what was asked?

**Yes, on all six items**, and several go past what was asked.

* **Item 1** is closed in **both** directions, not the one the packet was allowed to choose. Both
  halves are independently red under mutation (§2), and the reason for doing both is argued at the
  branch: `KeepEnds` is a plain pair of `Int`s any caller can build, so the loader cannot be the only
  guard. The old `MaskingEngineSuite` case that carried the promise in its name and the bug in its
  assertion (`"abc"` for *"keeping more than there is masks everything rather than silently doing
  nothing"*) is genuinely repaired to `"***"` — I read the diff.
* **Item 2** is done at the seam rule 22 names, and the assertion is over the rendered line with
  five negative substring checks including `MaskingRule(` and `KeepEnds`, which is the form that
  catches the actual one-word mutation. Gap: the prose (F3).
* **Item 3** reads points back off a real SDK and asserts the exact attribute map, both orders of the
  cluster/topic mistake, series separation by target, accumulation, an exact three-key set, and both
  negative cases. Gap: the wiring (F2).
* **Item 4** is a decision, written in the port's scaladoc as asked, with three alternatives argued
  down, and implemented with `target` and `serde` surviving. The `Decoded.properties` carve-out is
  sound: I checked, and the only writer is `ClusterSerdeSource.scala:142`, which maps a
  `DeserializeResult`'s properties through, so the claim that the repair would belong at the serde
  holds. Gap: the sentence's content (F1).
* **Item 5** — `grep -c masking docs/operations/configuration.md` → **16** (was 0). `masking.md` is
  **234** lines, not the 232 claimed; a two-line overstatement and the only number in the report I
  found wrong. Both YAML examples in it use only keys the loader accepts (checked against
  `UnknownKeys.Known`; no unknown key). Its factual claims about scope, ordering, header handling,
  first-rule-only for non-JSON and the overlap rule all match `MaskingEngine`; the one exception is
  F5. The quickstart's rules are live on a real stack, not just in a suite:
  `docker-compose.quickstart.yml:484` mounts the file and `:470` passes it as `--config`, and the
  seed record for `CUST-8812` is literally
  `{"customerId":"CUST-8812","name":"Marta Zielinska","email":"marta.zielinska@example.com", …}` —
  the shape `ShippedConfigurationSuite` asserts against. The browse is still unwitnessed (below).
* **Item 6** is measured, not asserted, and the measurement reproduces exactly (§2).
* **Item 7** — the matrix was not edited by this packet and W10-03 has already acted on the hand-off.

**No panel renders a zero or a bare em dash where a sentence is promised** — except under F1, where
one line of mutation produces exactly that and no test objects.

**The honest gap the packet named is real and still open.** Nobody has browsed
`customers.profiles` on a running stack. Everything a suite can reach is reached; the last step is a
human one and belongs in the definition-of-done pass, as the packet said.

---

## For W10-A2

1. **F2 is the one to act on.** It is a one-line, compiling, `-Werror`-clean edit that silences a
   metric this wave spent a whole suite proving correct. It is the wave's own thesis — a green gate
   measuring something adjacent to the claim made about it — reproduced inside the packet that
   discovered the pattern.
2. **F1 next**, because a constant asserted against itself is a gate that cannot fail, and there
   will be more of them.
3. **F6 is a hand-off W10-04 missed** and needs to reach W10-05 before the quickstart files are
   frozen.
4. **F7's closing case is cheap and has a precedent in this repository** (`TopicsConfigSuite` already
   asserts a table in `configuration.md`). It needs an owner for `libs/observability`, which nobody
   had this wave.
5. Carry forward the packet's own correct finding that **`kui.gateway.aggregation.section` is emitted
   and absent from `MetricNames.all`**, which makes `FEATURE_MATRIX` MT-007's *"26 declared metric
   names"* wrong by one.
6. **`-Werror` as a structural gate now has six instances** — wave 9's two, this packet's two, and
   I re-confirmed the `refuseAKeepThatRevealsTheField` one. But note that **neither F2 nor F3 is
   protected by it**: the cheapest attack on a *wiring* choice or a *string* is never a deletion.

**Tree state at the end of this pass:** every mutation reverted; `MaskingConfig.scala` and
`MaskingEngine.scala` back to md5 `3148d627…` and `29e6dc55…`; `KuiConfigSource.scala` and
`MessageWiring.scala` and `ConfiguredRecordMasking.scala` restored from saved bytes;
`git diff --stat ee81a440` over the packet's paths shows the 14 tracked files and the 2 untracked
ones and nothing else; the scoped acceptance re-runs `1595/1595, SUCCESS` and `run-tests.sh`
re-runs 4328 passing.
