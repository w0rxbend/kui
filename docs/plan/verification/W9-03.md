# W9-03 — verification pass

**Packet.** *The last two screens, and the words that are still hand-copied.*
**Verifier.** An independent, skeptical pass, 2026-09-11, against the same running quickstart the
packet reported on: `kui-frontend:0.1.0-SNAPSHOT` = **34db4fcf7948**,
`kui-allinone:0.1.0-SNAPSHOT` = **00c497c06754** (confirmed with `docker inspect` on
`kui-quickstart-frontend` / `kui-quickstart-kui`), two clusters registered (`quickstart`,
`staging-eu-01`), both seed scripts applied.

**Verdict: CONFIRMED, with four filed findings.** Both acceptance commands reproduce to the case.
The owned rule is genuinely closed — the mutation was re-applied by this pass, independently, and
the named case goes red with the exact message the packet published. Ownership is exact: five files,
all under `frontend/e2e/**`. Nothing was left running and nothing was left on the broker.

Everything below was measured. The mutation vehicle is a 47-line rewriting reverse proxy in this
pass's scratch directory that serves the interface container's own bytes with one substitution
applied to JavaScript in flight, pointed at with `KUI_E2E_UI=http://localhost:8091`. **Nothing on
disk and no byte in the container was ever written**; `git diff --stat a134d74c` is byte-identical
before and after this pass.

> **A note for anyone who repeats this.** The product opens two SSE endpoints —
> `/api/v1/capabilities/stream` and `/api/v1/clusters/<id>/alerts/stream`. A mutating proxy that
> buffers the upstream response (`await response.arrayBuffer()`) never answers them, and the
> **environment rail renders zero tiles** as a result. `connect.spec.ts` does not notice; every
> case this packet added does. Stream everything that is not `application/javascript`.

---

## 1. Acceptance, re-run

| Command | Packet claimed | This pass measured | |
| --- | --- | --- | --- |
| `pnpm -C frontend e2e` | 108 tests, **105 passed / 3 skipped**, 1.9m | 108 tests, **105 passed / 3 skipped**, **2.1m** | ✅ |
| `pnpm -C frontend e2e e2e/connect.spec.ts` | `4 passed (1.6s)` | `4 passed (1.6s)` | ✅ |

The three skips are the three named, at the lines named: `alerts.spec.ts:268`, `ksql.spec.ts:175`,
`shell.spec.ts:155`. `traffic.spec.ts:536` ran and passed. `git diff a134d74c -- frontend/e2e/`
contains **no added `test.skip`**, so the "no fifth skip" constraint holds and the count went down
rather than up, as reported.

`pnpm` is not on `PATH` in a bare shell here; it lives at
`/home/worxbend/.local/share/pnpm/bin/pnpm` (`PNPM_HOME` points one directory above its own `bin`).

## 2. The owned rule — genuinely closed

**Rule.** *A connector card's pill draws the state the Connect worker reported.*

Re-applied by this pass, not taken on report:

| | |
| --- | --- |
| Mutation | in `assets/index-DczEQZ42.js`: `` dot:!0,get dataState(){return e.state},get children(){return t().label} `` → `` dot:!0,get dataState(){return `UNASSIGNED`},get children(){return `unassigned`} `` |
| Command | `KUI_E2E_UI=http://localhost:8091 pnpm -C frontend e2e e2e/connect.spec.ts` |
| Result | **1 failed, 3 passed.** `connect.spec.ts:196` — *quickstart/quickstart-file-source: the pill on screen and the state RUNNING the worker reported disagree · Expected: "RUNNING" · Received: "UNASSIGNED"* |
| Control | same proxy, empty substitution: `4 passed (1.7s)` |
| Reverted | nothing to revert. `git diff --stat a134d74c -- frontend/e2e/` still `5 files changed, 530 insertions(+), 37 deletions(-)` |

The substitution string occurs **once** in the served bundle. The packet's "3 substitutions reported
by the proxy" is a per-request tally across the run, not three sites in the file; worth restating so
the next reader does not go looking for two more.

## 3. Findings — mutations this packet's cases do NOT catch

One row per finding: file, exact mutation, suite command, the case that would close it. House rule 18.

### F1 · `M09`'s second-cluster case cannot tell the second cluster's brokers screen from the first's

| | |
| --- | --- |
| **File** | `frontend/e2e/brokers.spec.ts:371` — *follows a cluster change into the second cluster's brokers screen* |
| **Mutation** | served chunk `assets/feature-clusters-DfkFvAa-.js`, the brokers route's three loaders pinned to the **first** cluster while their reactive keys are left alone: <br>`` function Ht(e){let t=W(),n=I({key:()=>`clusters/${e.clusterId}/brokers`,load:()=>wt(t.api,e.clusterId)}),r=I({key:()=>`clusters/${e.clusterId}`,load:()=>Ct(t.api,e.clusterId)}),i=I({key:()=>`clusters/${e.clusterId}/log-dirs`,load:()=>At(t.api,e.clusterId)}) `` → the same with ``load:()=>wt(t.api,`quickstart`)``, ``Ct(t.api,`quickstart`)``, ``At(t.api,`quickstart`)`` |
| **Suite** | `KUI_E2E_UI=http://localhost:8091 pnpm -C frontend e2e e2e/brokers.spec.ts` |
| **Result** | **11 passed (14.4s) — GREEN.** Confirmed in the browser that `/ui/clusters/staging-eu-01/brokers` issues `GET /api/v1/clusters/quickstart/brokers` and draws that answer. |
| **Why** | The case's *identity* assertions — the drawer head and the rail's `aria-current` — are driven by the **route parameter**, not by the data the screen fetched. Its *data* assertions compare card count and voice line against `/clusters/staging-eu-01`, and both clusters are registered over the same `kafka:9092` with identical summaries (`brokerCount: 1`, `underReplicatedPartitionCount: 0`), so every figure matches whichever cluster was asked. The packet's own header states the opposite: *"A route pinned to the first cluster's document reddens on the identity assertions whatever the figures say."* **It does not.** |
| **Case that would close it** | Observe the request, which is the one thing that differs: wrap the `page.goto` in `const asked = page.waitForRequest((r) => r.url().includes(\`/clusters/${other.id}/brokers\`)); await page.goto(...); await asked;` — the same shape `topics.spec.ts:432` already uses for `q=`. One line, no second broker needed. |

### F2 · The "Nothing matches" empty state is now asserted by no browser case on the shipped stack

| | |
| --- | --- |
| **File** | `frontend/e2e/search.spec.ts:133` — *says so in words when nothing matches, and never calls it a failure* |
| **Mutation** | served bundle `assets/index-DczEQZ42.js`: <br>`` th=c(`<p class=kui-global-search__message role=status>Nothing matches “<!>”.`) `` → `` th=c(`<p class=kui-global-search__message role=status>0`) `` |
| **Suite** | `KUI_E2E_UI=http://localhost:8091 pnpm -C frontend e2e e2e/search.spec.ts` |
| **Result** | **6 passed (5.7s) — GREEN.** The honest empty state was replaced by a bare `0` and nothing reddened. |
| **Why** | The repair branches on the wire, and on this deployment the wire has only one value: `staging-eu-01` has no schema registry and `/api/v1/search` is global, so **every** query answers `partial: ["schema"]` (measured on `?q=zzz-nothing-matches-this-zzz` and on `?q=orders`). The `partial.length === 0` arm is dead code on the deployment the suite runs against. The repair is stronger in kind and **weaker in practice**: before this wave the case pinned the empty-state sentence; now nothing does. |
| **Case that would close it** | The empty arm is reachable without a registry: scope the query to a source that *was* searched. Either assert the empty rendering against a `partial`-free response the suite can produce (a cluster-scoped query, or a route intercept), or add a second Playwright project against a stack with a registry on every cluster. Failing that, the arm should say out loud that it did not run — `test.info().annotations.push(...)` — so a green run stops reading as coverage. |

### F3 · `/ui/` on a two-cluster deployment draws a permanently-loading dashboard, and the repaired smoke test is green over it

| | |
| --- | --- |
| **File** | `frontend/e2e/shell.spec.ts:30` — *loads, and reaches the gateway through its own proxy* (the smoke test, rewritten by this packet) |
| **Mutation** | **None needed — the shipped product already draws it.** Measured in a browser on the unmodified stack: `/ui/` renders the voice line *"Asking the cluster how it is."* over six stat tiles (`BROKERS ONLINE`, `TOPICS`, `PARTITIONS IN SYNC`, `PRODUCTION`, `CONSUME`, `CONSUMER LAG`) that are **blank** — no figure, no "not measured" sentence — while the drawer head reads *"no cluster"*. Nothing is being asked. The sentence is false. |
| **Suite** | `pnpm -C frontend e2e e2e/shell.spec.ts` → green, including this case |
| **Why** | `soleClusterChoice` correctly selects nothing when two clusters are registered. The old assertion, `getByText("Quickstart")`, would have **reddened** on this screen. The replacement asserts only the environment rail's roster, which is chrome — so the rewrite moved the suite's only `/ui/` body assertion off the body at exactly the moment the body broke. The packet filed the product defect under `needsOutsideOwnership` (correctly, `App.tsx` is W9-04's) and did not note that its own repair is what stopped the suite seeing it. |
| **Case that would close it** | In the same case, over `/ui/` with no cluster selected: assert every stat tile either carries a figure or carries a sentence — `expect(tile).toHaveText(/\S/)` per tile — and assert the voice line is not a progress claim while no request is in flight. That is house rule 7 and SPEC §4.0's own rule, and it is one assertion. Production fix belongs to `App.tsx` / `shell/src/overview/`. |

### F4 · The bulk bar's "N topics selected" cannot fail in the case that reads it

| | |
| --- | --- |
| **File** | `frontend/e2e/topics.spec.ts:432` — *a bulk delete says how many it deleted, in the plural* (and `:396`, pre-existing) |
| **Mutation** | served chunk `assets/feature-topics-D4ns2HYt.js`, the bulk bar's count switched from the **selected set** to the **rows on the page**: <br>`` f(ee,{get count(){return C().size},get actions(){return e.bulkActions??[]},noun:`topic` `` → `` f(ee,{get count(){return S().length},get actions(){return e.bulkActions??[]},noun:`topic` `` |
| **Suite** | `KUI_E2E_UI=http://localhost:8091 pnpm -C frontend e2e e2e/topics.spec.ts -g "chips and selection"` |
| **Result** | **3 passed (3.5s) — GREEN.** |
| **Why** | The case's own arrangement defeats it: the list is filtered on the server to exactly the two topics it then ticks, so "rows drawn" and "rows selected" are the same number and `toContainText("2 topics selected")` distinguishes nothing. A bar counting the page instead of the selection — which is the misreading that would send an operator into a destructive dialog with the wrong figure — survives both cases. |
| **Case that would close it** | Tick **one** of the two filtered rows first and assert `1 topic selected` (which also drives the singular, currently exercised nowhere in the browser), then tick the second and assert `2 topics selected`. Two lines inside the case that already exists, no new fixture. |

### F5 (reproduced, already filed by the packet) · the connector pill's tone

The packet's own green mutation reproduces here and should not be argued down: hard-coding the pill's
tone at `ConnectorCard`'s call site — `` k(qt,{get tone(){return t().tone},dot:!0,get dataState(){return e.state} `` →
`` k(qt,{tone:`danger`,dot:!0,get dataState(){return e.state} `` — draws a **RUNNING** connector with a
red pill and `pnpm -C frontend e2e e2e/connect.spec.ts` is `4 passed (1.6s)`. The packet's proposed
one-line closer in `packages/kernel/src/components/connect.test.tsx` (assert
``pill?.className`` contains ``kui-pill--${connectorChip(state).tone}`` inside the five-state loop) is
the right fix. Owner: W9-04, or W9-A2 after freeze.

## 4. Ownership

`git diff --name-only a134d74c` over the packet's Owns (`frontend/e2e/**`) is exactly five files:
`brokers.spec.ts`, `connect.spec.ts`, `search.spec.ts`, `shell.spec.ts`, `topics.spec.ts`.
**No file outside `frontend/e2e/**` is attributable to this packet.** The harness
(`fixtures.ts`, `globalSetup.ts`, `tsconfig.json`, `playwright.config.ts`) is untouched, as claimed.
The packet correctly declined to edit `deployment/quickstart/kui-quickstart.yaml` to un-skip
`shell.spec.ts:155` while W9-01 held it.

## 5. Guard files, and one drift nobody owns

No registry, golden file, exhaustive match or checked-claim gate enumerates the browser suite:
`scripts/feature-matrix-check.sh` contains no reference to `e2e`, and `.github/workflows/ci.yml`'s
only e2e step is `pnpm e2e`. So the following is **ungated prose, and it is now false**:

* **`docs/FEATURE_MATRIX.md:674` still publishes "21 of 23 covered, 1 partial, 1 uncovered"**, and
  its bullets below still name `M08` as the uncovered screen and `M14` as the partial. This packet
  reports **23 / 23 / 0** and was told — correctly — not to copy the mapping into another document.
  But the count *is* in that document, it contradicts the packet's report, nothing checks it, and it
  is **not in the packet's `needsOutsideOwnership` list**. This is the ADR-053 shape the wave's own
  plan opens with: one line, stale because ownership outlived the owner. **Owner: the integrator, or
  W9-02, this wave.**
* `docs/FEATURE_MATRIX.md:475` (`KU-034`) says `search.spec.ts` "drives five cases"; it drives six.
  Pre-existing, not this packet's, recorded so it is not re-discovered.

## 6. Orphans

Every helper the packet created has a caller inside the file that declares it: `secondCluster`
(`brokers.spec.ts`), `listedAfterRefresh`, `topicsMatching`, `SETTLE` (`topics.spec.ts`), `clusters`,
`quoted` (`shell.spec.ts`). `wordFor` is gone from code — the three remaining occurrences in
`connect.spec.ts` are all inside comments explaining its deletion. No orphan.

## 7. Reading the code — does it do the thing, and are the numbers right

**It does the thing.** The three new cases assert content, not addresses. `M14` names both topics in
the dialog rather than counting them, reads the receipt before the six-second timer, takes the "no
refusals" half off the text the first assertion already found — which is the right call, and the
reason is correctly written above it — and confirms the delete against the API rather than against a
filter that stopped matching. Item 4's locator is genuinely scoped to `.kui-connector__head .kui-pill`
with `toHaveCount(1)` and `toHaveText(/\S/)` beside the attribute, so "no pill at all" and "a silent
stadium" both redden. Item 5 is real: `wordFor` is deleted, and the labels it transcribed are pinned
by literal in `packages/kernel/src/components/connect.test.tsx`, whose new five-state loop asserts
`pill.textContent === connectorChip(state).label` — so "the pill draws the product's word and not the
wire token" is gated in the language that owns the words. The claim holds.

**Numbers checked, one wrong:**

| Claim | Measured |
| --- | --- |
| 5 files, 530 insertions, 37 deletions | ✅ exact |
| 108 cases, 12 spec files | ✅ exact |
| bundle `assets/index-DczEQZ42.js`, 259,353 bytes, md5 `f0b7f993fd7393aee7b4fdeb0430647d` | ✅ exact |
| `[data-testid="cluster-selector-trigger"]` resolves to 0 on a running stack | ✅ 0, on `/ui/`, `/brokers` and `/connect` |
| `ClusterSelector.tsx` 214 lines; stories 105 lines / 8 stories; `shell/src/index.ts:14` exports it; zero production callers | ✅ all four |
| "**8** unit cases in `chrome.test.tsx`'s own describe" | ❌ **7** (`chrome.test.tsx:883–964`). The eight figure appears to have picked up the a11y story sweep. Does not change the finding. |
| "3 substitutions" for the pill mutation | ⚠️ one site in the file; three is a per-request tally |
| `kafka-topics --list \| grep -c '^kui-e2e-'` is 0 | ✅ 0, before and after this pass |
| no background load left running | ✅ `pgrep` empty |

**One cost the packet under-reports.** `topics.spec.ts:432` took **58.7 s** in this pass's full-file
run — 46% of the suite's entire 2.1-minute wall time — against 2.1 s when the same case ran alone.
The packet's "settles in under a second on a quiet stack" is true and is not the number a reader of a
full run will see. The 90 s `SETTLE` budget is doing real work, and it is doing it on every run.

**One panel that renders nothing where the promise requires a sentence:** `/ui/`'s six stat tiles
under *"Asking the cluster how it is."* — F3 above. Everything else the new cases drive either
carries a figure or carries a sentence.

## 8. Housekeeping done by this pass

Mutating proxy killed (`pgrep` empty), `frontend/test-results/` removed, no `kui-e2e-*` topic on the
broker, no file written anywhere in the repository except this one.
