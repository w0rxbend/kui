# W10-01 — verification

Adversarial re-verification of packet **W10-01** ("The screen the product opens on"), against the
working tree at baseline `ee81a440`, on 2026-09-11/12. Every figure below was printed by the command
beside it, by me, in this pass. Every mutation named here was applied by me and reverted by me; the
packet's six files are back to exactly the stats they carried when this pass started:

```
$ git diff --stat ee81a440 -- frontend/e2e/shell.spec.ts \
    frontend/packages/kernel/src/components/record.ts frontend/packages/kernel/src/components/record.test.ts \
    frontend/packages/shell/src/overview/
 frontend/e2e/shell.spec.ts                              |  70 ++++++
 frontend/packages/kernel/src/components/record.test.ts  |  27 ++
 frontend/packages/kernel/src/components/record.ts       |  39 ++-
 frontend/packages/shell/src/overview/Overview.stories.tsx |  12 +-
 frontend/packages/shell/src/overview/Overview.tsx       | 273 +++++++++++++++++----
 frontend/packages/shell/src/overview/overview.render.test.tsx |  94 +++++++
 6 files changed, 454 insertions(+), 61 deletions(-)
```

**Verdict: the packet's headline claim is true and I proved it in both harnesses.** The `/ui/` screen
does what it says, the `formatBytes` repair holds on the running product, and item 6 ("the brief is
stale") is correct — I re-verified it under its own mutation rather than take the packet's word.
What the packet did **not** disclose is that the rule it closed is closed *for six hard-coded testids,
not for the row*: a seventh stat tile drawing its label and nothing else — the exact shipped defect —
ships green through all three suites. That row is §4.1 below and it is the one W10-A2 should carry.

Concurrency note, in the terms house rule 19 asks for: **every browser figure below was taken against
the quickstart stack** on `:8090` (`kui-frontend:0.1.0-SNAPSHOT` = `sha256:44c3a3d51447`, the second
of the two images the packet reports building; gateway `kui-allinone:0.1.0-SNAPSHOT`), with clusters
`[quickstart, staging-eu-01]` registered. `kui-quickstart-kui` was restarted **twice by another
packet** while I was running; my first full `pnpm e2e` died of it (§1, row 4). Every mutant was served
as a **side container on `:8091`** against the same gateway, so the shipped stack was never touched.

---

## 1. Acceptance, re-run

| # | Command | Result | Packet's claim |
| --- | --- | --- | --- |
| 1 | `pnpm -C frontend test packages/kernel packages/shell` | **Test Files 45 passed (45) / Tests 1023 passed (1023)**, 18.72s | 45 / 1,023 — **reproduces** |
| 2 | `pnpm -C frontend typecheck` | `tsc --build --pretty false`, **exit 0**, no diagnostics | exit 0 — **reproduces** |
| 2b | `npx tsc --noEmit -p e2e/tsconfig.json` | **exit 0**, silent | silent — **reproduces** |
| 3 | `pnpm -C frontend e2e e2e/shell.spec.ts` | **8 passed, 1 skipped (5.6s)**. The skip is `shell.spec.ts:181`, the seeded open alert | 8 passed / 1 skipped — **reproduces** |
| 4 | `pnpm -C frontend e2e` (first attempt) | **93 passed, 9 failed, 3 skipped, 4 did not run.** *Not the packet's fault* — see below | — |
| 4b | `pnpm -C frontend e2e e2e/traffic.spec.ts e2e/topics.spec.ts` (the nine, re-run) | **31 passed (20.9s)**, all nine green | — |
| 4c | `pnpm -C frontend e2e` (second attempt, clean run) | **106 passed, 3 skipped, 0 failed (2.3m)** | 106 / 3 / 0 — **reproduces exactly** |
| 5 | `pnpm -C frontend test` (whole workspace) | **Test Files 83 passed (83) / Tests 1927 passed (1927)** | 83 / 1,927 — **reproduces** |
| 6 | `pnpm -C frontend a11y` (build-storybook → serve `:6017` → sweep) | *"Sweeping 790 stories in 2 themes … ✓ 790 stories × 2 themes: no violations."* exit 0 | 790 × 2, no violations — **reproduces** |

**Row 4 is contention, not a regression, and I established that rather than assuming it.** All nine
failures were `SyntaxError: Unexpected end of JSON input` at `e2e/fixtures.ts:26` (`signedIn` reading
`/auth/me`) or a 503 — never an assertion. `docker ps` immediately after showed `kui-quickstart-kui`
**"Up 7 seconds"**: another packet replaced the gateway mid-suite. Re-running the same two files with
no edit of any kind gave 31/31 (row 4b), and the next full run gave the packet's exact figure (4c).
The guard-file budget in `WAVE-10.md` — *105 passed, 3 skipped, 0 failed; no packet may add a fourth
skip* — is **respected**: 106/3/0, one more passing case than baseline, skips unchanged at three.

`:6017`, `:6018` and `:6099` are clear; `storybook-static/` and `test-results/` are removed; no side
container and no mutant image is left (`docker ps -a | grep w10-01` → nothing).

---

## 2. The owned rule — genuinely closed, in **both** harnesses

> *Every stat tile on the cluster dashboard carries a figure or a sentence — never its own label
> alone — and the voice line does not claim a request is in flight when none is.*

I re-applied the packet's mutation myself in each harness and reverted each from the bytes I saved.

### 2.1 jsdom

Mutation, in `frontend/packages/shell/src/overview/Overview.tsx:515`:

```diff
-              why={`No cluster is selected, so KUI has not asked ${STATS[id].question}.`}
+              why={undefined as unknown as string}
```

`pnpm -C frontend test packages/shell/src/overview` → **Tests 2 failed | 231 passed (233)**:

```
FAIL  overview.render.test.tsx > … > carries a sentence on every one of the six tiles …
AssertionError: stat-brokers carries its label and nothing else: expected '' not to be ''
FAIL  overview.render.test.tsx > … > says each tile's own subject rather than one sentence six times
AssertionError: expected 1 to be 6
```

### 2.2 The browser — this is the one that matters, and it is real

The frontend container is `read_only: true`, so I used the workaround the packet documents: patched
the shipped bundle, bind-mounted it over `/usr/share/nginx/html/ui` in a **second** frontend container
from the same image on `:8091`, same gateway, same network. Patch, in
`assets/index-DdoKviY8.js` (the minified form of the same edit):

```
`No cluster is selected, so KUI has not asked ${Vp[e].question}.`   →   void 0
```

*(Note for anyone repeating this: the minifier emits `return` with no trailing space, so the
replacement must carry a leading space or the bundle becomes `returnvoid 0` and the whole app dies
with `PAGEERROR: Unexpected number` — which fails every case for the wrong reason and reads exactly
like a successful mutation. I made that mistake first and caught it with a DOM probe.)*

Probe of the mutant before running anything, which is what makes the red meaningful:

```
VOICE: "Nothing has been asked yet, because no cluster is selected. Pick one and this page fills in."
stat-brokers    busy=null  label="BROKERS ONLINE"     rest=""
stat-topics     busy=null  label="TOPICS"             rest=""
stat-in-sync    busy=null  label="PARTITIONS IN SYNC" rest=""
stat-production busy=null  label="PRODUCTION"         rest=""
stat-consume    busy=null  label="CONSUME"            rest=""
stat-lag        busy=null  label="CONSUMER LAG"       rest=""
EMPTY-STATE: 1   SKELETONS: 0   ERRORS: []
API: ["GET /api/v1/auth/me","GET /api/v1/auth/settings","GET /api/v1/capabilities/stream"]
```

`KUI_E2E_UI=http://localhost:8091 pnpm -C frontend e2e e2e/shell.spec.ts` → **1 failed, 7 passed,
1 skipped**:

```
1) shell.spec.ts:32:3 › the shell › loads, and reaches the gateway through its own proxy
   Error: stat-brokers drew its label and nothing else — no figure and no sentence
   - Timeout 15000ms exceeded while waiting on the predicate
     at eachTileSaysSomething (frontend/e2e/shell.spec.ts:499:12)
     at frontend/e2e/shell.spec.ts:66:5
```

Exactly the file, function and line the packet reported. **`ownedRuleGenuinelyClosed: true`.**

One correction to the packet's own bookkeeping: it gives the jsdom assertion's address as
`overview.render.test.tsx:800`; it is **:803**. The e2e address (`:499`, called from `:66`) is right.

---

## 3. The packet's disclosed green mutation — confirmed, and it is worse than disclosed

The packet disclosed that swapping the PRODUCTION and CONSUME `question` strings leaves everything
green. **Confirmed in both harnesses, by me.** Source swap in `Overview.tsx` → `Tests 1023 passed`.
Bundle swap, served on `:8091`, probed first:

```
stat-production => "PRODUCTION | No cluster is selected, so KUI has not asked how fast anything is being consumed."
stat-consume    => "CONSUME    | No cluster is selected, so KUI has not asked how fast anything is being produced."
```

`pnpm e2e e2e/shell.spec.ts` against it → **8 passed, 1 skipped**. Two tiles each describing the
other's subject, in a browser, green. The packet's own suggested closer — one line asserting
`[data-testid="stat-consume-note"]` contains "consumed" — closes it and is W10-A2's to land.

---

## 4. HUNT — rules this packet shipped that stay green when broken

Eleven mutations applied and reverted one at a time against
`pnpm -C frontend test packages/kernel packages/shell`; the load-bearing ones then re-applied to the
shipped bundle and run through the browser suite. **Controls first, so the harness is known to work:**
`neverzero` → 1 failed; `wholebyte` → 3 failed; `threshold` → 1 failed. The harness bites.

### 4.1 The per-tile rule covers six hard-coded testids, not the row — **the headline finding**

| | |
| --- | --- |
| **File** | `frontend/packages/shell/src/overview/Overview.tsx:239-268` (`STAT_ORDER` / `STATS`) |
| **Rule** | *Every stat tile on this row carries a figure or a sentence — never its own label alone.* |
| **Mutation** | Add a seventh entry to `STATS` (`seventh: { head: { label: "REPLICATION", icon: "brokers", testId: "stat-seventh" }, question: "" }`), add `"seventh"` to `STAT_ORDER`, and make `NoClusterChosen` pass `why={undefined as unknown as string}` for the entry whose `question` is empty. The row then draws **seven** tiles and the seventh carries its label and nothing else. |
| **Suites** | `pnpm -C frontend test packages/kernel packages/shell` → **Tests 1023 passed (1023)**. `pnpm -C frontend typecheck` → **exit 0**. Same mutation patched into the bundle and served on `:8091`: `KUI_E2E_UI=http://localhost:8091 pnpm -C frontend e2e e2e/shell.spec.ts e2e/traffic.spec.ts` → **19 passed, 1 skipped, 0 failed**. |
| **Measured DOM** | `stat-seventh="REPLICATION"` — label, nothing else, held after `networkidle` + 2.5s, beside six honest tiles. |
| **Why it is open** | Both closing cases iterate a **literal of six ids** — `overview.render.test.tsx:797-804` and `e2e/shell.spec.ts:468-475` — rather than the production `STAT_ORDER`. `Overview.tsx`'s own `STATS` header argues that *"two hand-written rows would drift the first time a label or a testid changed, and the drift would be invisible"*; the repair removed that drift from production and re-introduced it **at the test layer**, twice. The rule is closed for the six tiles that exist today and for no tile added tomorrow — which is precisely how the hole it repairs stayed open for a wave. |
| **Closing case** | In `overview.render.test.tsx`, drive the loop off the production roster instead of a literal: export `STAT_ORDER` (or a `statTestIds()` helper) from `Overview.tsx` and assert `TILES` equals it before the per-tile loop — `expect(container.querySelectorAll(".kui-overview__stats > .kui-stat")).toHaveLength(STAT_ORDER.length)` and then loop over `STAT_ORDER.map((id) => STATS[id].head.testId)`. In `e2e/shell.spec.ts`, assert the *count* of `.kui-overview__stats > [data-testid^="stat-"]` equals `STAT_TILES.length` before the per-tile poll, so a seventh tile fails the roster rather than escaping it. |
| **Seam (house rule 22)** | One word: `STAT_ORDER` and `STATS` in `frontend/packages/shell/src/overview/Overview.tsx` are **module-private**. The case needs `export` on both (or on a small `statTestIds()`). W10-01 has frozen, so W10-A2 cannot add it — this row is filed with the word, as rule 22 asks. Without it the e2e count assertion alone (which needs no export) still closes the browser half. |

### 4.2 The empty state can be emptied of every word it has, in both suites

| | |
| --- | --- |
| **File** | `frontend/packages/shell/src/overview/Overview.tsx:519-526` (`NoClusterChosen`'s `EmptyState`) |
| **Rule** | *The address that names no cluster names the choice that has to be made before anything fills in.* This is the whole point of the repair — the sentence that tells an operator what to do instead of a progress claim. |
| **Mutation** | `title="No cluster is selected."` → `title=""`, and the two-line `description` → `"" + ""`. |
| **Suites** | Source: **Tests 1023 passed (1023)** (each half measured separately, both green). Bundle (`title:``,description:``` in `assets/index-DdoKviY8.js`), served on `:8091`: `pnpm e2e e2e/shell.spec.ts` → **8 passed, 1 skipped**. |
| **Measured DOM** | `innerText` of `[data-testid="overview-no-cluster"]` on the mutant is the single word **`"Manage clusters"`** — a card with no heading, no explanation and one button, on the screen the product opens on. |
| **Why it is open** | The packet's own render case asserts only that `[data-testid="overview-no-cluster"]` is **not null** (`overview.render.test.tsx:846`) and that the action's `href` is right. Nothing reads a word of it. The packet disclosed this in one clause ("Also ungated: the `EmptyState`'s title and description text, which no case reads") without measuring it; it is measured here, and it is a browser-visible product promise, not a copy nit. |
| **Closing case** | In `overview.render.test.tsx`'s *"offers the choice rather than making it"*: `expect(container.querySelector('[data-testid="overview-no-cluster"]')?.textContent).toContain("environment rail")` and `…toContain("No cluster is selected")`. No production seam needed — both strings are already inside the rendered subtree. |

### 4.3 The row's order is ungated — the six tiles can be drawn backwards

| | |
| --- | --- |
| **File** | `frontend/packages/shell/src/overview/Overview.tsx:239` (`STAT_ORDER`) |
| **Rule** | *"The six tiles, in the order the design puts them in (§3.2)"* — the comment directly above the constant. |
| **Mutation** | `const STAT_ORDER = ["lag", "consume", "production", "inSync", "topics", "brokers"] as const;` |
| **Suites** | Source: **Tests 1023 passed (1023)**. Bundle (`Bp=[…]` reversed), served on `:8091`: `pnpm e2e e2e/shell.spec.ts` → **8 passed, 1 skipped**. |
| **Measured DOM** | `ROW ORDER: ["stat-lag","stat-consume","stat-production","stat-in-sync","stat-topics","stat-brokers"]` |
| **Why it is open** | Both rosters are *sets* — every case queries tiles by testid one at a time and none reads document order. The `<For each={STAT_ORDER}>` is the only thing deciding it and nothing holds it to §3.2. |
| **Closing case** | In `overview.render.test.tsx`: `expect([...container.querySelectorAll(".kui-overview__stats > .kui-stat")].map((e) => e.getAttribute("data-testid"))).toEqual(["stat-brokers","stat-topics","stat-in-sync","stat-production","stat-consume","stat-lag"])`. Closes 4.1's count half at the same time. |

### 4.4 The six tile labels are gated only by another packet's file

| | |
| --- | --- |
| **File** | `frontend/packages/shell/src/overview/Overview.tsx:243-266` (`STATS[*].head.label`) |
| **Mutation** | `label: "BROKERS ONLINE"` → `label: "BROKERS"` (source and bundle). |
| **Suites** | Source: `pnpm -C frontend test packages/kernel packages/shell` → **Tests 1023 passed (1023)** — green. Bundle on `:8091`: `pnpm e2e e2e/shell.spec.ts` → **green**; but `pnpm e2e e2e/traffic.spec.ts` → **1 failed** at `traffic.spec.ts:141`. |
| **Why this is a finding and not a clean pass** | The label roster that catches it is `e2e/traffic.spec.ts:142` — `["BROKERS ONLINE","TOPICS","PARTITIONS IN SYNC","PRODUCTION","CONSUME","CONSUMER LAG"]`, a **third** hand-written copy of `STATS`, in a file **W10-02 owns**. W10-01's own three files leave the labels entirely open, and the packet's `STATS` header claims the table exists so the two rows *"cannot drift"* — there are three rows, in two ownerships, and the packet named neither. See §5. |
| **Closing case** | The 4.3 ordering assertion should read labels rather than testids, or add to `overview.render.test.tsx`: `expect([...container.querySelectorAll(".kui-stat__label")].map((e) => e.textContent)).toEqual([...])`. That puts the labels under the owning packet's own suite instead of a neighbour's. |

### 4.5 `formatBytes` has no top-unit cap, and its header says it does

| | |
| --- | --- |
| **File** | `frontend/packages/kernel/src/components/record.ts:155` |
| **Rule, as the packet's own new header states it** | *"The loop promotes at the value that rounds to the next unit instead, **so the printed figure is always in `[0, 1000)`**."* |
| **Measured** | It is not. `units` ends at `TB`, so the loop stops at `unit === 4` and `formatBytes(1.5e15)` returns **`"1500.0 TB"`** — a four-digit figure under the unit, which is the exact defect the `999.95` threshold was introduced to remove. Sibling `feature-topics` copy carries a `PB` and does not have this. |
| **Mutation** | `while (value >= 999.95 && unit < units.length)` (removing the cap entirely, so `units[unit]` goes `undefined` and the `?? "B"` fallback fires) → **Tests 1023 passed (1023)**. Nothing in the workspace tests the top of the ladder in either direction. |
| **Closing case** | `record.test.ts`: `expect(formatBytes(1_500_000_000_000_000)).toBe("1.5 PB")` after adding `"PB"` to `units` — or, if TB is deliberately the ceiling, correct the header sentence and assert the saturating spelling: `expect(formatBytes(1.5e15)).toBe("1500.0 TB")`. Either way the claim and the code must agree; today they do not. |

### 4.6 An enabled primary action on the repaired screen does nothing

| | |
| --- | --- |
| **File** | `frontend/packages/shell/src/overview/Overview.tsx:410-414` (the `PageHeader` `actions`), with `frontend/packages/shell/src/App.tsx:580-585` |
| **Measured on the shipped product**, `http://localhost:8090/ui/`, after `networkidle` + 2s — **no mutation, this is what ships**: `BUTTON COUNT: 1  ENABLED: true`; click → `URL BEFORE: /ui/`, `URL AFTER: /ui/`, `ANY DIALOG: 0`. |
| **Why it is a finding** | The repair's whole argument is that this address must not make a claim it cannot keep. It now says *"Nothing has been asked yet, because no cluster is selected"* — and offers an **enabled** primary button labelled *Create topic* that silently does nothing. `App.tsx:581` already knows: *"No cluster, no topic list to send anybody to … a button that navigates nowhere is not."* That comment pre-dates this packet (`App.tsx` is unchanged from `ee81a440`) and describes the button that is still on the page. Both files are W10-01's Owns; the packet did not mention it. No mutation is needed to make this fail a user — only a case. |
| **Closing case** | `overview.render.test.tsx`, in the no-cluster describe: `expect(container.querySelector('[data-testid="overview-header"] .kui-button--primary')).toBeNull()` — after `Overview.tsx` stops rendering `actions` when `cluster() === undefined` (one `Show`, in W10-01's own file). Filed with the seam per rule 22: the production change is *"draw no primary action on an address that has no cluster"*. |

### 4.7 Mutations that correctly went red (the harness works)

| Mutation | Suite | Result |
| --- | --- | --- |
| delete `if (value > 0 && value < 0.05) return "<0.1";` (`record.ts:161`) | kernel+shell | **1 failed** |
| `if (Number.isInteger(value))` — drop the `unit === 0` guard (`record.ts:160`) | kernel+shell | **3 failed** |
| `while (value >= 1000 …)` — restore the old threshold (`record.ts:155`) | kernel+shell | **1 failed** |
| `why={undefined as unknown as string}` (`Overview.tsx:515`) | shell/overview | **2 failed** |
| `data-state={props.dataState ?? "RUNNING"}` (`StatusPill.tsx:89,101`) | `controls.test.tsx` | **1 failed \| 49 passed** |

---

## 5. Guard files and rosters

| Roster | Where | Owner | Status |
| --- | --- | --- | --- |
| `STATS` / `STAT_ORDER` — six labels, icons, testids | `Overview.tsx:239-268` | W10-01 | The repair. Correct, and it did remove the production-side duplication it was written for (the six `StatCard`/`RateCard` literals now spread `{...STATS.x.head}`). |
| `TILES` — six testids | `overview.render.test.tsx:797-804` | W10-01 | **Second hand-written copy.** Not tied to `STAT_ORDER`; see §4.1. |
| `STAT_TILES` — six testids | `e2e/shell.spec.ts:468-475` | W10-01 | **Third copy.** Its own comment defends being written out rather than collected off `.kui-stat` — correct against an *empty* row, but it is exactly what lets a *seventh* tile through (§4.1). |
| `cards` — six labels | `e2e/traffic.spec.ts:142` | **W10-02** | **Fourth copy, in another packet's file**, and the only thing anywhere gating the labels (§4.4). The packet did not name it. |
| five-of-six testids | `overview.render.test.tsx:1236` | W10-01 | Pre-existing. Omits `stat-consume`; harmless today, another place to update when the row changes. |
| `frontend/e2e/**`: 105 passed / 3 skipped / 0 failed | `WAVE-10.md:150` | W10-01, W10-02 | **Respected**: 106 / 3 / 0. One case added, no fourth skip. |
| 790 stories × 2 themes | `scripts/a11y-stories.mjs` | all | **Respected**: no violations. `NoClusterInTheAddress` changed args `HEALTHY` → `LOADING` and stays clean. |

No golden file, no wire-shape interface and no `openApiCheck` count is touched by this packet — the
six files are frontend-only and none of them crosses the API boundary.

---

## 6. Ownership — clean

`git diff --name-only ee81a440` names 47 files. The six the packet claims are all inside its Owns
block (`overview/**`, `kernel/src/components/record.{ts,test.ts}`, `e2e/shell.spec.ts`); it touched
**none** of its remaining Owns paths (`App.tsx`, `app.render.test.tsx`, `shell.test.tsx`) and nothing
outside. Every other changed path maps to another packet on this wave's partition — `feature-ksql/**`
and `StreamProxy.scala` to W10-06; `libs/config`, `libs/security-core`, `services/message/**`,
`docs/operations/masking.md` to W10-03/W10-04/W10-05; `e2e/{brokers,search,topics}.spec.ts` to W10-02;
`e2e/globalSetup.ts`, `deployment/**`, `build.mill`, `.github/workflows/ci.yml`, `scripts/` and the
`docs/` set to W10-03/W10-05. **No ownership violation. No new ungated file outside the packet's tree.**

The one procedural gap is the packet's own: **`docs/plan/verification/W10-01.md` was not written.**
Its reasoning — that `docs/plan/verification/**` is W10-A2's under this wave's partition and that its
instructions forbade report files — is stated honestly in `needsOutsideOwnership`, and W10-03 and
W10-05 did file. This file closes the gap. Everything it needed to say survived in its report; nothing
was lost. But house rule 18 asks for the file, and the packet's own material is why §2-§4 above could
be checked rather than re-discovered.

---

## 7. Reading the code, and every stated number

**It does what the brief asked, not merely exit zero.** Item 1's three choices were redirect, chooser,
or the same dashboard with per-tile *not measured* sentences; it took the third and the reasoning is
written at the `<Show>` in `Overview.tsx:429-446` and on `NoClusterChosen:487-506`, including why a
redirect was refused (`soleClusterChoice` already decided that) and why the tiles were kept rather than
removed (a page that draws no tiles satisfies the new assertion vacuously — which §4.1 shows is still
the loophole, one tile in the other direction). The body — seven cards each drawing a heading and
nothing else, which the brief also measured — is gone entirely, replaced by the empty state. The tab
strip correctly does not draw (`tabs()` is empty with no cluster).

**Numbers, checked one at a time:**

| Claim | Verdict |
| --- | --- |
| 45 files / 1,023 cases; 83 / 1,927 workspace; 106 / 3 / 0 e2e; 790 × 2 a11y | **all four reproduce exactly** |
| "9 of the 15 extra cases are mine (3 in `record.test.ts`, 6 in `overview.render.test.tsx`)" | **correct** — `git diff ee81a440 \| grep -c "^+  it("` gives 3 and 6 |
| §3: "**Five** new cases in `record.test.ts`" | **wrong, and it contradicts the packet's own acceptance note.** Three new `it()` blocks (carrying nine `expect`s). Harmless, but it is a stated number and it is off. |
| `assertionAddedAt … overview.render.test.tsx:800` | **:803**. Off by three. |
| `assertionAddedAt … e2e/shell.spec.ts:499`, called from `:66` | **correct** — confirmed by the mutant's own stack trace |
| `TopicListPage.tsx:771` still exports a second `formatBytes` with the old unrounded branch | **correct**, verbatim: `${unit === 0 ? String(value) : value.toFixed(1)}` |
| "four importers inside `feature-topics`" | **correct** (`TopicStatisticsRegion`, `TopicOverviewTab`, `TopicPartitions`, the list page) — and **understated**: it is also re-exported from `feature-topics/src/index.tsx:18`, so it is part of that package's public surface, not merely internal. Its `units` list also runs to `PB` where the kernel's stops at `TB` (§4.5), so the two disagree about more than rounding. |
| `topics.test.tsx:389-391` tests that copy | **correct** |
| `ClusterSelector.tsx` — 214 lines, zero production callers | **correct** on both counts; note it *is* exported from `packages/shell/src/index.ts:14`, so deleting it is an API removal, not a dead-code sweep |
| Item 6: both W9-04 cases already at `ee81a440`, brief is stale | **correct, and I re-verified rather than believed it.** `controls.test.tsx:502` and `:517` exist at baseline, the file is untouched by this packet, and `data-state={props.dataState ?? "RUNNING"}` on both branches of `StatusPill.tsx` gives **1 failed \| 49 passed**. Nothing was owed. |
| "the three requests are unchanged: `/auth/me`, `/auth/settings`, `/capabilities/stream`" | **correct**, measured on my own probe |

**Is a zero or a bare em dash drawn where the promise requires a sentence?** On the live dashboard
(`/ui/clusters/quickstart/dashboard/overview`, `networkidle` + 4s) I swept every `<td>`: **1,159 cells,
0 with three or more decimal places, 0 zero-looking byte figures, 0 figures with six or more decimals
anywhere on the page**. The stat row reads `1`, `14`, `100.0%`, `1.2 kB/s`, `179.7 B/s`, `17`. The
packet's `38.1 B/s` is simply a different moment on a live cluster; the *shape* it claimed is what
ships. The `formatBytes` repair holds.

One observation, **pre-existing and not this packet's**: 1,144 bare `<td>—</td>` cells, 572 each in
the accessible data tables under `panel-throughput` and `panel-latency`, carry no `aria-hidden` and no
visually-hidden alternative — unlike `feature-topics`'s own `Quantity`, which pairs its dash with
`<span class="kui-visually-hidden">not known</span>`. `ThroughputCard.tsx:19` documents the dash as
deliberate for an unmeasured bucket, and the a11y sweep is green because the sweep reads stories, not
this table. Recorded for W10-A2 as an observation, not charged to W10-01.

**Orphans:** everything created has a caller. `STAT_ORDER`/`STATS`/`StatId`/`StatHead` → `StatRow` and
`NoClusterChosen`; `NO_CLUSTER_LEDE` → the header; `NoClusterChosen` → the `<Show>` fallback;
`StatShell` → two callers (`RateNote`, `StatNote`); `StatNote` → `NoClusterChosen`; `formatMagnitude`
→ `formatBytes`; `eachTileSaysSomething`/`STAT_TILES` → `shell.spec.ts:66`;
`data-testid="no-cluster-manage"` and `overview-no-cluster` → the render cases. The one symbol with a
single reference, `NoClusterInTheAddress`, is a Storybook export discovered by glob and swept by the
a11y run — not an orphan.

---

## 8. For W10-A2 — the rows to carry, in order

1. **§4.1** — the seventh blank tile. Green in all three suites, measured in a browser. Needs
   `export` on `STAT_ORDER`/`STATS` for the jsdom half (rule 22 seam, named); the e2e count assertion
   needs nothing and closes the browser half on its own. **This is the wave's shape repeating inside
   its own repair** and it should be the first thing closed.
2. **§4.2** — the empty state with every word removed, green in both suites. One `toContain`.
3. **§3** — the swapped tile subjects the packet disclosed. One `toContain`, already specified.
4. **§4.3** — the reversed row. One `toEqual` over document order; folds into 1.
5. **§4.6** — the enabled *Create topic* button that does nothing on the screen that just said nothing
   is selected. Needs one `Show` in `Overview.tsx` (seam named) plus a `toBeNull`.
6. **§4.4** — the six labels, gated only from `e2e/traffic.spec.ts`, W10-02's file. Bring them under
   the owning suite.
7. **§4.5** — `formatBytes` prints `1500.0 TB` while its own new header promises `[0, 1000)`. Either
   add `PB` or correct the sentence; assert whichever.
8. **Carried forward from the packet, unowned:** the duplicate `formatBytes` at
   `TopicListPage.tsx:771` (now also known to be re-exported from that package's `index.tsx` and to
   carry a different unit ladder) → TECH_DEBT, W10-03; and `ClusterSelector.tsx`, 214 lines, no
   production caller, exported from the shell's public index.
