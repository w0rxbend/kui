# W14-02 — verification

**Packet:** W14-02 — *The eight sentences, and the decision about `screens/`*
**Verified:** 2026-09-12, against working tree at baseline `3bd6c8cf`
**Suite command used throughout:** `./scripts/feature-matrix-check.sh`
**Verdict:** the work is real and the owned rule is genuinely closed. Eight of the eight items
re-measure correctly, `ADR-057` is substantive and every piece of evidence it quotes verifies. But
**the gate closes one document out of seven**, and the packet disclosed two of the ten ungated
reverts that actually stay green.

---

## 0. A note on the checkout, which affects how these numbers should be read

`scripts/feature-matrix-check.sh` was being rewritten by W14-01 *while this verification ran*. Two
runs returned exit 127 (`line 3889: ------------------: command not found`,
`line 4796: ---…---: command not found`). I polled `md5sum scripts/feature-matrix-check.sh` at 10 s
intervals until it held the same digest for eight consecutive samples
(`1d0bc47823b1c97e0aea84b99231b6b1`, 4,932 lines) and **re-ran every mutation below on that stable
script**. No result in this file was taken from a run against a script that was moving. W14-02's own
report makes the same observation and is right to.

---

## 1. Acceptance, re-run

| Acceptance item | Result |
| --- | --- |
| `./scripts/feature-matrix-check.sh` green | **Reproduced.** exit 0, `458 claims checked, all true.` Sections: self-check 7, rows 30, merged-document 50, milestones 49, adr-index 114, openapi-totals 15, guard-fixtures 29, capability-claims 44, quotations 12, debt-register 3, listings 12, gate-table 5, dependencies 88 over 12 manifests. |
| item 4.1 `screens/` out of §16 | **Reproduced.** `sed -n '/^kui\/$/,/^```$/p' ARCHITECTURE.md \| grep -c 'screens/'` → `0`; `git ls-files screens \| wc -l` → `0`. |
| item 4.2 `.github/` in, all 29 top-level names | **Reproduced.** `git ls-files \| cut -d/ -f1 \| sort -u \| wc -l` → `29`; the published omission loop prints nothing. **But the loop itself is unsound — see V-13.** |
| item 4.3 AU-005 | **Reproduced.** `git ls-files \| grep -i usermenu` → nothing. `frontend/packages/shell/src/chrome/AccountMenu.tsx` exists and the row's description of it (name, `authType` line, `POST /api/v1/auth/logout`, reload, failed sign-out keeps the panel open, nothing rendered for an anonymous principal) is faithful to the file, scaladoc and JSX both. `grep -rn roles frontend/packages/shell/src` → one hit, a comment in `SignIn.tsx`. |
| item 4.4 AU-001 | **Reproduced.** `git ls-files frontend/ui-shell` → `0`. |
| item 4.5 `research/design/gaps.md` | **Reproduced.** `git log --all -- 'research/design/gaps.md'` empty; `git ls-files research/design` → `REFERENCE.md SCREENS-V4.md SCREENS.md`; `grep -c 'research/design/gaps.md' docs/frontend/README.md` → `2`, both inside the correcting paragraph. |
| item 4.6 `kui.ui.*` residue | **Reproduced as measured, incomplete as disclosed.** `grep -c '^```scala' docs/frontend/README.md` → `0`; `tail -n +40 … \| grep -c 'kui\.ui\.'` → `1`. All four cited suites confirmed absent (`git ls-files '*FeatureStateSuite.scala'` etc. → 0 each). `hideForbidden` confirmed live at `frontend/packages/kernel/src/data/capabilities/featureState.ts:127` and `frontend/packages/shell/src/nav/navigation.ts:63`. **The banner's section-by-section list is still short — see V-14, V-15.** |
| item 4.7 `SCREENS-V4.md` | **Reproduced.** `grep -c 'committed, twenty-three'` → `0`; `git log --all -- 'screens*'` empty; `.gitignore:43` is `screens/` and its three comment lines are quoted verbatim in ADR-057. |
| item 4.8 `docs/plan/README.md` | **Reproduced.** `grep -c '(\.\./\.\./screens)'` → `0`. |
| `git ls-files` sweep of backticked paths, survivors argued | **Spot-checked and sound as a one-time measurement.** It is not a mechanism: V-04 shows a fresh phantom path in the same document goes green. |
| `ADR-057` in `docs/adr/` and its row in `DECISIONS.md` | **Reproduced.** Deleting the `DECISIONS.md` row goes **red** (`DECISIONS.md links ADR-057 to nothing`, plus an `adr-index` section-count refusal and a `gate-table` refusal). The ADR file is **untracked** — `git ls-files 'docs/adr/ADR-057*'` → 0 — so the row is green only because the checker walks `find docs/adr`. W14-02 filed this to W14-01 and was right to. |
| no figure in an owned file a command contradicts | **Two survive: V-16 and V-17.** |

## 2. The owned rule — genuinely closed

Re-applied by me, one at a time, on the stable script, each restored from bytes saved before the
mutation (`md5sum ARCHITECTURE.md` → `000f3d24fa8af0cdcda562a106f72d52` before and after both).

| Direction | Exact mutation | Result |
| --- | --- | --- |
| phantom | insert `├── screens/     the captures the browser suite is checked against` above the `├── scripts/` line of `ARCHITECTURE.md` §16 | **RED**, exit 1 — `ARCHITECTURE.md (checked: listings #1) asserts [screens/] as a name in the repository layout and \`git ls-files\` carries no such name.` |
| omission | delete the `├── .github/     workflows/ci.yml and actions/setup-build/ …` line | **RED**, exit 1 — `` `git ls-files` carries [.github] and ARCHITECTURE.md (checked: listings #1)'s listing does not name it.`` |

`git -C /home/worxbend/kui diff --stat 3bd6c8cf` confirms `ARCHITECTURE.md | 59 ++++-`, unchanged
from before the mutation pass. **`ownedRuleGenuinelyClosed: true`.**

Worth recording against the packet's own framing: at `3bd6c8cf` the block *did* still carry
`├── screens/` and *did not* carry `.github/` (`git show 3bd6c8cf:ARCHITECTURE.md | sed -n
'/^kui\/$/,/^```$/p'`). So items 4.1 and 4.2 were **open** when this packet arrived, contrary to
`WAVE-14.md`'s note that a concurrent session had repaired them. The packet's brief required it to
*"say in your report which rows were already closed when you arrived"*; the report never states one
way or the other. It happens to have been right to repair them.

---

## 3. Findings — one row each

Every mutation below was applied by me, run against the stable checker, and reverted from bytes
saved first; `md5sum -c` over all seven owned files passes at the end of this file.

| # | File | Exact mutation | Suite command | Result | The case that would close it |
| --- | --- | --- | --- | --- | --- |
| **V-01** | `research/design/SCREENS-V4.md` | `**Where the images are, and they are not here.** Twenty-three PNGs named by capture time, in a` → `**Where the images are.** \`screens/\`, committed, twenty-three PNGs named by capture time, in a` | `./scripts/feature-matrix-check.sh` | **GREEN**, exit 0, 458/458 | *Disclosed by the packet.* A claim that resolves every backticked repository path in **any** swept document, so `research/**` stops being outside every region. Not a third hand-named file. |
| **V-02** | `docs/plan/README.md` | restore `The working plan for turning this repository into the product drawn in\n[\`screens/\`](../../screens) and read in` | same | **GREEN**, 458/458 | *Disclosed.* Same derivation as V-01; `docs/plan/**` carries no `<!-- checked: -->` region at all. |
| **V-03** | `docs/FEATURE_MATRIX.md` AU-005 | `The account menu exists as \`frontend/packages/shell/src/chrome/AccountMenu.tsx\`: it names the signed-in user, says which mechanism signed them in,` → `The account menu now exists (\`layout/UserMenu.scala\`): it names the signed-in user, lists the roles they hold,` — i.e. **the exact wave-13 defect item 4.3 exists to repair, restored verbatim** | same | **GREEN**, 458/458 | **Not disclosed, and the sharpest one here.** `listing_absent_paths=("layout/UserMenu.scala")` (`scripts/feature-matrix-check.sh:4724`) exempts the *token* unconditionally, so the roster that was added to let AU-005 cite the dead path **inside the sentence saying it is dead** equally licenses citing it in the present tense as live. Close it by making the exemption conditional on the citing row: the roster entry carries the sentence fragment that must accompany it (e.g. `git ls-files | grep -i usermenu` or *"ADR-048 deleted"*) within the same row, and the exemption lapses when that fragment leaves the row. Owner: W14-01 / W14-A1 (the roster is theirs). Control: **V-04'** — the *other* direction is gated; restoring AU-001's `frontend/ui-shell/.../page/LoginPage.scala` as evidence goes **red** (`docs/FEATURE_MATRIX.md cites [frontend/ui-shell/.../page/LoginPage.scala] as files in this repository`). AU-005 is green only because of the roster. |
| **V-04** | `docs/frontend/README.md` | `frontend/packages/shell/src/health.ts` → `frontend/packages/shell/src/healthz.ts` (one character, a path the banner's `Where the wire meets the kernel` example cites) | same | **GREEN**, 458/458 | **Not disclosed.** The banner asserts *"Every path this page names is resolved against `git ls-files` and not against a working tree"* as a **property of the page**; it is a one-time sweep. Close it with an `audit_cited_paths`-shaped reader over `$frontdoc` — the mechanism already exists for `docs/FEATURE_MATRIX.md` and needs only a second document in its scope. Owner: W14-01 / W14-A1. |
| **V-05** | `docs/frontend/README.md` | `**Three preferences, all of them browser-local**` → `**Four preferences, all of them browser-local**` | same | **GREEN**, 458/458 | **Not disclosed.** This is the figure item 4 of the packet's own brief repaired (it said *Four* and listed five). It went back to being wrong for free. Close it with a `listing-count` claim over the preference table: the bold word against `wc -l` of the table's rows, inside a `checked: listings` region. |
| **V-06** | `docs/frontend/README.md` | ReasonCode table row `\| \`CircuitOpen\` \| \`CIRCUIT_OPEN\` \| KUI has paused its calls while the cluster recovers \|` → `… \| KUI has stopped calling the cluster for good \|` | same | **GREEN**, 458/458 | **Not disclosed, and it is the same defect the packet repaired.** The table is *again* a hand copy of generated `ReasonSentences` (`frontend/packages/api/src/constants.generated.ts`), which had already drifted from all eight once. Close it by comparing the table cell-for-cell against `ReasonSentences` — the generated file is committed and parseable with one `sed`; this is a `quotations`-shaped claim over a generated artefact, not a roster. |
| **V-07** | `docs/frontend/README.md` | banner: ``(`grep -c '^```scala' docs/frontend/README.md` → `0`)`` → `… → `7`)` | same | **GREEN**, 458/458 | The banner publishes its own greps as the measurable half of its disclosure, and nothing re-runs them. Close it by re-running the two published commands and comparing their output to the numbers beside them — the same shape the `gate-table` block already uses for the checker's own size. |
| **V-08** | `README.md` | restore `…left in a comment inside a\n3,700-line shell script.` | same | **GREEN**, 458/458 | **Not disclosed.** The very sentence the packet rewrote because the number rots takes the rotten number back silently. Close it with the `README.md` banner inside a checked region, or delete the figure rather than narrating it (see V-17). |
| **V-09** | `research/design/SCREENS-V4.md` | `**Four** captures — \`M05\`, \`M09\`, \`M19\` and \`M20\` —` → `**Three** captures — …` | same | **GREEN**, 458/458 | The internal-consistency defect the packet repaired (a count contradicted by the list beside it) is reintroducible for free. Close it with a `listing-count` claim over the bold word against the number of backticked `M`-ids in the same sentence. |
| **V-10** | `research/design/SCREENS-V4.md` | head reverted to `held in\n\`screens/\` in this repository` (the `:3` half of item 4.7) | same | **GREEN**, 458/458 | Same derivation as V-01. Filed separately because the packet's disclosure named only the `:34` half; **`:3` is the second sentence in the same file and is equally open**. |
| **V-11** | `docs/overview/README.md` | `` `DECISIONS.md` is that, and it lists all fifty-seven.`` → `… all fifty-five.` | same | **GREEN**, 458/458 | **Not disclosed, and the cleanest close in this file.** The *same document*, 200 lines lower, publishes `**57 rows**, one per ADR on disk` **inside** the `checked: gate-table` region, where it is read back and refused. The prose figure the packet edited this wave (56 → 57) is the ungated copy of a figure the file already gates. Close it by moving the sentence inside a checked region, or by adding a `gate-decisions-rows` comparison over the spelled-out numeral in §head. |
| **V-12** | `docs/overview/README.md` | `` `git ls-files services \| cut -d/ -f2 \| sort -u` gives eleven directories:`` → `… gives thirteen directories:` (and, independently, `` `git ls-files 'frontend/packages/*/package.json'` gives eleven packages:`` → `… nine packages:`) | same | **GREEN** both, 458/458 | **Not disclosed.** The packet's item-4 repair here converted `ls` to `git ls-files` — the derivation is now honest and the **count beside it is read by nothing**. A reader is told the command and given a number the command contradicts. Close it with `listing-count` claims over the two spelled-out numerals against the two commands quoted beside them. |
| **V-13** | `ARCHITECTURE.md` §16 | delete the standalone `mill` token from `├── build.mill  mill  .mill-version  .tool-versions  .gitignore` | the **loop §16 itself publishes** as the reader's proof | **the published command prints nothing** (says the listing is complete) while `./scripts/feature-matrix-check.sh` correctly refuses: `` `git ls-files` carries [mill] `` | **Not disclosed, and it is TD-051's own class.** §16 says *"The omission direction … is one command, and it printed nothing on 2026-09-12"* and publishes a `grep -qF -- "$name"` loop. `grep -qF mill` is satisfied by `mill-build/`, so the published command is silent for a listing that has genuinely lost `mill`. W14-01's checker says so in a comment at `check_repository_layout_region` (*"The token test and not a substring test. `grep -qF mill` — which is what §16's own published command does — is satisfied by `mill-build/`"*) — the script knows; the document does not. Close it by publishing the token form: `grep -qxF "$name" <<<"$(… | tr -s ' ' '\n' | sed 's,/$,,')"`, or by replacing the loop with the sentence *"the gate does this, and here is the section it reads"*. This is W14-02's own prose and W14-02's to fix. |
| **V-14** | `docs/frontend/README.md` | none needed — read, not mutated | `git ls-files \| grep -ic Sidebar` → `0`; `… -ic CapabilityBanner` → `0`; `grep -rn 'export function derive' frontend/packages/kernel/src/data/capabilities/featureState.ts` → `deriveFeatureState` | n/a | **The banner's section-by-section disclosure is short by two sections, which is the exact defect item 3 exists to end.** *The rendering rules, and where each one lives* (`:674`) promises *"names the file that draws it, so a wrong pixel can be traced to a line without a search"* and names `Sidebar`, `CapabilityBanner` and `FeatureFallbackPanel` — **none of the three has a file** — plus `FeatureState.derive`, which is `deriveFeatureState`. `Rendering a \`Section\`` (`:741`) publishes `Ok(data, fetchedAt)`, `Stale(data, fetchedAt, reason)`, `Unavailable(reason, message, since)`, `NotConfigured` — Scala constructor syntax for a TypeScript discriminated union whose real shape is `{ status: "ok" \| "stale" \| … }` in `frontend/packages/api/src/section.ts:44`, and which carries a **sixth** member (`"unreadable"`) the table does not list. Neither section is in the banner's *"what is not"* list and neither is in its *"reconciled"* list. Close it by adding both to the banner's named list, with `Sidebar` and `CapabilityBanner` in it. |
| **V-15** | `docs/frontend/README.md` `:20` | none — read | `grep -n 'kui\.ui\.' docs/frontend/README.md` → two lines, `:20` (the note) and `:895`; `:895` carries **two** identifiers (`kui.ui.kernel.prefs` and `kui.ui.kernel.theme`) | n/a | The banner says *"**no `kui.ui.*` identifier** outside this note, where **the one** below is a quotation"* and proves it with `grep -c`, which counts **lines**, not identifiers. There is one line and there are two identifiers, and the sentence contradicts itself (*"no identifier outside this note"* … *"the one below"*). Close it with `grep -o 'kui\.ui\.[A-Za-z.]*' \| wc -l`, and say *two*. |
| **V-16** | `docs/plan/README.md:23` | none — read | `ls docs/plan/verification \| wc -l` → **36**; `git ls-files docs/plan/verification \| wc -l` → **34** | n/a | The sentence says *"`verification/` holds **thirty-four** files"* with no command beside it. It is true of the index and false of the working tree **right now**, and this wave will make it false of the index too (W14-01.md, W14-03.md and this file are untracked). House rule 27's shape in a figure the packet wrote this wave. Close it by writing the command instead of the number, exactly as the packet did for the checker's length in `README.md`. |
| **V-17** | `README.md:194` | none — read | `wc -l scripts/feature-matrix-check.sh` → **4,932** | n/a | The repair for the 3,700-line figure narrates a second figure — *"a script that had grown to **4,446**"* — which was already 4,932 by the end of the same wave. The sentence's own argument (*"a number that moves every time a claim kind is added is a number nobody re-takes"*) applies to it. Close it by deleting the second number; the command alone carries the point. |
| **V-18** | `docs/adr/ADR-057-…md` §3.3 | none — read | `grep -cE '^\| M[0-9]{2} \|' research/design/SCREENS-V4.md` → `23`; `ls screens \| wc -l` → `23`; `git ls-files screens` → `0` | n/a | ADR-057 proposes moving definition-of-done item 1's denominator from `screens/` to *"the twenty-three screens indexed as `M01`…`M23` in `research/design/SCREENS-V4.md` §1"*. That index is a **23-row hand table of file names in an uncommitted directory**, reconciled by nothing — it is a roster of the exact shape house rule 26 abolishes, and it is proposed as the subject of the project's first exit criterion. The proposal is still better than the status quo; it should arrive with the claim that keeps the table at 23 rows and its ids contiguous. For the integrator, beside the `ROADMAP.md` wording. |

### The controls that went red, for contrast

| Mutation | Result |
| --- | --- |
| `ARCHITECTURE.md` §16 phantom `screens/` | **RED** (§2 above) |
| `ARCHITECTURE.md` §16 omission `.github/` | **RED** (§2 above) |
| `docs/FEATURE_MATRIX.md` AU-001 dead path restored as evidence | **RED** — `docs/FEATURE_MATRIX.md cites [frontend/ui-shell/.../page/LoginPage.scala] as files in this repository and \`git ls-files\` does not carry them.` |
| `DECISIONS.md` ADR-057 row deleted | **RED** — three refusals: the missing link, the `adr-index` section count (113 vs 114) and the `gate-table` figure (457 vs 458). |

**So the gate covers exactly one of the seven owned documents' repairs** (`ARCHITECTURE.md` §16),
plus the pre-existing `docs/FEATURE_MATRIX.md` cited-path reader and the pre-existing `adr-index`
comparison. `research/design/SCREENS-V4.md`, `docs/plan/README.md`, `docs/frontend/README.md`,
`docs/overview/README.md` and `README.md` carry **ten** repairs between them that revert green.

---

## 4. Ownership

`git -C /home/worxbend/kui diff --name-only 3bd6c8cf` lists 14 modified files plus 4 untracked.
**No ownership violation attributable to W14-02 was found.**

* Outside W14-02's `Owns` and changed: `TECH_DEBT.md` (W14-03 rows / W14-01 markers),
  `deployment/storybook/Dockerfile` (W14-03), `scripts/feature-matrix-check.sh` (W14-01),
  `docs/plan/WAVE-14.md` and `docs/plan/WAVE-13.md` (deleted) (integrator),
  `docs/plan/ROADMAP.md` (+223, integrator).
* `docs/plan/ROADMAP.md` is the one that had to be checked, since W14-02 states it made no edit
  there. It carries **no** W14-02 fingerprint: `grep 'ADR-057\|AccountMenu\|4,446\|M01…M23'` over its
  diff hunk finds nothing; the added text is the wave-13 close retrospective. The two `screens/`
  sentences W14-02 filed to the integrator (`ROADMAP.md:4` and the definition-of-done item 1 at
  `:26`) are **still there, unedited**, which is the correct outcome for a packet that does not own
  the file.
* `repo.txt` was added to `ARCHITECTURE.md` §16's listing (correctly — the claim is bidirectional)
  and filed as a `TECH_DEBT.md` row for W14-03; `grep -c 'repo.txt' TECH_DEBT.md` → `0`, so the row
  is **not yet written**. That is W14-03's to close, not a W14-02 violation.

## 5. Guard files, registries and hand-written mirrors this packet's work rests on

| Artefact | Shape | State |
| --- | --- | --- |
| `scripts/feature-matrix-check.sh:4724` `listing_absent_paths=("layout/UserMenu.scala")` | one-entry hand roster, reconciled in both directions | **Load-bearing for AU-005 and the reason V-03 stays green.** Also a trap in the other direction: a future packet that finishes the repair by *removing* the dead path from AU-005 turns the roster red (`no row cites it`). |
| `docs/frontend/README.md` ReasonCode table, 8 rows | hand mirror of generated `ReasonSentences` | **Ungated — V-06.** Drifted once already; the packet's fix was a fresh hand copy. |
| `docs/frontend/README.md` preference table, 3 rows + a bold count | hand mirror of `localStorage` keys | **Ungated — V-05.** |
| `docs/frontend/README.md` `Section` table, 5 rows | hand mirror of the `Section<T>` union in `frontend/packages/api/src/section.ts` | **Ungated and already short by one member** (`"unreadable"`) — V-14. |
| `research/design/SCREENS-V4.md` §1, 23 rows `M01`…`M23` | hand roster of files in an ignored directory | **Ungated, and proposed by ADR-057 as the subject of definition-of-done item 1** — V-18. |
| `ARCHITECTURE.md` §16 fence | the one listing with a derivation | **Gated, both directions.** |
| `docs/overview/README.md` `checked: gate-table` block | hand figures read back by the run | **Gated.** W14-02 correctly moved 444 → 458 and 12 → 13 sections and 56 → 57 rows. |

## 6. Orphans

Nothing created by this packet is orphaned. `ADR-057` is cited from `DECISIONS.md` (the row),
`docs/plan/README.md:8`, `research/design/SCREENS-V4.md` (twice) and `ARCHITECTURE.md:1200`; every
relative link resolves to
`docs/adr/ADR-057-design-captures-stay-outside-the-repository.md`, which exists. It is **untracked**,
so a clone taken today would have a `DECISIONS.md` row pointing at nothing — already filed by the
packet against `adr_files`' `find docs/adr` walk, and the wave's commit closes it.

## 7. Does it do what was asked, or merely exit zero

It does the work. The eight sentences are genuinely repaired and each one re-measures. `ADR-057` is
not a formality: the `.gitignore:43` comment and `research/design/SCREENS.md:17` are quoted verbatim
and both verify, the rejected options carry real reasons (the resampling argument against a reduced
set is the strongest sentence in the file), and §3 states the cost instead of glossing it. AU-005 is
now a faithful reading of `AccountMenu.tsx` — the refused-sign-out behaviour, the reload, the absence
of a role list and the anonymous-principal case are all in the file. Four further false statements
the brief did not ask for were found and repaired.

Two things are less than the report says:

1. **The report's §4 disclosure (house rule 5) names two ungated repairs. There are at least ten.**
   V-03 through V-12 all revert green, and V-03 restores the exact wave-13 defect this packet
   existed to repair. The report's framing — *"the class this wave was written against is closed in
   one document and open in two"* — should read **open in five**.
2. **§16 publishes a command that runs and lies (V-13)**, in the section whose repair is this
   packet's one gated deliverable, and the packet's own surprises list says it caught and rejected a
   published-command defect there. It caught the `diff <(…) <(…)` one and shipped a different one.

No panel renders a zero or a bare em dash where a sentence is required: `docs/FEATURE_MATRIX.md`'s
`—` cells are the matrix's existing convention for *no MFE / no owner*, not new output, and
`docs/frontend/README.md`'s derivation tables use `—` only in the `NotConfigured` row, which is the
row whose whole point is that nothing is drawn.

## 8. Restoration

Every mutation in this file was applied to a copy-on-disk backup taken first and restored with `cp`,
never with `git checkout`/`restore`/`stash`. Final state:

```
$ md5sum -c md5.orig
research/design/SCREENS-V4.md: OK
docs/plan/README.md: OK
docs/FEATURE_MATRIX.md: OK
docs/frontend/README.md: OK
README.md: OK
DECISIONS.md: OK
docs/overview/README.md: OK
$ md5sum ARCHITECTURE.md
000f3d24fa8af0cdcda562a106f72d52  ARCHITECTURE.md
$ ./scripts/feature-matrix-check.sh
feature-matrix-check: 458 claims checked, all true.      # exit 0
```
