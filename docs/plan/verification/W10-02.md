# W10-02 — verification

Adversarial re-verification of packet **W10-02** ("The four browser cases that pass without asking
anything"), against the working tree at baseline `ee81a440`, on 2026-09-11/12. Every figure below
was printed by the command beside it, by me, in this pass. Every mutation named here was applied by
me through a rewriting reverse proxy and reverted by me; nothing was written to disk, to a container
or to an image, and `git diff --stat ee81a440` is back to **47 files changed, 3,069 insertions(+),
373 deletions(-)**, with the three W10-02 files carrying exactly the stats they carried when this
pass started (`brokers.spec.ts` +51, `search.spec.ts` +91, `topics.spec.ts` +47/-4).

**Images.** The pair moved twice under me while other packets rebuilt. Acceptance section 1 was
taken on `kui-allinone 8696c6568f42` / `kui-frontend adb9024e7fd0` (bundle `index--ZOmurSX.js`,
`feature-clusters-BW9eOdYe.js`, `feature-topics-BoliTiqR.js`). Everything from section 2 onward was
taken on `kui-allinone 8696c6568f42` / `kui-frontend 44c3a3d51447` (bundle `index-DdoKviY8.js`,
`feature-clusters-CxHxSXER.js`, `feature-topics-r5T7U45s.js`), after another packet tore the
quickstart down mid-pass and brought it back. Every mutation string below was located in *that*
bundle by me, not transcribed from the packet's report, and each was asserted to occur exactly once
before it was substituted.

**Method.** A 70-line Node reverse proxy on `:8099` streaming everything to `:8090` untouched except
JavaScript bodies, which are buffered and substituted. Two details matter and the packet's own
report does not mention them: conditional request headers must be stripped (`If-None-Match` →
nginx 304 → empty body → a silent no-op substitution, which is a green run that means nothing) and
`Accept-Encoding` must be forced to `identity`. My first proxy aborted loudly on the first 304
rather than passing a mutation that had not landed; a proxy that did not count its own
substitutions would have reported a false green.

---

## 1. Acceptance, re-run

| # | Command | Result |
| --- | --- | --- |
| 1 | `pnpm -C frontend e2e` | `109 tests … 3 skipped, 106 passed (1.6m)`, exit 0. Skips: `alerts.spec.ts:268`, `ksql.spec.ts:175`, **`shell.spec.ts:181`** |
| 2 | `pnpm -C frontend e2e e2e/brokers.spec.ts` | `11 passed (17.8s)`, case 11 is `brokers.spec.ts:371` |
| 3 | `pnpm -C frontend e2e e2e/search.spec.ts` | `7 passed (20.6s)`, case 6 is the new `search.spec.ts:196` |
| 4 | `pnpm -C frontend e2e` (final, after every mutation reverted, on the final image pair) | `109 tests … 3 skipped, 106 passed (2.1m)`, exit 0. Same three skips |
| 5 | `./node_modules/.bin/tsc -p e2e/tsconfig.json --noEmit` | exit 0 |
| 6 | `pnpm -C frontend typecheck` | exit 0 |

Rows 1–3 reproduce the packet's run (1) exactly, including the counts. **Row 1 does not meet the
acceptance the wave file states for this packet** — `# 105 passed -> 105+, skips 3 -> 2`. The count
went 105 → 106; the skips did not go 3 → 2. Item 4 of the packet's brief is open. See finding 1.

## 2. The owned rule — genuinely closed

The rule: *a cluster's brokers screen reads that cluster's own endpoints.* The mutation, located by
me in `feature-clusters-CxHxSXER.js` (one occurrence, verified before substitution): the brokers
route's three loaders pinned to the first cluster while their reactive keys are left alone.

```
load:()=>wt(t.api,e.clusterId)}),r=I({key:()=>`clusters/${e.clusterId}`,load:()=>Ct(t.api,e.clusterId)}),i=I({key:()=>`clusters/${e.clusterId}/log-dirs`,load:()=>At(t.api,e.clusterId)})
      ->  wt(t.api,`quickstart`)            Ct(t.api,`quickstart`)                  At(t.api,`quickstart`)
```

Measured in both directions, on the same proxy, in the same ten minutes:

| Spec bytes | Command | Result |
| --- | --- | --- |
| `git show ee81a440:frontend/e2e/brokers.spec.ts` | `KUI_E2E_UI=http://localhost:8099 pnpm -C frontend e2e e2e/brokers.spec.ts` | **`11 passed (14.3s)` — GREEN.** W9-03 F1 reproduced independently |
| the packet's repaired file | same command | **`1 failed, 10 passed (17.0s)`** at `brokers.spec.ts:371` |

The failure message is the one the repair promises, and the ledger holds exactly the three paths the
packet named and nothing else:

```
this screen asked another cluster's endpoints, so the figures above are that cluster's and
agree only because both profiles point at one broker
- Array []
+ Array [
+   "/api/v1/clusters/quickstart/brokers",
+   "/api/v1/clusters/quickstart",
+   "/api/v1/clusters/quickstart/log-dirs",
+ ]
```

Proxy stopped, `:8099` free, `frontend/e2e/brokers.spec.ts` md5 back to
`a9f1aadf96773d5ef2481bbe2e8a976c`. **ownedRuleGenuinelyClosed: true.**

I also checked the packet's harder claim — that a `waitForRequest` for the right URL would *not*
have closed this — and it is correct in kind: the drawer's tree and the drawer foot's storage meter
issue `/api/v1/clusters/<current>/…` on every navigation, so the right request is on the wire under
the mutation too. The ledger's discriminator is whose id a request carried, not that one happened.

## 3. The other three repairs, mutated

All four of these went red, so none of them is in `newUngatedRules`. They are recorded because a
repair nobody re-mutated is a repair nobody has checked.

| Mutation | Bundle edit | Command | Result |
| --- | --- | --- | --- |
| drop `searchStatus`'s **partial** guard | `ig(e.answer)===0&&e.answer.partial.length===0?` → `ig(e.answer)===0?` | `pnpm -C frontend e2e e2e/search.spec.ts` | **RED** at `search.spec.ts:133`, `expect(locator).not.toContainText` |
| bulk bar counts the **page** and not the selection | `f(ee,{get count(){return C().size},…,noun:\`topic\`` → `return e.topics.length` | `pnpm -C frontend e2e e2e/topics.spec.ts` | **RED** at `:396`, `Expected substring: "1 topic selected" / Received: "14 topics selectedExport…"` |
| same mutation, case `:459` alone | as above | `… e2e/topics.spec.ts -g "a bulk delete says how many it deleted"` | **RED**, `Received: "2 topics selectedExport…"` |
| bulk bar always plural | `n=()=>e.count===1?t.noun:…` → `n=()=>!1?t.noun:…` | `… e2e/topics.spec.ts -g "selecting rows raises the bulk bar"` | **RED**, `Received: "1 topics selectedExport…"` |
| empty state stops quoting the query | `u(t,()=>e.value,n)` → `u(t,()=>\`\`,n)` in the `status()==="empty"` arm | `pnpm -C frontend e2e e2e/search.spec.ts` | **RED** at `:196`, `Received: "Search topics, groups, anythingNothing matches “”."` |

Two notes a closer should carry forward. First, the file is `test.describe.configure({ mode:
"serial" })`, so `:396` going red **stops `:459` from running at all** — the packet's claim that
both bulk cases redden is true, but only when each is driven on its own. Second, the singular is
doing all the work at `:396`: the plural assertion there is still `toContainText("2 topics
selected")` over an unfiltered list, and a page of twelve rows would satisfy it. The substring trap
the packet documented is closed by the new line, not by the old one.

## 4. Ownership

`git diff --name-only ee81a440` lists five files under `frontend/e2e/`. Three are the packet's
(`brokers.spec.ts`, `search.spec.ts`, `topics.spec.ts`) and match what it reported. The other two
are not its work and do not read as its work: `shell.spec.ts` adds `eachTileSaysSomething` and a
`STAT_TILES` roster about the cluster dashboard's six stat tiles, which is W10-01's Overview repair;
`globalSetup.ts` adds `awaitFirstScrape`, which is W10-05's stack concern and cites
`brokers.spec.ts:157` only as the case it lost. **No ownership violations.** Nothing outside
`frontend/e2e/**` is attributable to this packet.

## 5. Guard files, registries and orphans

Nothing in the repository enumerates browser cases by name or count as a gate: `.github/workflows/
ci.yml` runs `pnpm e2e` and asserts nothing about the number; `scripts/feature-matrix-check.sh`
never mentions `e2e`; `build.mill` names Playwright only in a comment saying nothing depends on it.
The one enumeration that exists is prose — see finding 3.

**Orphans: none.** The packet created no exported symbol. `foreign` is read at the end of the case
that declares it; the new `search.spec.ts:196` case is collected by `testDir: "./e2e"`; the
`test.info().annotations.push` call has a consumer only in the HTML/GitHub reporters — see
finding 4.

---

## 6. Findings

| # | File | Exact mutation (or defect) | Suite command | The case that would close it |
| --- | --- | --- | --- | --- |
| 1 | `deployment/quickstart/kui-quickstart.yaml:117` / `frontend/e2e/shell.spec.ts:181` | **No mutation — the acceptance is unmet.** `diskUsedWarningPercent` is still `80` in the shipped quickstart. `shell.spec.ts:181` skipped in all four of my full runs. The wave file's acceptance for this packet is `skips 3 -> 2`; it is 3 | `pnpm -C frontend e2e` → `3 skipped, 106 passed` | Land `diskUsedWarningPercent: 1` in `deployment/quickstart/kui-quickstart.yaml` with the comment saying why (the packet's own recommendation, which I did not verify because the container is shared) and re-run. `M06`'s acknowledgement path is still driven by no browser |
| 2 | `frontend/packages/shell/src/data/search.ts:181` | `searchHitCount(state.answer) === 0 && state.answer.partial.length === 0` → **drop the left conjunct** (bundle: `ig(e.answer)===0&&e.answer.partial.length===0?\`empty\`:\`ready\`` → `e.answer.partial.length===0?\`empty\`:\`ready\``). A deployment whose services **all** answered now draws *"Nothing matches “orders”."* over a full result list | `pnpm -C frontend e2e` → **`3 skipped, 106 passed (2.2m)`, GREEN**, with 23 substitutions confirmed served. Also green over `search.spec.ts` + `features.spec.ts` (`23 passed`) | The packet's new case forces `partial: []` **and** picks a query that matches nothing, so both conjuncts agree and neither is pinned. A second case using the same `page.route` intercept over a **matching** query (`?q=orders`, `partial` rewritten to `[]`) asserting the result groups are drawn and `/Nothing matches/i` is absent. This is the "everybody answered and something matched" arm, which no browser drives on this deployment either |
| 3 | `frontend/packages/kernel/src/components/BulkActionBar.tsx:112` | `<Show when={props.count > 0}>` → `<Show when={true}>` (bundle: `return k(p,{get when(){return e.count>0},get children(){var r=di()` → `return k(p,{get when(){return!0},…`). The destructive bar — Export, Empty, Delete — floats over a list with **nothing selected**, reading `0 topics selected` | `pnpm -C frontend e2e` → **`3 skipped, 106 passed (2.2m)`, GREEN**, 328 substitutions served. `e2e/topics.spec.ts` alone: `20 passed (1.1m)` | `topics.spec.ts` references `topic-bulk-bar` twice and both are after a tick. The repaired `:396` opens with `await expect(bar).toBeVisible()` immediately after the first click, which is vacuous under this mutation. Add, before the first tick: `await expect(page.getByTestId("topic-bulk-bar")).toHaveCount(0)` — and after `Clear selection`, the same. TopicListPage's own comment states the rule ("absent at zero selection: a bar that is always in the document is a strip of the window nobody can use") and nothing asserts it |
| 4 | `frontend/e2e/search.spec.ts:150` | **No mutation — the annotation does not reach its reader.** `test.info().annotations.push({type: "arm not exercised", …})` is invisible under the `list` reporter, which is what `reporter: process.env.CI ? … : [["list"]]` selects for every local run, including the packet's own acceptance command. The stated purpose — *"what stops the reader of a green run from believing this case did"* — is served only in CI's HTML report | `pnpm -C frontend e2e e2e/search.spec.ts` prints seven ticks and no annotation | Either name the untaken arm in the test title the reporter does print, or replace the annotation with `test.info().annotations` **plus** a `console.warn`, or split the two arms into two titled cases so the run itself says which one this deployment took |
| 5 | `docs/FEATURE_MATRIX.md:713-722` | **Stale prose, now wrong.** *"Three of the twenty-three covering cases do not discriminate … Repairing them is wave 10's, and this paragraph is where the caveat lives until they are."* Two of the three are repaired by this packet and the third is annotated and joined by a real case. Line 484 also says `search.spec.ts` "drives five cases"; it drives **seven** | `wc -l`-free: read the file | W10-03 owns `docs/FEATURE_MATRIX.md`. The paragraph needs rewriting against this pass and the count at :484 needs re-taking. W10-02 did not flag it to W10-03 in its own hand-off, and a closer reading the matrix would still believe these three cases are vacuous |
| 6 | `frontend/tsconfig.json` / `frontend/e2e/tsconfig.json` | **Confirmed, and the packet's figure is wrong.** The solution tsconfig references ten packages and not `./e2e`; `e2e/tsconfig.json` sets `composite: false`, so it cannot be referenced as it stands. `pnpm -C frontend typecheck` is green over a tree it does not read. The packet reports "483 lines of browser suite are outside the type gate"; `wc -l frontend/e2e/*.ts` is **4,947** | `pnpm -C frontend typecheck` → exit 0; `./node_modules/.bin/tsc -p e2e/tsconfig.json --noEmit` → exit 0, and nothing in the repository runs the second | `composite: true` plus `{ "path": "./e2e" }` in `frontend/tsconfig.json`, or a `typecheck:e2e` script wired into the same CI step. Owned by nobody this wave |

### Not findings, checked and clear

* `brokers.spec.ts:371`'s ledger resets on the main frame's own navigation request, so the previous
  screen's in-flight requests are not counted; it is read after the last screen assertion. I could
  not make it produce a false red.
* The new `search.spec.ts:196` intercept rewrites exactly one field of one path, chosen by predicate
  rather than by glob (Playwright's URL globs treat `?` as a wildcard), and it carries a `served > 0`
  guard, so a product that stopped calling `/api/v1/search` reddens it rather than passing it.
* The `expect(drawn).toBeGreaterThan(2)` guard at `topics.spec.ts:409` is real: it is what stops
  "1 topic selected" from being satisfiable by a page that happens to draw one row.
* No panel involved in these repairs renders a zero or a bare em dash where a sentence is promised;
  the empty state is a sentence quoting the query back, and finding 4's mutation proves the quote is
  pinned.

---

Closing state: the working tree is the packet's, unmodified by this pass
(`brokers.spec.ts` md5 `a9f1aadf96773d5ef2481bbe2e8a976c`, `search.spec.ts`
`492c0dc1b796a702fc99d00b822c1485`, `topics.spec.ts` `bb6b39c6ae1b87aa96be1cb92fe25e74`), the proxy
is dead and `:8099` is free, and nothing of mine is left running on this machine.
