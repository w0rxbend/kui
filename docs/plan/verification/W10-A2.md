# W10-A2 — the closer. Every finding filed to wave 10, and what happened to it.

**Input.** `docs/plan/verification/W10-01.md`, `W10-02.md`, `W10-03.md`, `W10-04.md`, `W10-05.md`,
`W10-06.md` — the six files this wave's verification passes filed under house rule 18 — plus the
wave-8 and wave-9 files kept in this directory. **W10-A1 filed nothing**: there is no `W10-A1.md` in
`docs/plan/verification/`, so the hunter's twenty-odd rows named in the contract table were not
available to this pass. Its work was visible only as edits arriving in the tree while this pass ran
(see §5).

**Method, unchanged from wave 9.** Every closure below is a case that goes **red** under a mutation
this pass applied itself, against a suite named beside it, reverted from bytes saved before the edit.
No `git checkout --`, no `git restore`, no `git stash`. Eighteen mutations were applied across this
pass; every one was restored and every restore was confirmed by md5 (§5).

---

## 1. The denominator

| report | rows filed | closed here | argued down | handed back |
| --- | --- | --- | --- | --- |
| `W10-01.md` | 6 | **6** | 0 | 0 |
| `W10-02.md` | 2 | **1** | 1 | 0 |
| `W10-03.md` | 4 (F-1…F-4) | 0 | 0 | 4 |
| `W10-04.md` | 5 (F1–F4, F7) | **5** | 0 | 1 partial (F2's seam) |
| `W10-05.md` | 5 | 0 | 0 | 5 |
| `W10-06.md` | 3 | **3** | 0 | 0 |
| **total** | **25** | **15** | **1** | **9** |

**Two percentages, per the contract.**

* Closures over **all** filed rows: **15 / 25 = 60%**.
* Closures over rows whose closing case was **inside this packet's ownership** (the test trees and
  `docs/plan/verification/`): **15 / 16 = 94%**. The sixteenth is W10-02's bulk-bar row, which is
  argued down rather than closed because the rule is already gated where it lives.

Rule 21 worked. Wave 9's closer converted **12 of 12** inside its partition and **0 of 28** outside
it, and the first percentage was 28%. Widening the partition to the test trees moved the first figure
from 28% to 60% without moving the second. **What is left outside is not a test file at all**: a
shell script's claim machinery, a `build.mill` task, a CI step, a Dockerfile, a compose smoke script
and two `tsconfig`/`package.json` entries. All nine handed-back rows need an edit in one of those,
and so do the three partials in §4.

---

## 2. Findings CLOSED, one row per finding

Each row: the case that landed, the mutation re-applied to the **production** source, the suite, what
it printed, and the md5 the file was restored to.

### W10-01 — the six tiles, the row and the empty state (6 of 6)

One case carries four of them, and it is the one W10-01's report asked for: the row is read **off the
DOM** rather than looped over a literal of six ids, so a seventh tile, a reordering, a renamed label
and a tile with nothing under it all fail in the same place.

| finding | case landed | mutation | result |
| --- | --- | --- | --- |
| **seventh blank tile** | `overview.render.test.tsx` — *draws exactly the six tiles, in order, each carrying words and not a bare head*: `[...container.querySelectorAll(".kui-overview__stats > .kui-stat")]`, compared against the roster and then read tile by tile | `Overview.tsx` — `seventh: { head: { label: "REPLICATION", … }, question: "" }` added to `STATS`, `"seventh"` added to `STAT_ORDER`, and `NoClusterChosen`'s `why` made `undefined` for an empty question (W10-01's exact mutation) | **RED** — `Tests 1 failed | 99 passed (100)` |
| **the row's order** | the same case | `STAT_ORDER` reversed | **RED** — 1 failed / 100 |
| **the six labels** | the same case (`.kui-stat__label` read in order) | `label: "BROKERS ONLINE"` → `"BROKERS"` | **RED** — 1 failed / 100 |
| **the empty state's words** | *offers the choice rather than making it* — now asserts the text contains `No cluster is selected` and `environment rail` | `title=""` and the two-line `description` → `"" + ""` | **RED** — 1 failed / 100 |
| **production ↔ consume** | *gives each tile its own subject and not its neighbour's* — six notes, each read for its own subject | the two `question` strings swapped | **RED** — 1 failed / 100 |
| **`formatBytes` top-unit cap** | `record.test.ts` — *stops at the top unit rather than dividing off the end of the scale* | `unit < units.length - 1` → `unit < units.length` | **RED** — `expected '1.5 B' to be '1500.0 TB'`, 1 failed / 18 |

Suite: `pnpm -C frontend vitest run packages/shell/src/overview/overview.render.test.tsx` and
`… packages/kernel/src/components/record.test.ts`. Reverts: `Overview.tsx` md5
`f2a65278178d65337b18cbaffa291f9a`, `record.ts` md5 `c9716be21b3122540c9be53a8aa70936`.

**No production seam was needed.** W10-01's report named one (house rule 22: `export STAT_ORDER` and
`STATS` for the jsdom half). Reading the rendered row instead of importing the table closes the same
hole and closes it better: it holds for **any** production shape that draws that row, including the
second row `StatRow` writes out by hand, which the exported-constant form could not see.

**One line beyond the filed rows, this pass's own:** `the healthy dashboard > draws the design's six
tiles in the design's order, and no seventh`. `StatRow` writes the six cards out by hand and
`NoClusterChosen` loops over `STAT_ORDER`; nothing compared them. Mutation: swap the `brokers` and
`topics` cards in `StatRow`. **RED** — 1 failed / 100.

**The `formatBytes` header is still false and this pass did not repair it** — the scaladoc promises
"the printed figure is always in `[0, 1000)`" and `formatBytes(1.5e15)` is `1500.0 TB`. The case
asserts the shipped behaviour and says in a comment that TB is deliberately the ceiling. Correcting
the sentence is a one-line edit in `record.ts`, which is W10-01's file. **Handed back, §4.**

### W10-06 — the latch, the cap and the tautology (3 of 3)

| finding | case landed | mutation | result |
| --- | --- | --- | --- |
| **the `seen` latch is not monotone** | `StreamProxySuite` — *theTerminalLatchIsNotClearedByTheBytesThatFollowIt*: a terminal frame, then a `: keepalive` comment, then a bare newline, with `sawTerminal` read after each | `StreamProxy.scala:150` — `seen.update(_ \|\| complete.exists(isTerminalLine))` → `seen.update(_ => …)` | **RED** — `1 failed, 0 ignored, 17 total`, and it is the **only** failure: the flaky case W10-06's report saw reddening did not this time |
| **`MAX_NAMED_UNREADABLE` is held by nothing** | `model.test.ts` — *names ten of them, counted out of the sentence and not derived from the bound*: `expect(MAX_NAMED_UNREADABLE).toBe(10)` **and** the `ROW_n` names counted out of the finished sentence against a literal ten | `model.ts:361` — `= 10` → `= 30` | **RED** — `expected 30 to be 10`, 1 failed / 22 |
| **the annotated-port check's second half is a tautology** | `ArchitectureDocumentSuite` — *theRowReaderRefusesTheDisagreementsItIsWrittenToRefuse*: the row reader extracted into `portDisagreements(root, service, cell)` and driven over a **fixture tree** holding a port in both layers, a port in neither, an annotation pointing at a layer that does not exist, and a row for a service with no `domain` | the extracted reader — `Option.when(claimed.isDefined && inDomain.contains(name))` → `… && !inDomain.contains(name)`, the tautology W10-06's report built with `\|\| inDomain.contains(name)` | **RED** — 1 failed / 10 |

Suites: `./mill --no-daemon services.gateway.api.test.testOnly kui.gateway.api.StreamProxySuite`,
`… kui.gateway.api.ArchitectureDocumentSuite`, `pnpm -C frontend vitest run
packages/feature-ksql/src/model.test.ts`. Reverts: `StreamProxy.scala` md5
`cf1ce5e13996a21ad2c8ee4802498e45`, `model.ts` md5 `0d3f8edc2f5eb1418db2a871d0363825`; the checker was
restored from a copy saved before the mutation and the file now carries no `!inDomain` clause.

The extraction is inside the suite: the four disagreements the fixture builds **cannot be written into
`ARCHITECTURE.md`** without failing the build, which is exactly why none of them was ever exercised. A
check whose only input is this repository can only fail on this repository as it is.

### W10-04 — the masking wiring (5 of 5, one of them partially)

| finding | case landed | mutation | result |
| --- | --- | --- | --- |
| **F1 — `WithheldCause` asserted against itself** | `ConfiguredRecordMaskingSuite` — three lines beside the tautology: `cause.nonEmpty`, `cause.contains("could not be decoded")`, `cause.contains("masking rule")` | `ConfiguredRecordMasking.scala:104` — `WithheldCause` emptied | **RED** — 1 failed / 15 |
| **F3 — "never applied on produce" read by nothing** | `MessageWiringSuite` — two lines inside the existing start-up-line case | `MessageWiring.scala:380` — `"Masking is never applied on produce " + "or resend"` → `"… also applied on produce " + "and resend"` | **RED** — 1 failed / 7 |
| **F4 — the counter's description** | `MaskingMetricsSuite` — *the series carries the sentence a metric catalogue is read by*, reading `getDescription` off the SDK | `MaskingMetrics.scala:44` — the description rewritten to `"Fields masked, by cluster, topic and target"` | **RED** — 1 failed / 7 |
| **F2 — which `MaskingMetrics` actually runs** | `MessageWiringSuite` — *the wiring hands the masking mask the recording metrics adapter and not the silent one*: the composition root's own source read, and every one of `MaskingMetrics`, `CacheMetrics`, `FilterMetrics` required to appear as `.otel4s` and refused as `.noop` | `MessageWiring.scala:129` — `Resource.eval(MaskingMetrics.otel4s[F](meter))` → `Resource.eval(MaskingMetrics.noop[F].pure[F])`, W10-04's exact mutation | **RED** — 1 failed / 7 |
| **F7 — `docs/operations/*.md` read by nothing** | `MetricNamesSuite` (`libs/observability`, a module nobody owned this wave) — *the operator's metric table names exactly the metrics this build declares*: `observability.md`'s metric table parsed, both set differences asserted against `Nil`, the one documented exception (`kui.gateway.aggregation.section`) asserted rather than silently filtered, and a `> 20 rows` guard so a reader that matched nothing cannot pass vacuously | (a) the `kui.masking.applied` row deleted from `observability.md`; (b) `kui.filter.compile` renamed to `kui.filter.compiled` in the same table | **RED on each** — 1 failed / 8 both times |

Suites: `./mill --no-daemon services.message.__.test` (1442/1442 green with the cases in and the
mutations out), `… services.message.app.test.testOnly kui.message.app.MessageWiringSuite`,
`… services.message.infrastructure.test.testOnly …`, `… libs.observability.test.testOnly
kui.observability.MetricNamesSuite`. Reverts: `MessageWiring.scala` md5
`49c069b7926ceea48d988a4422a9e740`, `ConfiguredRecordMasking.scala` md5
`a1af858eb1fa8afb0e7fb566956bdb7c`, `MaskingMetrics.scala` md5 `7e861a64dc1c0625a643ee7a7a1b87cc`,
`observability.md` md5 `edc6de0e437e5d8361040946f44a2ac8`.

**F2 is closed at second best, and the seam is still filed.** The assertion that belongs here is the
one W10-04's report names — widen `MessageWiring.resource` to publish the constructed
`MaskingMetrics`, the same `private[app]` widening `describeMasking` already carries, and record a
masked read through an `OtelJavaTestkit` meter. That is a production edit in another packet's file.
Reading the composition root's text holds the exact defect filed (a `noop` adapter in the production
path) and nothing more; it is labelled as a stopgap in its own comment. **Handed back, §4.**

**F7 is closed for one of three documents.** `observability.md`'s metric table is now read by a
suite. `masking.md` and `configuration.md` are still read by nothing, and the general form — a claim
kind comparing a sentence with a fact — is W10-03 item 7's, not this pass's.

### W10-02 — the search box's healthy arm (1 of 2)

| finding | case landed | mutation | result |
| --- | --- | --- | --- |
| **the empty-state left conjunct** | `shell/src/data/search.test.ts` — *is ready when everybody answered and something matched*: `partial` is `[]`, `searchHitCount > 0`, and the status is `ready` | `search.ts:181` — `searchHitCount(state.answer) === 0 && state.answer.partial.length === 0` → `state.answer.partial.length === 0` | **RED** — `expected 'empty' to be 'ready'`, 1 failed / 79 |

Suite: `pnpm -C frontend vitest run packages/shell/src/data/search.test.ts
packages/kernel/src/components/surfaces.test.tsx`. Revert: `search.ts` restored from its saved copy;
`git status` clean for that path.

Closed in the **unit** tree rather than with the browser case W10-02's report prescribed. The rule is
a function of plain data — *the four arms of `searchStatus`* — and three of the four were driven while
the fourth, the one every healthy deployment takes, was not. The prescribed browser case would close
the same hole at forty times the cost and only on a deployment that answers `partial: []`, which is
every deployment the suite runs against and therefore the arm it could never distinguish.

---

## 3. Findings ARGUED DOWN, with the argument

### W10-02's bulk bar — the rule is gated; the browser is what is blind

W10-02 filed `BulkActionBar.tsx:112` — `<Show when={props.count > 0}>` → `<Show when={true}>` — as
ungated, having measured it in the **bundle** against `pnpm -C frontend e2e` (109 tests, 106 passed,
green).

Applied to the **source** and measured against the suite that exercises that component, it is red:

```
pnpm -C frontend vitest run packages/kernel/src/components/surfaces.test.tsx
FAIL  BulkActionBar > renders nothing at all at zero selection
AssertionError: expected <div class="kui-bulkbar" …(2)>…(6)</div> to be null
```

`surfaces.test.tsx:991` has held this rule since it was written, with the same argument in its
docstring ("a bar that is always in the document is a strip of the window nobody can use"). This is
precisely the census failure the brief warns about: a finding measured against the wrong suite. It is
**not counted in the denominator as a closure and not counted as ungated.**

What W10-02 found is real and narrower: **`e2e/topics.spec.ts` could not see it**, because the file
referenced `topic-bulk-bar` only after a tick. That half is repaired — `topics.spec.ts:396` now
asserts `await expect(page.getByTestId("topic-bulk-bar")).toHaveCount(0)` **before** the first tick,
which is green on the running stack (`e2e` below: 106 passed, 3 skipped, 0 failed). Its red proof
would need a patched bundle served beside the stack; this pass did not build one, and says so rather
than claiming a measurement it did not make.

---

## 4. Findings HANDED BACK, with the seam named

Nine rows. None is disputed; each needs an edit in a file this packet does not own.

| # | file and edit | then the case is | owner |
| --- | --- | --- | --- |
| **W10-03 F-1** | `scripts/feature-matrix-check.sh` — a second `verify_service_fact_independence`-shaped fixture **after** `audit_service_states` (line 2598), seeding `declared_service_state[fixture-routed]='Built and routed'` and asserting `service_state_fact fixture-routed "$root" "$contracts"` still answers `Not built but routed`; kind `service-fact-roster-independence` into `registry[capability-claims]`, `close_section capability-claims 35` → `36` | the fixture itself | **W10-03.** This packet owns the script's **§6 `guard-fixtures`** only, and the fixture cannot live there: `service_state_fact` is defined at line 2404, after §6 has already run, so a call from §6 is a `command not found`. The placement the finding requires is §8's. |
| **W10-03 F-2** | the `capability-prose` claim kind over the marked block's residue | as written in `W10-03.md` | W10-03 |
| **W10-03 F-3, F-4** | `docs/overview/README.md` — `390 claims` → `404`, and a `<!-- checked: gate-table -->` region around §5 | — | W10-03 |
| **W10-04 F2's seam** | `MessageWiring.scala` — widen `resource` (or a helper) to publish the constructed `MaskingMetrics`, as `describeMasking` already is | `MessageWiringSuite` over `OtelJavaTestkit.inMemory`, asserting `kui.masking.applied` is recorded by what the process actually holds. The text-reading case landed here is the stopgap, not the answer. | W10-04 |
| **W10-04 F7's other two documents** | `docs/operations/masking.md` and `configuration.md` | a claim kind that compares a sentence with a fact — W10-03 item 7 | W10-03 / W10-04 |
| **W10-01's `formatBytes` header** | `record.ts:151` — "the printed figure is always in `[0, 1000)`" is false at the top unit, where the shipped answer is `1500.0 TB`; either correct the sentence or add `PB` | `record.test.ts` already pins the behaviour either way | W10-01 |
| **W10-05 #1** | `deployment/compose/smoke.sh` — read the first SSE frame (`curl -sN … \| head -c 200`) and fail on `^event: error` | the script's own arm | W10-05 |
| **W10-05 #2, #3** | `.github/workflows/ci.yml:170-208` — make the sorted-name `diff` the exit-status test rather than the count | the step's own run script | W10-05 |
| **W10-05 #4** | `build.mill:134-166` — extract the `(exitCode, out, err) -> Either[String, Unit]` decision into a testable object | three cases in `build-tests` | W10-05 |
| **W10-05 #5** | `frontend/tsconfig.json` (a reference to `e2e/tsconfig.json`) **or** `frontend/package.json` (a `typecheck:e2e` script) plus the CI job | `e2e/tsconfig.json` is inside this packet's ownership and `composite: true` alone changes nothing: the solution file that would reference it is not | nobody owns either file this wave |

---

## 5. Acceptance, as it actually printed

Run **strictly one at a time**. This pass started **no** background load: `pgrep -af
"vitest|playwright|http-server"` at the end finds nothing of this pass's, and the only Mill process is
the shared daemon.

| # | command | what it printed |
| --- | --- | --- |
| A1 | `./scripts/run-tests.sh` | **exit 0 — `81 modules (81 with tests), 4345 test cases, all passing.`** |
| A2 | `pnpm -C frontend test` | **exit 0 — `Test Files 83 passed (83) / Tests 1933 passed (1933)`, 22.15 s** |
| A3 | `pnpm -C frontend e2e` | **`106 passed, 3 skipped` (2.1 m), 0 failed** — including `topics.spec.ts:396` with the new zero-selection assertion |
| A4 | `./mill --no-daemon -k __.checkFormat` | **495/495 targets, 3 FAILED — none of them this packet's**: `services/gateway/api/src/…/auth/AuthRoutes.scala`, `…/test/…/auth/SessionMiddlewareSuite.scala` and `…/auth/AuthRoutesSuite.scala`, plus `services/ksql/infrastructure/test/…/KsqlHttpSuite.scala`. All four are **W10-A1's** files and all four were modified in the tree while this ran. This packet's four modules re-checked clean on their own: `{services.gateway.api.test, libs.observability.test, services.message.app.test, services.message.infrastructure.test}.checkFormat` → **18/18, SUCCESS**. |

**A1 was run twice and the first run is worth recording.** At 00:52 it came back
`1 FAILED — kui.ksql.infrastructure.KsqlHttpSuite.a failure that is not a response names the
exception's kind and its own words`, reproducible on a scoped re-run. `KsqlHttpSuite.scala` carried
98 added lines at that moment and `services/ksql/infrastructure/src` was unmodified, so it was
W10-A1's case red in its own tree, not a mutation of this pass. By 01:05 the same command printed
4,345 passing. **The tree was being written by another packet throughout this pass**, which is worth
saying plainly: A1's figures above are a measurement of a tree two packets were writing, they came
back green, and house rule 10 says to say so rather than to publish them as this packet's alone.

**The deltas.**

* Frontend: **+6 cases, and all six are this packet's** — `overview.render.test.tsx` +3 (the DOM row,
  the per-tile subject, the healthy-row order), `record.test.ts` +1, `model.test.ts` +1,
  `search.test.ts` +1. `W10-01.md` measured the whole workspace at **1,927** before this pass and it
  now reads 1,933, which is exactly those six; the assertions added inside existing cases (the empty
  state's words, the `WithheldCause` sentence, the start-up line's claim, the e2e zero-selection
  check) add no case.
* Scala: **+5 cases, and all five are this packet's** — `StreamProxySuite` 16 → 17,
  `ArchitectureDocumentSuite` 9 → 10, `MetricNamesSuite` 7 → 8, `MaskingMetricsSuite` 6 → 7,
  `MessageWiringSuite` 6 → 7, each counted from the run this pass made before its own edit. The
  repository total is 4,345 here; **this pass did not measure the total before it started**, and two
  other packets were landing cases in the same checkout, so no delta over the whole tree is claimed.
* `e2e` unchanged at 106/3/0: the bulk-bar repair is an assertion inside an existing case.

---

## 6. The tree, checked rather than claimed

Eighteen mutations were applied across this pass, in ten files. Every one was restored from a copy
this packet made **before** the edit, and every restore was confirmed by md5:

```
frontend/packages/shell/src/overview/Overview.tsx                     f2a65278178d65337b18cbaffa291f9a  OK
frontend/packages/kernel/src/components/record.ts                     c9716be21b3122540c9be53a8aa70936  OK
frontend/packages/feature-ksql/src/model.ts                           0d3f8edc2f5eb1418db2a871d0363825  OK
frontend/packages/shell/src/data/search.ts                            f7e5dc3b602fe0271ecf527a0b7609ff  OK
frontend/packages/kernel/src/components/BulkActionBar.tsx             783c9fbbe12b2bf7922a1f3a1345df16  OK
services/gateway/api/src/kui/gateway/api/StreamProxy.scala            cf1ce5e13996a21ad2c8ee4802498e45  OK
services/message/app/src/kui/message/app/MessageWiring.scala          49c069b7926ceea48d988a4422a9e740  OK
services/message/infrastructure/src/.../ConfiguredRecordMasking.scala a1af858eb1fa8afb0e7fb566956bdb7c  OK
services/message/infrastructure/src/.../MaskingMetrics.scala          7e861a64dc1c0625a643ee7a7a1b87cc  OK
docs/operations/observability.md                                      edc6de0e437e5d8361040946f44a2ac8  OK
```

`git -C /home/worxbend/kui status --porcelain` at the end of this pass carries **no production file
this packet mutated in a modified state**. What this packet leaves changed is eleven files, and every
one of them is a test source or this report:

```
frontend/e2e/topics.spec.ts
frontend/packages/feature-ksql/src/model.test.ts
frontend/packages/kernel/src/components/record.test.ts
frontend/packages/shell/src/data/search.test.ts
frontend/packages/shell/src/overview/overview.render.test.tsx
libs/observability/test/src/kui/observability/MetricNamesSuite.scala
services/gateway/api/test/src/kui/gateway/api/ArchitectureDocumentSuite.scala
services/gateway/api/test/src/kui/gateway/api/StreamProxySuite.scala
services/message/app/test/src/kui/message/app/MessageWiringSuite.scala
services/message/infrastructure/test/src/.../ConfiguredRecordMaskingSuite.scala
services/message/infrastructure/test/src/.../MaskingMetricsSuite.scala
docs/plan/verification/W10-A2.md
```

`ArchitectureDocumentSuite.scala` was run through `./mill services.gateway.api.test.reformat` after
editing, which rewrapped five lines of one scaladoc this packet wrote and nothing else (diffed against
the pre-reformat copy).

---

## 7. For the integrator

1. **`W10-A1.md` does not exist.** House rule 18 says a verification pass writes its file first, as a
   stub. Five of six filed; the hunter did not. Whatever it found is in its own transcript and in the
   tree, and this closer never saw it.
2. **Three of this wave's four `checkFormat` failures are live in `services/gateway/api/.../auth/`**
   and one in `services/ksql/infrastructure/test/`. They are W10-A1's and they were not there twenty
   minutes before the acceptance run. They need a `reformat` before the wave is recorded.
3. **The rule this wave should keep.** Reading a rendered row off the DOM, rather than looping over a
   literal of ids the test file already knows, is what closed four of W10-01's six findings with one
   case and no production seam. Four hand-written rosters of the same six tiles exist across this
   repository (`overview.render.test.tsx`, `shell.spec.ts`, `traffic.spec.ts`, and `Overview.tsx`'s own
   `STAT_ORDER`); the case that reads the row is the only one of them that can notice a seventh.
